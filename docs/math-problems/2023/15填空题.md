## 🎯 题目回顾

**题目**：设连续函数 $f(x)$ 满足 $f(x+2)-f(x)=x, \int_{0}^{2}f(x)dx=0$，则 $\int_{1}^{3}f(x)dx=$ \_\_\_\_\_\_。

---

## 第一部分：积分区间拆分与换元

### 1.1 拆分目标积分

为了利用已知条件 $\int_{0}^{2}f(x)dx$，我们将目标积分 $\int_{1}^{3}f(x)dx$ 拆分为两部分：
$$
\int_{1}^{3}f(x)dx = \int_{1}^{2}f(x)dx + \int_{2}^{3}f(x)dx
$$

### 1.2 对第二部分积分进行换元

对于右边的第二项 $\int_{2}^{3}f(x)dx$，我们进行换元，目的是将其区间平移回 $[0, 1]$ 附近，以便利用函数方程。
令 $x = t + 2$，则 $dx = dt$。
- 当 $x = 2$ 时，$t = 0$；
- 当 $x = 3$ 时，$t = 1$。

代入得：
$$
\int_{2}^{3}f(x)dx = \int_{0}^{1}f(t+2)dt
$$

---

## 第二部分：利用函数方程化简

### 2.1 代入递推关系

已知 $f(x+2) - f(x) = x$，即 $f(x+2) = f(x) + x$。
将此关系代入上面的积分（注意积分变量符号可由 $t$ 换回 $x$）：
$$
\begin{aligned}
\int_{0}^{1}f(t+2)dt &= \int_{0}^{1} [f(t) + t] dt \\
&= \int_{0}^{1}f(t)dt + \int_{0}^{1}t dt \\
&= \int_{0}^{1}f(x)dx + \int_{0}^{1}x dx
\end{aligned}
$$

### 2.2 合并积分项

现在回到第一步的拆分式：
$$
\begin{aligned}
\int_{1}^{3}f(x)dx &= \int_{1}^{2}f(x)dx + \left( \int_{0}^{1}f(x)dx + \int_{0}^{1}x dx \right) \\
&= \left( \int_{0}^{1}f(x)dx + \int_{1}^{2}f(x)dx \right) + \int_{0}^{1}x dx
\end{aligned}
$$
根据定积分的可加性 $\int_{0}^{1} + \int_{1}^{2} = \int_{0}^{2}$：
$$
\int_{1}^{3}f(x)dx = \int_{0}^{2}f(x)dx + \int_{0}^{1}x dx
$$

---

## 第三部分：计算最终结果

### 3.1 代入已知值

题目已知 $\int_{0}^{2}f(x)dx = 0$。

### 3.2 计算剩余积分

计算 $\int_{0}^{1}x dx$：
$$
\int_{0}^{1}x dx = \left[ \frac{1}{2}x^2 \right]_{0}^{1} = \frac{1}{2}(1^2 - 0) = \frac{1}{2}
$$

### 3.3 得出结论

$$
\int_{1}^{3}f(x)dx = 0 + \frac{1}{2} = \frac{1}{2}
$$

---

## ✅ 最终答案

$$
\frac{1}{2}
$$

---

## 📌 关键要点总结

1.  **区间平移技巧**：当题目给出 $f(x+T)$ 与 $f(x)$ 的关系时，处理积分 $\int_{a}^{a+T} f(x) dx$ 通常需要将其拆分或换元，将其转化为 $\int_{0}^{T} f(x) dx$ 的形式。
2.  **定积分的可加性**：$\int_a^b + \int_b^c = \int_a^c$ 是重新组合积分区间的核心工具。
3.  **构造变上限函数法（扩展思路）**：
    设 $F(x) = \int_{x}^{x+2} f(t) dt$，对 $x$ 求导得 $F'(x) = f(x+2) - f(x) = x$。
    则 $F(x) = \frac{1}{2}x^2 + C$。
    由 $F(0) = \int_{0}^{2} f(t) dt = 0$ 可知 $C=0$。
    故 $\int_{1}^{3} f(t) dt = F(1) = \frac{1}{2}(1)^2 = \frac{1}{2}$。此方法更为简捷。