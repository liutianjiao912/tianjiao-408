## 🎯 题目回顾

**5.** 设函数 $y=f(x)$ 由 $\begin{cases}x=2t+|t|,\\ y=|t|\sin t\end{cases}$ 确定，则（ ）。

(A) $f(x)$ 连续， $f^{\prime}(0)$ 不存在

(B) $f^{\prime}(0)$ 存在， $f^{\prime}(x)$ 在 $x=0$ 处不连续

(C) $f^{\prime}(x)$ 连续， $f^{\prime\prime}(0)$ 不存在

(D) $f^{\prime\prime}(0)$ 存在， $f^{\prime\prime}(x)$ 在 $x=0$ 处不连续

---

## 第一部分：去绝对值，化为分段函数

由于表达式中含有 $|t|$，我们需要根据 $t$ 的符号分情况讨论，消去参数 $t$，得到 $y$ 关于 $x$ 的显函数 $f(x)$。

### 1.1 当 $t \ge 0$ 时

- $x = 2t + t = 3t \implies t = \frac{x}{3}$ （此时 $x \ge 0$）
- $y = t \sin t$
- 代入消参得：
  $$f(x) = \frac{x}{3} \sin \frac{x}{3}, \quad x \ge 0$$

### 1.2 当 $t < 0$ 时

- $x = 2t - t = t$ （此时 $x < 0$）
- $y = -t \sin t$
- 代入消参得：
  $$f(x) = -x \sin x, \quad x < 0$$

### 1.3 综合函数表达式

$$
f(x) = \begin{cases} 
\frac{x}{3} \sin \frac{x}{3}, & x \ge 0 \\
-x \sin x, & x < 0
\end{cases}
$$

显然 $\lim_{x\to 0} f(x) = 0 = f(0)$，函数连续。

---

## 第二部分：一阶导数 $f'(x)$ 的分析

### 2.1 非零处的导数

利用乘法法则和链式法则求导：

- **当 $x > 0$ 时**：
  $$
  f'(x) = \left( \frac{x}{3} \sin \frac{x}{3} \right)' = \frac{1}{3} \sin \frac{x}{3} + \frac{x}{3} \cos \frac{x}{3} \cdot \frac{1}{3} = \frac{1}{3} \sin \frac{x}{3} + \frac{x}{9} \cos \frac{x}{3}
  $$

- **当 $x < 0$ 时**：
  $$
  f'(x) = (-x \sin x)' = -(\sin x + x \cos x) = -\sin x - x \cos x
  $$

### 2.2 $x=0$ 处的导数 $f'(0)$

利用导数定义计算左右导数：

- **右导数**：
  $$
  f'_+(0) = \lim_{x \to 0^+} \frac{f(x) - f(0)}{x} = \lim_{x \to 0^+} \frac{\frac{x}{3} \sin \frac{x}{3}}{x} = \lim_{x \to 0^+} \frac{1}{3} \sin \frac{x}{3} = 0
  $$
- **左导数**：
  $$
  f'_-(0) = \lim_{x \to 0^-} \frac{f(x) - f(0)}{x} = \lim_{x \to 0^-} \frac{-x \sin x}{x} = \lim_{x \to 0^-} (-\sin x) = 0
  $$

由于 $f'_+(0) = f'_-(0) = 0$，故 **$f'(0)$ 存在且等于 0**。
❌ **排除 (A)**。

### 2.3 $f'(x)$ 的连续性

检查 $\lim_{x \to 0} f'(x)$ 是否等于 $f'(0)$：
- $\lim_{x \to 0^+} f'(x) = \lim_{x \to 0^+} (\frac{1}{3} \sin \frac{x}{3} + \frac{x}{9} \cos \frac{x}{3}) = 0 + 0 = 0$
- $\lim_{x \to 0^-} f'(x) = \lim_{x \to 0^-} (-\sin x - x \cos x) = 0 - 0 = 0$

极限存在且等于函数值 $f'(0)$，故 **$f'(x)$ 在 $x=0$ 处连续**。
❌ **排除 (B)**。

---

## 第三部分：二阶导数 $f''(x)$ 的分析

### 3.1 考察 $f''(0)$ 的存在性

我们通过一阶导数的差商极限来判断二阶导数是否存在：$f''(0) = \lim_{x \to 0} \frac{f'(x) - f'(0)}{x}$。

- **右二阶导数** $f''_+(0)$：
  $$
  \begin{aligned}
  f''_+(0) &= \lim_{x \to 0^+} \frac{f'(x) - 0}{x} = \lim_{x \to 0^+} \frac{\frac{1}{3} \sin \frac{x}{3} + \frac{x}{9} \cos \frac{x}{3}}{x} \\
  &= \lim_{x \to 0^+} \left( \frac{1}{3} \cdot \frac{\sin(x/3)}{x} + \frac{1}{9} \cos \frac{x}{3} \right) \\
  &= \frac{1}{3} \cdot \frac{1}{3} + \frac{1}{9} \cdot 1 = \frac{1}{9} + \frac{1}{9} = \frac{2}{9}
  \end{aligned}
  $$

- **左二阶导数** $f''_-(0)$：
  $$
  \begin{aligned}
  f''_-(0) &= \lim_{x \to 0^-} \frac{f'(x) - 0}{x} = \lim_{x \to 0^-} \frac{-\sin x - x \cos x}{x} \\
  &= \lim_{x \to 0^-} \left( -\frac{\sin x}{x} - \cos x \right) \\
  &= -1 - 1 = -2
  \end{aligned}
  $$

### 3.2 结论

由于 $f''_+(0) = \frac{2}{9} \ne -2 = f''_-(0)$，左右二阶导数不相等。
✅ **因此 $f''(0)$ 不存在**。

---

## 第四部分：选项分析

| 选项 | 内容 | 判定 | 理由 |
|---|---|---|---|
| (A) | $f(x)$ 连续， $f^{\prime}(0)$ 不存在 | ❌ | $f'(0)=0$，存在。 |
| (B) | $f^{\prime}(0)$ 存在， $f^{\prime}(x)$ 在 $x=0$ 处不连续 | ❌ | $f'(x)$ 连续。 |
| (C) | $f^{\prime}(x)$ 连续， $f^{\prime\prime}(0)$ 不存在 | ✅ | 符合上述所有计算结果。 |
| (D) | $f^{\prime\prime}(0)$ 存在... | ❌ | $f''(0)$ 不存在。 |

---

## ✅ 最终答案：**(C)**

---

## 📌 关键要点总结

1.  **参数方程消参**：遇到含绝对值的参数方程，第一步永远是**分段**（$t \ge 0$ 和 $t < 0$）将其转化为显函数 $y=f(x)$。
2.  **导数存在性判断**：分段点处的导数（$f'(0), f''(0)$）必须严格使用**定义的极限形式**（左右导数）计算，不能直接代入导函数公式。
3.  **$C^1$ 连续性**：即函数可导且导函数连续。本题中 $f(x)$ 属于 $C^1$ 类函数，但不是 $C^2$ 类（二阶导数不存在）。