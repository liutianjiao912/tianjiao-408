## 🎯 问题回顾

已知平面区域 $D=\left\{(x,y) \mid 0\le y\le\dfrac{1}{x\sqrt{1+x^{2}}}, x\ge1\right\}$ 。

需要解决以下两个问题：
1.  求区域 $D$ 的面积 $S$；
2.  求区域 $D$ 绕 $x$ 轴旋转所成旋转体的体积 $V$。

---

## 第一部分：求区域 $D$ 的面积

### 1.1 面积积分公式

根据定积分的几何意义，平面区域的面积 $S$ 由广义积分给出：
$$
S = \int_{1}^{+\infty} y(x) \, dx = \int_{1}^{+\infty} \dfrac{1}{x\sqrt{1+x^{2}}} \, dx
$$

这是一个无穷限的广义积分，严格定义为：
$$
S = \lim_{b \to +\infty} \int_{1}^{b} \dfrac{1}{x\sqrt{1+x^{2}}} \, dx
$$

### 1.2 计算积分

#### ✅ 方法：倒代换或三角代换

此处我们采用**倒代换**（令 $x = \frac{1}{t}$），这是一种处理分母含有 $x\sqrt{1+x^2}$ 形式的标准技巧。

1.  **变量代换**：
    令 $x = \dfrac{1}{t}$，则 $dx = -\dfrac{1}{t^2} dt$。
    同时处理根号项：
    $$
    \sqrt{1+x^2} = \sqrt{1+\frac{1}{t^2}} = \frac{\sqrt{t^2+1}}{t} \quad (\text{因 } x\ge 1 \implies 0 < t \le 1, t>0)
    $$

2.  **确定积分限**：
    - 当 $x = 1$ 时，$t = 1$；
    - 当 $x \to +\infty$ 时，$t \to 0^+$。

3.  **代入积分**：
    $$
    \begin{aligned}
    \int \dfrac{1}{x\sqrt{1+x^{2}}} \, dx &= \int \dfrac{1}{\frac{1}{t} \cdot \frac{\sqrt{t^2+1}}{t}} \cdot \left(-\dfrac{1}{t^2}\right) \, dt \\
    &= \int \dfrac{t^2}{\sqrt{t^2+1}} \cdot \left(-\dfrac{1}{t^2}\right) \, dt \\
    &= -\int \dfrac{1}{\sqrt{t^2+1}} \, dt
    \end{aligned}
    $$

4.  **计算定积分**：
    这是一个常用积分公式：$\int \frac{1}{\sqrt{t^2+1}} dt = \ln(t + \sqrt{t^2+1})$。

    $$
    \begin{aligned}
    S &= \int_{1}^{0} -\dfrac{1}{\sqrt{t^2+1}} \, dt = \int_{0}^{1} \dfrac{1}{\sqrt{t^2+1}} \, dt \\
    &= \left[ \ln(t + \sqrt{t^2+1}) \right]_{0}^{1} \\
    &= \ln(1 + \sqrt{2}) - \ln(0 + \sqrt{1}) \\
    &= \ln(1 + \sqrt{2})
    \end{aligned}
    $$

### 1.3 面积结论

✅ **区域 $D$ 的面积为 $\ln(1 + \sqrt{2})$**。

---

## 第二部分：求旋转体的体积

### 2.1 体积积分公式

根据旋转体体积公式，区域 $D$ 绕 $x$ 轴旋转的体积 $V$ 为：
$$
V = \pi \int_{1}^{+\infty} [y(x)]^2 \, dx = \pi \int_{1}^{+\infty} \left( \dfrac{1}{x\sqrt{1+x^{2}}} \right)^2 \, dx
$$

化简被积函数：
$$
V = \pi \int_{1}^{+\infty} \dfrac{1}{x^2(1+x^2)} \, dx
$$

### 2.2 计算积分

#### ✅ 方法：裂项法（部分分式分解）

观察被积函数 $\dfrac{1}{x^2(1+x^2)}$，利用代数恒等式进行拆分：
$$
\dfrac{1}{x^2(1+x^2)} = \dfrac{(1+x^2) - x^2}{x^2(1+x^2)} = \dfrac{1}{x^2} - \dfrac{1}{1+x^2}
$$

代入体积公式：
$$
V = \pi \lim_{b \to +\infty} \int_{1}^{b} \left( \dfrac{1}{x^2} - \dfrac{1}{1+x^2} \right) \, dx
$$

计算原函数：
$$
\int \left( \dfrac{1}{x^2} - \dfrac{1}{1+x^2} \right) \, dx = -\dfrac{1}{x} - \arctan x
$$

代入上下限：
$$
\begin{aligned}
V &= \pi \lim_{b \to +\infty} \left[ -\dfrac{1}{x} - \arctan x \right]_{1}^{b} \\
&= \pi \left\{ \lim_{b \to +\infty} \left( -\dfrac{1}{b} - \arctan b \right) - \left( -1 - \arctan 1 \right) \right\}
\end{aligned}
$$

处理极限值：
- $\lim_{b \to +\infty} (-\frac{1}{b}) = 0$
- $\lim_{b \to +\infty} (\arctan b) = \frac{\pi}{2}$
- $\arctan 1 = \frac{\pi}{4}$

计算最终结果：
$$
\begin{aligned}
V &= \pi \left[ \left( 0 - \dfrac{\pi}{2} \right) - \left( -1 - \dfrac{\pi}{4} \right) \right] \\
&= \pi \left[ -\dfrac{\pi}{2} + 1 + \dfrac{\pi}{4} \right] \\
&= \pi \left( 1 - \dfrac{\pi}{4} \right) \\
&= \pi - \dfrac{\pi^2}{4}
\end{aligned}
$$

### 2.3 体积结论

✅ **旋转体的体积为 $\pi - \dfrac{\pi^2}{4}$**。

---

## 🎯 最终答案总结

| 项目 | 结果 |
| :--- | :--- |
| **(1) 区域面积** | $S = \ln(1 + \sqrt{2})$ |
| **(2) 旋转体体积** | $V = \pi - \dfrac{\pi^2}{4}$ |

---

## 📌 关键要点回顾

1.  **广义积分的处理**：题目涉及区间 $[1, +\infty)$，本质是广义积分。计算时虽然可以简写，但内心必须明确这是极限 $\lim_{b \to +\infty}$ 的过程。
2.  **积分技巧的选择**：
    * 求面积时遇到 $\frac{1}{x\sqrt{1+x^2}}$，**倒代换** ($x=1/t$) 是消去分母中 $x$ 的最快路径。
    * 求体积时平方后变为有理函数 $\frac{1}{x^2(1+x^2)}$，**裂项法** (Partial Fractions) 是标准解法，避免了复杂的三角代换。
3.  **常见极限值**：熟记 $\arctan(+\infty) = \frac{\pi}{2}$ 和 $\arctan(1) = \frac{\pi}{4}$ 是准确计算体积的关键。