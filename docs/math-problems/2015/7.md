---
title: 2015 数学二真题 第7题
---

我们来**详细分析这道线性代数题目**，从题设到解析过程逐层剖析，并验证其正确性。

---

### **题目原文**

设矩阵

$$
A = \begin{pmatrix}
1 & 1 & 1 \\
1 & 2 & a \\
1 & 4 & a^2
\end{pmatrix}, \quad
\mathbf{b} = \begin{pmatrix}
1 \\ d \\ d^2
\end{pmatrix}
$$

若集合 $\Omega = \{1, 2\}$，则线性方程组 $A\mathbf{x} = \mathbf{b}$有无穷多解的充分必要条件为：

(A)$a \notin \Omega, d \notin \Omega$(B)$a \notin \Omega, d \in \Omega$(C)$a \in \Omega, d \notin \Omega$(D)$a \in \Omega, d \in \Omega$

【答案】D

【解析】

增广矩阵：

$$
(A, \mathbf{b}) = \left(\begin{array}{ccc|c}
1 & 1 & 1 & 1 \\
1 & 2 & a & d \\
1 & 4 & a^2 & d^2
\end{array}\right)
\rightarrow
\left(\begin{array}{ccc|c}
1 & 1 & 1 & 1 \\
0 & 1 & a-1 & d-1 \\
0 & 0 & (a-1)(a-2) & (d-1)(d-2)
\end{array}\right)
$$

由 $r(A) = r(A, \mathbf{b}) < 3$，故 $a = 1$或$a = 2$，同时 $d = 1$或$d = 2$。故选 (D)

---

## **一、问题本质：何时齐次方程组有无穷多解？**

线性方程组 $A\mathbf{x} = \mathbf{b}$ 有**无穷多解**的充要条件是：

> $$
> \text{秩}(A) = \text{秩}(A, \mathbf{b}) < n
> $$

其中 $n$是未知数个数（这里是3）。

所以必须满足：
-$\text{rank}(A) = \text{rank}(A, \mathbf{b})$
- 并且这个秩小于3 → 即系数矩阵不满秩。

所以我们需要通过初等行变换化简增广矩阵，判断什么时候秩小于3，且增广矩阵与系数矩阵同秩。

---

## **二、构造增广矩阵并化简**

给定：
$$
(A, \mathbf{b}) =
\begin{pmatrix}
1 & 1 & 1 & | & 1 \\
1 & 2 & a & | & d \\
1 & 4 & a^2 & | & d^2
\end{pmatrix}
$$

进行初等行变换。

### 第一步：消去第一列下方元素

$R_2 \leftarrow R_2 - R_1$，$R_3 \leftarrow R_3 - R_1$

$$
\Rightarrow
\begin{pmatrix}
1 & 1 & 1 & | & 1 \\
0 & 1 & a-1 & | & d-1 \\
0 & 3 & a^2 - 1 & | & d^2 - 1
\end{pmatrix}
$$

### 第二步：消去第二列下方元素

用 $R_3 \leftarrow R_3 - 3R_2$

计算：
- 第三行第2列：$3 - 3 \times 1 = 0$
- 第三行第3列：$(a^2 - 1) - 3(a - 1) = a^2 - 1 - 3a + 3 = a^2 - 3a + 2$
- 第三行第4列（常数项）：$(d^2 - 1) - 3(d - 1) = d^2 - 1 - 3d + 3 = d^2 - 3d + 2$注意：
-$a^2 - 3a + 2 = (a-1)(a-2)$-$d^2 - 3d + 2 = (d-1)(d-2)$

所以得到：
$$
\begin{pmatrix}
1 & 1 & 1 & | & 1 \\
0 & 1 & a-1 & | & d-1 \\
0 & 0 & (a-1)(a-2) & | & (d-1)(d-2)
\end{pmatrix}
$$

这就是题目中给出的化简结果。

---

## **三、分析秩的情况**

我们现在有阶梯形矩阵：

$$
\begin{pmatrix}
1 & 1 & 1 & | & 1 \\
0 & 1 & a-1 & | & d-1 \\
0 & 0 & (a-1)(a-2) & | & (d-1)(d-2)
\end{pmatrix}
$$

记第三行主元为 $(a-1)(a-2)$，它决定是否能继续往下。

### 情况分类：

#### **情况1：$(a-1)(a-2) \neq 0$**

即 $a \ne 1$且$a \ne 2$，此时第三行非零 → $\text{rank}(A) = 3$- 此时若$(d-1)(d-2) \ne 0$，则 $\text{rank}(A,\mathbf{b}) = 3$，有唯一解。
- 若 $(d-1)(d-2) = 0$，即 $d=1$或$d=2$，但此时右边是0，左边非零 → 矛盾！
  - 因为第三行变为：$0x_1 + 0x_2 + c x_3 = 0$，其中 $c \ne 0$，所以 $x_3 = 0$，但右边是0，可以解。
  - 实际上，只要 $(d-1)(d-2) = 0$，那么右边为0，左边主元非零，仍然可以解出 $x_3$，但整体秩仍为3。

⚠️ 所以当 $a \notin \Omega$（即 $a \ne 1,2$），无论 $d$如何，都有$\text{rank}(A) = 3$，所以不可能有无穷多解。

---

#### **情况2：$(a-1)(a-2) = 0$**

即 $a = 1$或$a = 2$ → 这意味着第三行变成：

$$
0 \quad 0 \quad 0 \quad | \quad (d-1)(d-2)
$$

这时，要看这个常数项是否为0：

- 若 $(d-1)(d-2) = 0$，即 $d = 1$或$d = 2$，则第三行为全0 → $\text{rank}(A) = \text{rank}(A,\mathbf{b}) = 2 < 3$→ **有无穷多解**
- 若$(d-1)(d-2) \ne 0$，即 $d \notin \Omega$，则第三行是 $0= \text{非零}$→ 矛盾 → **无解**

因此：

|$a$|$d$| 是否有无穷多解 |
|--------|--------|----------------|
|$a \in \Omega$（即 $a=1$或$2$） | $d \in \Omega$| ✅ 有无穷多解 |
|$a \in \Omega$|$d \notin \Omega$| ❌ 无解 |
|$a \notin \Omega$| 任意$d$| ❌ 唯一解或无解，但不会无穷多解 |

所以，**有无穷多解的充要条件是：**

>$a \in \Omega$且$d \in \Omega$

也就是选项 **(D)**

---

## **四、验证具体例子**

### 示例1：$a = 1, d = 1$

则：

$$
A = \begin{pmatrix}
1 & 1 & 1 \\
1 & 2 & 1 \\
1 & 4 & 1
\end{pmatrix}, \quad
\mathbf{b} = \begin{pmatrix}1\\1\\1\end{pmatrix}
$$

观察：第三行减第一行：$(0,3,0)$，第二行减第一行：$(0,1,0)$，可看出秩为2。

增广矩阵化简后：
- 第三行：$0 \quad 0 \quad 0 \quad | \quad (1-1)(1-2)=0$
→ 全零行 → 秩为2 → 有无穷多解 ✅

---

### 示例2：$a = 1, d = 3$则$(d-1)(d-2) = 2 \times 1 = 2 \ne 0$

第三行：$0 \quad 0 \quad 0 \quad | \quad 2$ → 不可能 → 无解 ❌

---

### 示例3：$a = 3, d = 1$

$a \notin \Omega$，所以 $(a-1)(a-2) = 2 \times 1 = 2 \ne 0$，第三行主元非零 → 秩为3 → 有唯一解（除非矛盾）

检查：$(d-1)(d-2) = 0 \times (-1) = 0$，所以右边为0，左边非零 → 可解 → 唯一解 ❌（不是无穷多解）

---

## **五、为什么答案是 (D)？**

因为只有当：
- $a = 1$或$a = 2$→ 使$A$的秩下降
- 同时$d = 1$或$d = 2$ → 使增广矩阵秩不增加

才能满足：

$$
\text{rank}(A) = \text{rank}(A,\mathbf{b}) = 2 < 3
$$

从而有无穷多解。

所以充要条件是：

> $a \in \Omega$且$d \in \Omega$✅ 对应选项 **(D)**

---

## **六、解析中的逻辑漏洞？**

原解析说："由$r(A) = r(A,b) < 3$，故 $a = 1$或$a = 2$，同时 $d = 1$或$d = 2$。"

这句话**基本正确**，但略显跳跃。

更严谨地说：
- $r(A) < 3$⇒$(a-1)(a-2) = 0$⇒$a = 1$或$a = 2$-$r(A,b) = r(A)$⇒ 第三行右端也为0 ⇒$(d-1)(d-2) = 0$⇒$d = 1$或$d = 2$所以两者都需要成立。

---

## **七、结论**

### ✅ 正确答案是：**(D)**$a \in \Omega, d \in \Omega$### 🔍 核心思想：
- 无穷多解 ⇔ 系数矩阵秩 = 增广矩阵秩 < 未知数个数
- 利用行变换求出主元条件
- 分析何时出现零行和零常数项

---

### 📌 总结要点：

| 条件 | 结果 |
|------|------|
|$a \notin \Omega$|$\text{rank}(A) = 3$→ 不可能有无穷多解 |
|$a \in \Omega$且$d \in \Omega$|$\text{rank}(A) = \text{rank}(A,b) = 2$→ 无穷多解 |
|$a \in \Omega$且$d \notin \Omega$|$\text{rank}(A) = 2$, $\text{rank}(A,b) = 3$ → 无解 |

所以，**充要条件是 (D)**。

---

✅ **最终答案：(D)**
