## 🎯 问题回顾

计算二重积分：
$$
I = \iint_{D} x(x+y) dxdy
$$
其中积分区域 $D$ 由 $x^{2}+y^{2}\le2$ 和 $y\ge x^{2}$ 围成。

---

## 第一部分：分析积分区域 $D$

### 1.1 确定边界曲线及其交点

区域 $D$ 由两条曲线围成：
1.  **圆**：$x^2 + y^2 = 2$（上半部分）
2.  **抛物线**：$y = x^2$

我们需要找到这两个边界的交点，联立方程：
$$
\begin{cases}
y = x^2 \\
x^2 + y^2 = 2
\end{cases}
$$
将 $x^2 = y$ 代入圆方程：
$$
y + y^2 = 2 \implies y^2 + y - 2 = 0 \implies (y+2)(y-1) = 0
$$
解得 $y = 1$ （$y=-2$ 舍去，因为 $y=x^2 \ge 0$）。
当 $y=1$ 时，$x^2 = 1 \implies x = \pm 1$。

**交点坐标**：$(-1, 1)$ 和 $(1, 1)$。

### 1.2 绘制区域草图

* $y \ge x^2$：表示抛物线开口内部（上方）。
* $x^2 + y^2 \le 2$：表示圆内部。
* 区域 $D$ 位于抛物线 $y=x^2$ 上方且在圆 $x^2+y^2=2$ 内部，是一个关于 $y$ 轴对称的区域。

---

## 第二部分：利用对称性简化积分

### 2.1 拆分被积函数

$$
I = \iint_{D} (x^2 + xy) dxdy = \iint_{D} x^2 dxdy + \iint_{D} xy dxdy
$$

### 2.2 对称性分析

1.  **区域对称性**：区域 $D$ 关于 $y$ 轴对称（即若 $(x,y) \in D$，则 $(-x,y) \in D$）。
2.  **函数的奇偶性**：
    * 对于 $\iint_{D} xy dxdy$：被积函数 $f(x,y) = xy$ 关于 $x$ 是**奇函数**（$(-x)y = -xy$）。
        * 根据二重积分的对称性性质，在关于 $y$ 轴对称的区域上积分奇函数，结果为 0。
        * 即 $\iint_{D} xy dxdy = 0$。
    * 对于 $\iint_{D} x^2 dxdy$：被积函数 $g(x,y) = x^2$ 关于 $x$ 是**偶函数**。
        * 积分值等于右半区域积分值的 2 倍。

### 2.3 简化后的积分目标

$$
I = \iint_{D} x^2 dxdy
$$
利用右半区域 $D_1$（$x \ge 0$），我们可以写成：
$$
I = 2 \iint_{D_1} x^2 dxdy
$$
其中 $D_1 = \{(x,y) \mid 0 \le x \le 1, x^2 \le y \le \sqrt{2-x^2}\}$。

---

## 第三部分：计算积分 $\iint_{D} x^2 dxdy$

我们可以选择直角坐标系（先 $y$ 后 $x$）或极坐标系。由于边界包含圆和抛物线，直角坐标系较为直接，但为了展示通过性，我们这里采用**直角坐标系**进行计算。

### 3.1 确定积分限（X-型区域）

* $x$ 的范围：$[-1, 1]$
* $y$ 的范围：对于固定的 $x$，从抛物线 $y=x^2$ 到圆 $y=\sqrt{2-x^2}$。

$$
I = \int_{-1}^{1} dx \int_{x^2}^{\sqrt{2-x^2}} x^2 dy
$$

利用偶函数性质，改为：
$$
I = 2 \int_{0}^{1} x^2 dx \int_{x^2}^{\sqrt{2-x^2}} dy
$$

### 3.2 计算内层积分

$$
\int_{x^2}^{\sqrt{2-x^2}} dy = [y]_{x^2}^{\sqrt{2-x^2}} = \sqrt{2-x^2} - x^2
$$

### 3.3 计算外层积分

$$
I = 2 \int_{0}^{1} x^2 (\sqrt{2-x^2} - x^2) dx = 2 \left[ \underbrace{\int_{0}^{1} x^2\sqrt{2-x^2} dx}_{I_1} - \underbrace{\int_{0}^{1} x^4 dx}_{I_2} \right]
$$

#### 计算 $I_2$：
$$
I_2 = \int_{0}^{1} x^4 dx = \left[ \frac{x^5}{5} \right]_0^1 = \frac{1}{5}
$$

#### 计算 $I_1$：
令 $x = \sqrt{2}\sin t$，则 $dx = \sqrt{2}\cos t dt$。
* 当 $x=0$ 时，$t=0$。
* 当 $x=1$ 时，$\sin t = \frac{1}{\sqrt{2}} \implies t = \frac{\pi}{4}$。

$$
\begin{aligned}
I_1 &= \int_{0}^{\frac{\pi}{4}} (\sqrt{2}\sin t)^2 \cdot \sqrt{2-2\sin^2 t} \cdot (\sqrt{2}\cos t) dt \\
&= \int_{0}^{\frac{\pi}{4}} 2\sin^2 t \cdot (\sqrt{2}\cos t) \cdot (\sqrt{2}\cos t) dt \\
&= 4 \int_{0}^{\frac{\pi}{4}} \sin^2 t \cos^2 t dt \\
&= 4 \int_{0}^{\frac{\pi}{4}} (\sin t \cos t)^2 dt \\
&= 4 \int_{0}^{\frac{\pi}{4}} \left(\frac{1}{2}\sin 2t\right)^2 dt \\
&= \int_{0}^{\frac{\pi}{4}} \sin^2 2t dt
\end{aligned}
$$

使用降幂公式 $\sin^2 \theta = \frac{1-\cos 2\theta}{2}$：
$$
\begin{aligned}
I_1 &= \int_{0}^{\frac{\pi}{4}} \frac{1 - \cos 4t}{2} dt \\
&= \frac{1}{2} \left[ t - \frac{1}{4}\sin 4t \right]_{0}^{\frac{\pi}{4}} \\
&= \frac{1}{2} \left( (\frac{\pi}{4} - \frac{1}{4}\sin \pi) - (0 - 0) \right) \\
&= \frac{\pi}{8}
\end{aligned}
$$

### 3.4 合并结果

$$
I = 2 (I_1 - I_2) = 2 \left( \frac{\pi}{8} - \frac{1}{5} \right) = \frac{\pi}{4} - \frac{2}{5}
$$

---

## ✅ 最终答案

$$
\frac{\pi}{4} - \frac{2}{5}
$$

---

## 📌 关键要点总结

1.  **奇偶性简化**：在二重积分中，如果区域关于坐标轴对称，务必先检查被积函数的奇偶性。本题中 $\iint xy dxdy = 0$ 大大简化了计算。
2.  **区域划分**：准确求出交点 $(-1,1)$ 和 $(1,1)$ 是确定积分限的基础。
3.  **积分技巧**：
    * 处理 $\int x^2 \sqrt{a^2-x^2} dx$ 类型积分时，三角换元（$x=a\sin t$）是标准且高效的方法。
    * 熟练运用倍角公式 $\sin 2t = 2\sin t \cos t$ 和降幂公式。