---
title: 2015 数学二真题 第6题
---

我们来**详细分析这道线性代数题**，并逐层验证原解析的正确性。

---

### **题目：**

设矩阵  
$$
A = \begin{pmatrix}
1 & 1 & 1 \\
1 & 2 & a \\
1 & 4 & a^2
\end{pmatrix},\quad
b = \begin{pmatrix}
1 \\ d \\ d^2
\end{pmatrix}
$$

集合 $\Omega = \{1, 2\}$则线性方程组$A\mathbf{x} = \mathbf{b}$有**无穷多解**的充要条件是（ ）

选项：

(A)$a \notin \Omega,\ d \notin \Omega$(B)$a \notin \Omega,\ d \in \Omega$(C)$a \in \Omega,\ d \notin \Omega$(D)$a \in \Omega,\ d \in \Omega$**【答案】(D)**

---

## 🔍 第一步：理解“无穷多解”的条件

对于线性方程组$A\mathbf{x} = \mathbf{b}$，有**无穷多解**的充要条件是：

> $$
> \text{rank}(A) = \text{rank}(A, b) < n
> $$

其中 $n$是未知数个数（这里是 3）。

所以必须满足：
-$\text{rank}(A) = \text{rank}(A, b)$- 且都小于 3

即：系数矩阵和增广矩阵的秩相等，但小于未知数个数。

---

## ✅ 第二步：构造增广矩阵$(A, b)$

$$
(A, b) = 
\begin{pmatrix}
1 & 1 & 1 & | & 1 \\
1 & 2 & a & | & d \\
1 & 4 & a^2 & | & d^2
\end{pmatrix}
$$

进行行变换化为阶梯形。

---

## ✅ 第三步：行变换

记增广矩阵为：
$$
\left(\begin{array}{ccc|c}
1 & 1 & 1 & 1 \\
1 & 2 & a & d \\
1 & 4 & a^2 & d^2
\end{array}\right)
$$

### 第一步：消去第一列下方元素

- $R_2 \leftarrow R_2 - R_1$-$R_3 \leftarrow R_3 - R_1$

得到：
$$
\left(\begin{array}{ccc|c}
1 & 1 & 1 & 1 \\
0 & 1 & a-1 & d-1 \\
0 & 3 & a^2 - 1 & d^2 - 1
\end{array}\right)
$$

### 第二步：消去第二列下方元素

- $R_3 \leftarrow R_3 - 3R_2$

计算：
- 第二列：$3 - 3 \cdot 1 = 0$
- 第三列：$a^2 - 1 - 3(a - 1) = a^2 - 1 - 3a + 3 = a^2 - 3a + 2$
- 右侧：$d^2 - 1 - 3(d - 1) = d^2 - 1 - 3d + 3 = d^2 - 3d + 2$

所以：
$$
\left(\begin{array}{ccc|c}
1 & 1 & 1 & 1 \\
0 & 1 & a-1 & d-1 \\
0 & 0 & a^2 - 3a + 2 & d^2 - 3d + 2
\end{array}\right)
$$

注意：
- $a^2 - 3a + 2 = (a - 1)(a - 2)$-$d^2 - 3d + 2 = (d - 1)(d - 2)$

所以最终形式为：
$$
\left(\begin{array}{ccc|c}
1 & 1 & 1 & 1 \\
0 & 1 & a-1 & d-1 \\
0 & 0 & (a-1)(a-2) & (d-1)(d-2)
\end{array}\right)
$$

---

## ✅ 第四步：分析秩

要使方程组有无穷多解，必须满足：

1. $\text{rank}(A) = \text{rank}(A,b) < 3$

这意味着：

- 最后一行必须是零行，即：
  $$
  (a-1)(a-2) = 0 \quad \text{且} \quad (d-1)(d-2) = 0
  $$
  否则，若 $(a-1)(a-2) \ne 0$，则第三行主元非零，秩为 3 → 唯一解或无解；
  若 $(a-1)(a-2) = 0$但$(d-1)(d-2) \ne 0$，则秩不相等 → 无解。

所以：

- 要使 $\text{rank}(A) = \text{rank}(A,b) < 3$，必须：
  $$
  (a-1)(a-2) = 0 \quad \text{且} \quad (d-1)(d-2) = 0
  $$

即：
- $a = 1$或$a = 2$-$d = 1$或$d = 2$

也就是：
$$
a \in \Omega,\quad d \in \Omega
$$

其中 $\Omega = \{1,2\}$---

## ✅ 第五步：验证是否确实有无穷多解

当$a \in \Omega$且$d \in \Omega$时：

-$(a-1)(a-2) = 0$-$(d-1)(d-2) = 0$- 所以最后一行全为 0 → 秩为 2
- 且$\text{rank}(A) = \text{rank}(A,b) = 2 < 3$

所以方程组有**无穷多解**

---

## ❌ 验证其他情况

### 情况1：$a \in \Omega$，但 $d \notin \Omega$

例如$a = 1$，$d = 3$则：
- $(a-1)(a-2) = 0$
- $(d-1)(d-2) = (2)(1) = 2 \ne 0$

所以增广矩阵最后一行是$(0\ 0\ 0\ |\ 2)$ → 矛盾，无解

→ 不是无穷多解

### 情况2：$a \notin \Omega$，$d \in \Omega$

例如$a = 3$，$d = 1$则：
- $(a-1)(a-2) = (2)(1) = 2 \ne 0$
- $(d-1)(d-2) = 0$

所以$\text{rank}(A) = 3$，$\text{rank}(A,b) = 3$ → 唯一解

→ 不是无穷多解

### 情况3：$a \notin \Omega$，$d \notin \Omega$例如$a=3,d=3$则$(a-1)(a-2)=2\ne0$，$(d-1)(d-2)=2\ne0$，秩为 3 → 唯一解

---

## ✅ 结论

只有当：
$$
a \in \Omega,\quad d \in \Omega
$$
即选项 (D)

---

## ✅ 分析原解析

原解析写：

> $$
> (A,b) = \begin{pmatrix}
> 1 & 1 & 1 & 1 \\
> 1 & 2 & a & d \\
> 1 & 4 & a^2 & d^2
> \end{pmatrix}
> \to
> \begin{pmatrix}
> 1 & 1 & 1 & 1 \\
> 0 & 1 & a-1 & d-1 \\
> 0 & 0 & (a-1)(a-2) & (d-1)(d-2)
> \end{pmatrix}
> $$

✅ 正确！

然后说：

> 由 $r(A) = r(A,b) < 3$，故 $a=1$或$a=2$，同时 $d=1$或$d=2$。故选 (D)

✅ 完全正确！

---

## ✅ 注意事项

- “有无穷多解” ≠ “有解”，必须是**秩相等且小于未知数个数**
- 本题中，当 $a \in \Omega$且$d \in \Omega$时，方程组有无穷多解
- 其他情况要么无解，要么唯一解

---

### ✅ 最终答案：**(D)**

---

### ✅ 总结

| 条件 | 结果 |
|------|------|
|$a \in \Omega,\ d \in \Omega$| 无穷多解 ✅ |
|$a \in \Omega,\ d \notin \Omega$| 无解 ❌ |
|$a \notin \Omega,\ d \in \Omega$| 唯一解 ❌ |
|$a \notin \Omega,\ d \notin \Omega$ | 唯一解 ❌ |

---

### ✅ 答案：**(D)**