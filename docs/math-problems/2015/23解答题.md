## 🎯 问题回顾

设矩阵
$$
A=\begin{pmatrix}0&2&-3\\ -1&3&-3\\ 1&-2&a\end{pmatrix}
$$
相似于矩阵
$$
B=\begin{pmatrix}1&-2&0\\ 0&b&0\\ 0&3&1\end{pmatrix}
$$

1.  (I) 求 $a,b$ 的值;
2.  (II) 求可逆矩阵 $P$，使 $P^{-1}AP$ 为对角矩阵.

---

## 第一部分：(I) 求 $a, b$ 的值

### 1.1 利用相似矩阵的性质

若矩阵 $A$ 与 $B$ 相似 ($A \sim B$)，则它们具有相同的特征值。因此，它们的**迹（Trace，主对角线元素之和）**相等，且**行列式（Determinant）**相等。

### 1.2 比较迹（Trace）

$$
\text{tr}(A) = 0 + 3 + a = 3 + a
$$
$$
\text{tr}(B) = 1 + b + 1 = 2 + b
$$
由 $\text{tr}(A) = \text{tr}(B)$ 得：
$$
3 + a = 2 + b \implies b - a = 1 \quad \cdots ①
$$

### 1.3 比较行列式（Determinant）

计算 $|A|$（按第一列展开）：
$$
\begin{aligned}
|A| &= 0 \cdot (-1)^{1+1} \begin{vmatrix} 3 & -3 \\ -2 & a \end{vmatrix} + (-1) \cdot (-1)^{2+1} \begin{vmatrix} 2 & -3 \\ -2 & a \end{vmatrix} + 1 \cdot (-1)^{3+1} \begin{vmatrix} 2 & -3 \\ 3 & -3 \end{vmatrix} \\
&= 0 + 1 \cdot (2a - 6) + 1 \cdot (-6 - (-9)) \\
&= 2a - 6 + 3 \\
&= 2a - 3
\end{aligned}
$$

计算 $|B|$（按第二行或观察分块）：
$$
|B| = 1 \cdot \begin{vmatrix} b & 0 \\ 3 & 1 \end{vmatrix} - (-2) \cdot 0 + 0 = b
$$

由 $|A| = |B|$ 得：
$$
2a - 3 = b \quad \cdots ②
$$

### 1.4 解方程组

联立 ① 和 ②：
$$
\begin{cases}
b - a = 1 \\
b = 2a - 3
\end{cases}
$$
将 ② 代入 ①：
$$
(2a - 3) - a = 1 \implies a - 3 = 1 \implies a = 4
$$
求 $b$：
$$
b = 2(4) - 3 = 5
$$

**结论**：$a = 4, b = 5$.

---

## 第二部分：(II) 求可逆矩阵 $P$

### 2.1 确定 $A$ 的特征值

由 (I) 可知 $b=5$，矩阵 $B$ 为：
$$
B=\begin{pmatrix}1&-2&0\\ 0&5&0\\ 0&3&1\end{pmatrix}
$$
计算 $B$ 的特征多项式（或直接观察其特征值）：
$$
|\lambda E - B| = \begin{vmatrix} \lambda-1 & 2 & 0 \\ 0 & \lambda-5 & 0 \\ 0 & -3 & \lambda-1 \end{vmatrix}
$$
按第二行展开：
$$
= (\lambda-5) \begin{vmatrix} \lambda-1 & 2 \\ 0 & \lambda-1 \end{vmatrix} = (\lambda-5)(\lambda-1)^2
$$
故 $B$ 的特征值为 $\lambda_1 = 1, \lambda_2 = 1, \lambda_3 = 5$.
因为 $A \sim B$，所以 $A$ 的特征值也为 **1, 1, 5**.

此时 $A = \begin{pmatrix}0&2&-3\\ -1&3&-3\\ 1&-2&4\end{pmatrix}$.

### 2.2 求 $\lambda = 1$ 对应的线性无关特征向量

解方程 $(E - A)x = 0$：
$$
E - A = \begin{pmatrix} 1 & -2 & 3 \\ 1 & -2 & 3 \\ -1 & 2 & -3 \end{pmatrix}
$$
进行初等行变换：
$$
\begin{pmatrix} 1 & -2 & 3 \\ 1 & -2 & 3 \\ -1 & 2 & -3 \end{pmatrix} \xrightarrow{r_2-r_1, r_3+r_1} \begin{pmatrix} 1 & -2 & 3 \\ 0 & 0 & 0 \\ 0 & 0 & 0 \end{pmatrix}
$$
对应的同解方程组为：
$$
x_1 - 2x_2 + 3x_3 = 0 \implies x_1 = 2x_2 - 3x_3
$$
取自由未知量 $x_2, x_3$.
1.  令 $x_2 = 1, x_3 = 0$，得特征向量 $\xi_1 = (2, 1, 0)^T$.
2.  令 $x_2 = 0, x_3 = 1$，得特征向量 $\xi_2 = (-3, 0, 1)^T$.

### 2.3 求 $\lambda = 5$ 对应的特征向量

解方程 $(5E - A)x = 0$：
$$
5E - A = \begin{pmatrix} 5 & -2 & 3 \\ 1 & 2 & 3 \\ -1 & 2 & 1 \end{pmatrix}
$$
进行初等行变换：
$$
\begin{pmatrix} 5 & -2 & 3 \\ 1 & 2 & 3 \\ -1 & 2 & 1 \end{pmatrix} \xrightarrow{r_1 \leftrightarrow r_2} \begin{pmatrix} 1 & 2 & 3 \\ 5 & -2 & 3 \\ -1 & 2 & 1 \end{pmatrix}
$$
$$
\xrightarrow{r_2-5r_1, r_3+r_1} \begin{pmatrix} 1 & 2 & 3 \\ 0 & -12 & -12 \\ 0 & 4 & 4 \end{pmatrix} \xrightarrow{r_2 \div (-12), r_3 \div 4} \begin{pmatrix} 1 & 2 & 3 \\ 0 & 1 & 1 \\ 0 & 1 & 1 \end{pmatrix}
$$
$$
\xrightarrow{r_3-r_2, r_1-2r_2} \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 1 \\ 0 & 0 & 0 \end{pmatrix}
$$
对应的同解方程组为：
$$
\begin{cases} x_1 + x_3 = 0 \\ x_2 + x_3 = 0 \end{cases} \implies \begin{cases} x_1 = -x_3 \\ x_2 = -x_3 \end{cases}
$$
令 $x_3 = -1$（为了取整数方便），得特征向量 $\xi_3 = (1, 1, -1)^T$（或令 $x_3=1$ 得 $(-1,-1,1)^T$）.

### 2.4 构造矩阵 $P$

将求得的线性无关特征向量作为列向量构成矩阵 $P$：
$$
P = (\xi_1, \xi_2, \xi_3) = \begin{pmatrix} 2 & -3 & 1 \\ 1 & 0 & 1 \\ 0 & 1 & -1 \end{pmatrix}
$$
（注：$\xi_1, \xi_2$ 对应 $\lambda=1$，$\xi_3$ 对应 $\lambda=5$，列的顺序可以交换，只要与对角矩阵的特征值顺序对应即可。）

此时，验证 $P$ 可逆（特征向量线性无关），且：
$$
P^{-1} A P = \begin{pmatrix} 1 & 0 & 0 \\ 0 & 1 & 0 \\ 0 & 0 & 5 \end{pmatrix}
$$

---

## ✅ 最终答案

(I) $a = 4, b = 5$.
(II) $P = \begin{pmatrix} 2 & -3 & 1 \\ 1 & 0 & 1 \\ 0 & 1 & -1 \end{pmatrix}$ （答案不唯一，列向量倍数或顺序不同均可）.