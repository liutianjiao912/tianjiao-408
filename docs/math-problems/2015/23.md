---
title: 2015 数学二真题 第23题
---

我们来**详细分析这道关于矩阵相似、特征值与对角化的题目**，从题设到解析过程逐层剖析，并验证其正确性。

---

### **题目原文**

设矩阵

$$
A = \begin{pmatrix}
0 & 2 & -3 \\
-1 & 3 & -3 \\
1 & -2 & a
\end{pmatrix}, \quad
B = \begin{pmatrix}
1 & -2 & 0 \\
0 & b & 0 \\
0 & 3 & 1
\end{pmatrix}
$$

已知 $A \sim B$（即 $A$与$B$相似），求：

(1) 求$a, b$的值；
(2) 求可逆矩阵$P$，使得 $P^{-1}AP$为对角阵。

【答案】

(1)$a = 4, b = 5$

(2)

$$
P = \begin{pmatrix}
2 & -3 & -1 \\
1 & 0 & -1 \\
0 & 1 & 1
\end{pmatrix}
$$

---

## **一、核心概念回顾：矩阵相似的性质**

两个矩阵 $A$和$B$相似，记作$A \sim B$，当且仅当存在可逆矩阵 $P$，使得：

$$
B = P^{-1} A P
$$

相似矩阵具有以下**不变量**（即相同）：
1. **迹（trace）**：$\operatorname{tr}(A) = \operatorname{tr}(B)$
2. **行列式**：$\det(A) = \det(B)$
3. **秩**：$\operatorname{rank}(A) = \operatorname{rank}(B)$4. **特征值**（包括重数）
5. **特征多项式**
6. **若尔当标准形**

本题中，利用这些不变量来确定未知参数$a, b$。

---

## **二、第(1)问：求 $a, b$**

### 步骤1：利用迹相等

$$
\begin{aligned}
\operatorname{tr}(A) &= 0 + 3 + a = 3 + a \\
\operatorname{tr}(B) &= 1 + b + 1 = 2 + b
\end{aligned}
$$

由 $A \sim B$ 得：

$$
\operatorname{tr}(A) = \operatorname{tr}(B) \Rightarrow 3 + a = 2 + b \Rightarrow a - b = -1 \tag{1}
$$

---

### 步骤2：利用行列式相等

计算 $\det(A)$和$\det(B)$#### 计算$\det(A)$

$$
A = \begin{pmatrix}
0 & 2 & -3 \\
-1 & 3 & -3 \\
1 & -2 & a
\end{pmatrix}
$$

用第一行展开：

$$
\begin{aligned}
\det(A) &= 0 \cdot C_{11} - 2 \cdot C_{12} + (-3) \cdot C_{13} \\
&= -2 \cdot \det\begin{pmatrix}-1 & -3 \\ 1 & a\end{pmatrix} - 3 \cdot \det\begin{pmatrix}-1 & 3 \\ 1 & -2\end{pmatrix}
\end{aligned}
$$

计算：
- $\det\begin{pmatrix}-1 & -3 \\ 1 & a\end{pmatrix} = (-1)(a) - (-3)(1) = -a + 3$-$\det\begin{pmatrix}-1 & 3 \\ 1 & -2\end{pmatrix} = (-1)(-2) - (3)(1) = 2 - 3 = -1$

所以：

$$
\det(A) = -2(-a + 3) - 3(-1) = 2a - 6 + 3 = 2a - 3
$$

#### 计算 $\det(B)$

$$
B = \begin{pmatrix}
1 & -2 & 0 \\
0 & b & 0 \\
0 & 3 & 1
\end{pmatrix}
$$

这是上三角矩阵（或下三角？实际是分块三角），按第一列展开：

$$
\det(B) = 1 \cdot \det\begin{pmatrix}b & 0 \\ 3 & 1\end{pmatrix} = b \cdot 1 - 0 = b
$$

所以：

$$
\det(A) = \det(B) \Rightarrow 2a - 3 = b \tag{2}
$$

---

### 联立方程 (1) 和 (2)

由 (1): $a - b = -1 \Rightarrow b = a + 1$

代入 (2):

$$
\begin{aligned}
2a - 3 &= a + 1 \\
\Rightarrow a &= 4 \\
\Rightarrow b &= 5
\end{aligned}
$$

✅ 所以：

$$
a = 4, \quad b = 5
$$

---

## **三、第(2)问：求可逆矩阵 $P$，使 $P^{-1}AP$为对角阵**

### 分析：由于$A \sim B$，且 $B$是上三角矩阵，我们可以尝试将$A$ 对角化。

但注意：$B$不是对角阵，而是上三角，说明它可能是某个对角阵的相似变换结果。

但我们知道：**若$A$可对角化，则存在$P$使得$P^{-1}AP = D$（对角阵）**

而 $A \sim B$，所以 $B$也应能对角化（因为相似矩阵有相同的Jordan标准形）

先看$B$的特征值。

---

### 步骤1：求$A$的特征值

由于$A \sim B$，它们有相同的特征值。

先求 $B$ 的特征值（更简单）：

$$
B = \begin{pmatrix}
1 & -2 & 0 \\
0 & 5 & 0 \\
0 & 3 & 1
\end{pmatrix}
$$

这是分块三角矩阵，主对角线元素为特征值？不完全，因为不是对角块。

但注意：第二行只有中间非零，第三行有交叉项。

不过我们可以直接计算特征多项式：

$$
\det(B - \lambda I) = \det\begin{pmatrix}
1-\lambda & -2 & 0 \\
0 & 5-\lambda & 0 \\
0 & 3 & 1-\lambda
\end{pmatrix}
$$

按第一列展开：

$$
\begin{aligned}
&= (1-\lambda) \cdot \det\begin{pmatrix}5-\lambda & 0 \\ 3 & 1-\lambda\end{pmatrix} \\
&= (1-\lambda)[(5-\lambda)(1-\lambda)] \\
&= (1-\lambda)^2 (5-\lambda)
\end{aligned}
$$

所以 $B$ 的特征值为：$\lambda = 1$（二重），$\lambda = 5$因此$A$ 的特征值也为：$1, 1, 5$---

### 步骤2：对$A$进行对角化

我们要找三个线性无关的特征向量，构成矩阵$P$，使得 $P^{-1}AP = \operatorname{diag}(1,1,5)$令$A$的特征值为：
-$\lambda_1 = 1$（重数2）
- $\lambda_2 = 5$先解$(A - I)\mathbf{x} = 0$，找属于 $\lambda = 1$的特征向量

---

#### 解$(A - I)\mathbf{x} = 0$

$$
A - I = \begin{pmatrix}
-1 & 2 & -3 \\
-1 & 2 & -3 \\
1 & -2 & 3
\end{pmatrix}
$$

观察：第一行和第二行相同 → 线性相关

化简：
- 第一行：$-x_1 + 2x_2 - 3x_3 = 0$
- 第三行：$x_1 - 2x_2 + 3x_3 = 0$ → 与第一行相反

所以只有一条独立方程：$-x_1 + 2x_2 - 3x_3 = 0$令自由变量：
- 设$x_2 = s, x_3 = t$，则 $x_1 = 2s - 3t$

通解：

$$
\mathbf{x} = \begin{pmatrix} 2s - 3t \\ s \\ t \end{pmatrix} = s \begin{pmatrix} 2 \\ 1 \\ 0 \end{pmatrix} + t \begin{pmatrix} -3 \\ 0 \\ 1 \end{pmatrix}
$$

所以属于 $\lambda = 1$的特征空间维数为 2，有两个线性无关的特征向量：

-$\xi_1 = (2, 1, 0)^T$-$\xi_2 = (-3, 0, 1)^T$---

#### 解$(A - 5I)\mathbf{x} = 0$

$$
A - 5I = \begin{pmatrix}
-5 & 2 & -3 \\
-1 & -2 & -3 \\
1 & -2 & -1
\end{pmatrix}
$$

化简增广矩阵：

$$
\left(\begin{array}{ccc|c}
-5 & 2 & -3 & 0 \\
-1 & -2 & -3 & 0 \\
1 & -2 & -1 & 0
\end{array}\right)
$$

用初等行变换：

- $R_1 \leftarrow R_1 - 5R_2$：$[-5+5, 2+10, -3+15] = [0, 12, 12]$-$R_3 \leftarrow R_3 + R_2$：$[0, -4, -4]$

得到：

$$
\left(\begin{array}{ccc|c}
0 & 12 & 12 & 0 \\
-1 & -2 & -3 & 0 \\
0 & -4 & -4 & 0
\end{array}\right)
\Rightarrow
\left(\begin{array}{ccc|c}
0 & 1 & 1 & 0 \\
-1 & -2 & -3 & 0 \\
0 & 0 & 0 & 0
\end{array}\right)
$$

再化简：
- $R_2 \leftarrow R_2 + 2R_1$：$[-1, 0, -1]$

所以：

$$
\begin{aligned}
-x_1 - x_3 &= 0 \Rightarrow x_1 = -x_3 \\
x_2 &= -x_3
\end{aligned}
$$

令 $x_3 = t$，则 $x_1 = -t, x_2 = -t$

解为：

$$
\mathbf{x} = t \begin{pmatrix} -1 \\ -1 \\ 1 \end{pmatrix}
$$

所以属于 $\lambda = 5$的特征向量为：
-$\xi_3 = (-1, -1, 1)^T$---

### 构造矩阵$P$令$P = (\xi_1, \xi_2, \xi_3)$，即：

$$
P = \begin{pmatrix}
2 & -3 & -1 \\
1 & 0 & -1 \\
0 & 1 & 1
\end{pmatrix}
$$

与答案一致！

---

### 验证 $P^{-1}AP$是否为对角阵？

理论上，由于$A$ 有三个线性无关的特征向量，且特征值互异或重数满足条件，$P^{-1}AP$应为对角阵，对角线为$1,1,5$

即：

$$
P^{-1}AP = \begin{pmatrix}
1 & 0 & 0 \\
0 & 1 & 0 \\
0 & 0 & 5
\end{pmatrix}
$$

但题目中写的是：

$$
P^{-1}AP = \begin{pmatrix}
1 & & \\
& 1 & \\
& & 5
\end{pmatrix}
$$

形式上是对的。

---

## **四、解析中的技巧：构造 $C = A - E$**

解析中写道：

$$
A = E + C, \quad C = \begin{pmatrix}
-1 & 2 & -3 \\
-1 & 2 & -3 \\
1 & -2 & 3
\end{pmatrix}
$$

并说 $C$的特征值为$0,0,4$，所以 $A = E + C$的特征值为$1,1,5$这是正确的！

因为：
- 若$C$的特征值为$\mu_i$，则 $A = E + C$的特征值为$1 + \mu_i$-$C$的特征值：由前面计算$(A - I)$的秩为1，说明$C$的秩为1，所以有两个零特征值
-$\operatorname{tr}(C) = -1 + 2 + 3 = 4$，所以第三个特征值为4
- 故 $C$特征值为$0,0,4$→$A$特征值为$1,1,5$然后求$C$的特征向量就是求$(A - I)\mathbf{x} = 0$的解，与上面一致。

---

## **五、总结**

### ✅ 答案正确：
- (1)$a = 4, b = 5$- (2)$P = \begin{pmatrix} 2 & -3 & -1 \\ 1 & 0 & -1 \\ 0 & 1 & 1 \end{pmatrix}$### 🔍 关键步骤：
1. 利用**迹和行列式相等**求$a,b$2. 利用**相似矩阵特征值相同**求出$A$的特征值
3. 求特征向量，构造$P$4. 注意重特征值是否对应足够多的线性无关特征向量

---

### 📌 总结公式：

若$A \sim B$，则：
- $\operatorname{tr}(A) = \operatorname{tr}(B)$-$\det(A) = \det(B)$- 特征值相同
- 若$A$可对角化，则$P^{-1}AP = \operatorname{diag}(\lambda_1,\dots,\lambda_n)$---

✅ **最终答案：**

(1)$a = 4, b = 5$

(2)

$$
P = \begin{pmatrix}
2 & -3 & -1 \\
1 & 0 & -1 \\
0 & 1 & 1
\end{pmatrix}
$$
