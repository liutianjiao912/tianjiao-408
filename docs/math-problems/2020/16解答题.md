这是一个非常经典的微积分综合题，主要考察**变限积分求导**、**导数的定义**以及**函数连续性的判定**。解答此类问题时，最关键的是要区分 $x \neq 0$ 和 $x = 0$ 两种情况，严谨地使用定义法求特殊点的导数。

以下是详细的解析步骤。

---

## 🎯 问题回顾

**已知**：
1.  函数 $f(x)$ 在 $\mathbb{R}$ 上连续。
2.  极限条件：$\displaystyle \lim_{x \to 0}\frac{f(x)}{x}=1$。
3.  函数定义：$\displaystyle g(x)=\int_{0}^{1}f(xt)dt$。

**求解**：
1.  求 $g^{\prime}(x)$ 的表达式。
2.  证明 $g^{\prime}(x)$ 在 $x=0$ 处连续。

---

## 第一部分：预处理与分析

### 1.1 分析已知条件

由 $\displaystyle \lim_{x \to 0}\frac{f(x)}{x}=1$ 可知：
1.  当 $x \to 0$ 时，$f(x)$ 是 $x$ 的等价无穷小。
2.  由于极限存在且分母趋于 0，分子必趋于 0，即 $\lim_{x \to 0}f(x) = 0$。又因 $f(x)$ 连续，故 **$f(0) = 0$**。

### 1.2 简化 $g(x)$ 的表达式

积分 $\displaystyle g(x)=\int_{0}^{1}f(xt)dt$ 中，积分变量是 $t$，但被积函数中含有参变量 $x$。为了方便求导，我们通常通过**变量代换**将 $x$ 移至积分上限。

令 $u = xt$：
* 当 $t=0$ 时，$u=0$；
* 当 $t=1$ 时，$u=x$；
* $dt = \frac{1}{x} du$ （注意这里视 $x$ 为常数进行积分变换，前提是 $x \neq 0$）。

于是，当 $x \ne 0$ 时：
$$
g(x) = \int_{0}^{x} f(u) \cdot \frac{1}{x} du = \frac{1}{x} \int_{0}^{x} f(u) du
$$

> 💡 **关键点**：将积分变形为 $\frac{\int_{0}^{x} f(u) du}{x}$ 的形式，能够利用微积分基本定理求导，同时也方便后续使用洛必达法则。

---

## 第二部分：求 $g^{\prime}(x)$ 的表达式

我们需要分 $x \ne 0$ 和 $x = 0$ 两种情况讨论。

### 2.1 当 $x \ne 0$ 时

对 $g(x) = \frac{\int_{0}^{x} f(u) du}{x}$ 使用**商的求导法则**：

$$
\left( \frac{u}{v} \right)' = \frac{u'v - uv'}{v^2}
$$

其中：
* 分子求导：$\displaystyle \frac{d}{dx} \left( \int_{0}^{x} f(u) du \right) = f(x)$ （根据微积分基本定理）；
* 分母求导：$(x)' = 1$。

计算过程：
$$
\begin{aligned}
g'(x) &= \frac{f(x) \cdot x - \left( \int_{0}^{x} f(u) du \right) \cdot 1}{x^2} \\
&= \frac{x f(x) - \int_{0}^{x} f(u) du}{x^2}
\end{aligned}
$$

### 2.2 当 $x = 0$ 时

**注意**：不能直接将 $x=0$ 代入上面的式子，因为分母为 0。必须使用**导数的定义**。

由定义：
$$
g'(0) = \lim_{x \to 0} \frac{g(x) - g(0)}{x - 0}
$$

首先计算 $g(0)$：
由原定义 $g(0) = \int_{0}^{1} f(0\cdot t) dt = \int_{0}^{1} f(0) dt$。
因 $f(0)=0$，故 **$g(0) = 0$**。

代入极限式：
$$
g'(0) = \lim_{x \to 0} \frac{\frac{1}{x} \int_{0}^{x} f(u) du - 0}{x} = \lim_{x \to 0} \frac{\int_{0}^{x} f(u) du}{x^2}
$$

这是一个 $\frac{0}{0}$ 型极限。使用**洛必达法则**（L'Hôpital's Rule）：

$$
\begin{aligned}
g'(0) &= \lim_{x \to 0} \frac{\frac{d}{dx}\int_{0}^{x} f(u) du}{\frac{d}{dx}(x^2)} \\
&= \lim_{x \to 0} \frac{f(x)}{2x} \\
&= \frac{1}{2} \lim_{x \to 0} \frac{f(x)}{x}
\end{aligned}
$$

利用已知条件 $\lim_{x \to 0}\frac{f(x)}{x}=1$，得：
$$
g'(0) = \frac{1}{2} \cdot 1 = \frac{1}{2}
$$

### 2.3 综合 $g^{\prime}(x)$ 结果

$$
g'(x) = \begin{cases}
\dfrac{x f(x) - \int_{0}^{x} f(u) du}{x^2}, & x \ne 0 \\
\dfrac{1}{2}, & x = 0
\end{cases}
$$

---

## 第三部分：证明 $g^{\prime}(x)$ 在 $x=0$ 处连续

### 3.1 连续性判定标准

要证明 $g'(x)$ 在 $x=0$ 处连续，需验证：
$$
\lim_{x \to 0} g'(x) = g'(0)
$$

已知 $g'(0) = \frac{1}{2}$，接下来计算极限。

### 3.2 计算 $\lim_{x \to 0} g'(x)$

代入 $x \ne 0$ 时的导数表达式：

$$
\lim_{x \to 0} g'(x) = \lim_{x \to 0} \frac{x f(x) - \int_{0}^{x} f(u) du}{x^2}
$$

我们可以将其拆分为两部分，或者直接通分使用洛必达法则。为了清晰展示，我们将其拆开利用已知极限。

**拆分法**：
$$
\begin{aligned}
\lim_{x \to 0} g'(x) &= \lim_{x \to 0} \left( \frac{x f(x)}{x^2} - \frac{\int_{0}^{x} f(u) du}{x^2} \right) \\
&= \lim_{x \to 0} \frac{f(x)}{x} - \lim_{x \to 0} \frac{\int_{0}^{x} f(u) du}{x^2}
\end{aligned}
$$

**第一项极限**：
题目已知 $\displaystyle \lim_{x \to 0} \frac{f(x)}{x} = 1$。

**第二项极限**：
我们在 2.2 节中计算 $g'(0)$ 时已经算过这一项：
$$
\lim_{x \to 0} \frac{\int_{0}^{x} f(u) du}{x^2} \overset{\text{洛}}{=} \lim_{x \to 0} \frac{f(x)}{2x} = \frac{1}{2} \cdot 1 = \frac{1}{2}
$$

**合并计算**：
$$
\lim_{x \to 0} g'(x) = 1 - \frac{1}{2} = \frac{1}{2}
$$

### 3.3 结论

因为：
$$
\lim_{x \to 0} g'(x) = \frac{1}{2} = g'(0)
$$

✅ **所以 $g^{\prime}(x)$ 在 $x=0$ 处连续。**

---

## 📌 总结与最终答案

**1. 导数表达式：**

$$
g'(x) = \begin{cases}
\dfrac{x f(x) - \int_{0}^{x} f(t) dt}{x^2}, & x \ne 0 \\
\dfrac{1}{2}, & x = 0
\end{cases}
$$

**2. 连续性证明：**

由于 $\displaystyle \lim_{x \to 0} g'(x) = \lim_{x \to 0} \left( \frac{f(x)}{x} - \frac{\int_{0}^{x} f(t) dt}{x^2} \right) = 1 - \frac{1}{2} = \frac{1}{2}$，
且 $g'(0) = \frac{1}{2}$，
故 $\lim_{x \to 0} g'(x) = g'(0)$，证得 $g'(x)$ 在 $x=0$ 处连续。