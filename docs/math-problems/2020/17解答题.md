这是一道关于求多元函数极值的经典题目。我们将严格按照“求驻点 $\to$ 二阶导数判别法 $\to$ 计算极值”的步骤进行详细解析。

---

## 🎯 问题回顾

**题目**：
求函数 $f(x,y) = x^{3} + 8y^{3} - xy$ 的极值。

---

## 第一部分：求驻点（关键点）

### 1.1 计算一阶偏导数

首先对 $x$ 和 $y$ 分别求偏导数：

1.  对 $x$ 求偏导（将 $y$ 视为常数）：
    $$
    f_x(x,y) = \frac{\partial}{\partial x}(x^3 + 8y^3 - xy) = 3x^2 - y
    $$

2.  对 $y$ 求偏导（将 $x$ 视为常数）：
    $$
    f_y(x,y) = \frac{\partial}{\partial y}(x^3 + 8y^3 - xy) = 24y^2 - x
    $$

### 1.2 建立方程组并求解

令偏导数为零，得到方程组：
$$
\begin{cases}
3x^2 - y = 0 \quad \cdots ① \\
24y^2 - x = 0 \quad \cdots ②
\end{cases}
$$

由 ① 得：
$$
y = 3x^2 \quad \cdots ③
$$

将 ③ 代入 ②：
$$
24(3x^2)^2 - x = 0
$$
$$
24(9x^4) - x = 0
$$
$$
216x^4 - x = 0
$$

提取公因式 $x$：
$$
x(216x^3 - 1) = 0
$$

**解得 $x$ 的两个根**：
1.  **情形一**：$x_1 = 0$
2.  **情形二**：$216x^3 = 1 \implies x^3 = \frac{1}{216} \implies x_2 = \frac{1}{6}$

**求对应的 $y$ 值**（代入 ③）：
1.  当 $x_1 = 0$ 时：
    $$
    y_1 = 3(0)^2 = 0
    $$
    得到驻点 **$P_1(0, 0)$**。

2.  当 $x_2 = \frac{1}{6}$ 时：
    $$
    y_2 = 3\left(\frac{1}{6}\right)^2 = 3 \cdot \frac{1}{36} = \frac{1}{12}
    $$
    得到驻点 **$P_2\left(\frac{1}{6}, \frac{1}{12}\right)$**。

✅ **综上，函数的驻点为 $(0,0)$ 和 $\left(\frac{1}{6}, \frac{1}{12}\right)$。**

---

## 第二部分：二阶导数判别法（Hessian 矩阵）

### 2.1 计算二阶偏导数

$$
\begin{aligned}
A &= f_{xx} = \frac{\partial}{\partial x}(3x^2 - y) = 6x \\
B &= f_{xy} = \frac{\partial}{\partial y}(3x^2 - y) = -1 \\
C &= f_{yy} = \frac{\partial}{\partial y}(24y^2 - x) = 48y
\end{aligned}
$$

### 2.2 判别式定义

令判别式为 $\Delta = AC - B^2$。
判别规则如下：
* 若 $\Delta > 0$ 且 $A > 0$，为极小值；
* 若 $\Delta > 0$ 且 $A < 0$，为极大值；
* 若 $\Delta < 0$，非极值（鞍点）；
* 若 $\Delta = 0$，方法失效。

本题的判别式通式为：
$$
\Delta = (6x)(48y) - (-1)^2 = 288xy - 1
$$

---

## 第三部分：分类讨论与计算极值

### 3.1 考察驻点 $P_1(0, 0)$

代入坐标计算 $A, B, C$ 及 $\Delta$：
* $A = 6(0) = 0$
* $C = 48(0) = 0$
* $\Delta = 0 \cdot 0 - (-1)^2 = -1$

**结论**：
因为 $\Delta = -1 < 0$，所以点 $(0,0)$ **不是极值点**（这是一个鞍点）。

### 3.2 考察驻点 $P_2\left(\frac{1}{6}, \frac{1}{12}\right)$

代入坐标计算 $A, B, C$ 及 $\Delta$：
* $A = 6 \cdot \frac{1}{6} = 1$
* $C = 48 \cdot \frac{1}{12} = 4$
* $\Delta = AC - B^2 = 1 \cdot 4 - (-1)^2 = 4 - 1 = 3$

**结论**：
1.  **判别式** $\Delta = 3 > 0$，说明存在极值。
2.  **二阶导数** $A = 1 > 0$，说明开口向上，是**极小值**。

### 3.3 计算极小值数值

将 $x = \frac{1}{6}, y = \frac{1}{12}$ 代入原函数 $f(x,y) = x^3 + 8y^3 - xy$：

$$
\begin{aligned}
f\left(\frac{1}{6}, \frac{1}{12}\right) &= \left(\frac{1}{6}\right)^3 + 8\left(\frac{1}{12}\right)^3 - \left(\frac{1}{6}\right)\left(\frac{1}{12}\right) \\
&= \frac{1}{216} + 8 \cdot \frac{1}{1728} - \frac{1}{72} \\
\end{aligned}
$$

> 计算细节：$12^3 = (2 \times 6)^3 = 8 \times 6^3 = 8 \times 216 = 1728$。
> 所以 $8 \cdot \frac{1}{1728} = \frac{8}{8 \times 216} = \frac{1}{216}$。

代入继续计算：
$$
\begin{aligned}
f_{min} &= \frac{1}{216} + \frac{1}{216} - \frac{1}{72} \\
&= \frac{2}{216} - \frac{1}{72} \\
&= \frac{1}{108} - \frac{1}{72}
\end{aligned}
$$

寻找通分分母（最小公倍数）：
$108 = 36 \times 3$
$72 = 36 \times 2$
$\text{LCM} = 36 \times 2 \times 3 = 216$

$$
\begin{aligned}
f_{min} &= \frac{2}{216} - \frac{3}{216} \\
&= -\frac{1}{216}
\end{aligned}
$$

---

## ✅ 最终答案

函数 $f(x,y)$ 在点 $(0,0)$ 处无极值；
在点 $\left(\frac{1}{6}, \frac{1}{12}\right)$ 处取得**极小值**，极小值为 **$-\frac{1}{216}$**。