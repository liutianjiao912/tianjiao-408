## 🎯 问题回顾

计算广义积分（反常积分）：

$$
\int_{5}^{+\infty}\frac{1}{x^{2}-4x+3}dx = \_\_\_\_\_\_
$$

---

## 第一部分：积分前的分析与处理

### 1.1 积分类型识别
该积分为**无穷限广义积分**（第一类反常积分）。其定义为：
$$
\int_{a}^{+\infty} f(x) dx = \lim_{t \to +\infty} \int_{a}^{t} f(x) dx
$$
若该极限存在（为有限数），则称积分收敛；否则称积分发散。

### 1.2 被积函数分析
被积函数为有理函数：
$$
f(x) = \frac{1}{x^2 - 4x + 3}
$$
首先对分母进行因式分解：
$$
x^2 - 4x + 3 = (x-1)(x-3)
$$
分母的零点为 $x=1$ 和 $x=3$。
由于积分区间为 $[5, +\infty)$，这两个奇点均在积分区间之外。因此，函数在 $[5, +\infty)$ 上连续，只需处理无穷上限的问题。

---

## 第二部分：部分分式分解 (Partial Fraction Decomposition)

为了计算积分，我们需要将被积函数拆解为简单的分式之和。

### 2.1 待定系数法
设：
$$
\frac{1}{(x-1)(x-3)} = \frac{A}{x-3} + \frac{B}{x-1}
$$
通分右边：
$$
\frac{A}{x-3} + \frac{B}{x-1} = \frac{A(x-1) + B(x-3)}{(x-3)(x-1)}
$$
由于等式两边分母相同，故分子恒等：
$$
1 \equiv A(x-1) + B(x-3)
$$

### 2.2 求解系数
我们可以通过代入特殊值来快速求出 $A$ 和 $B$：

1.  **令 $x = 3$**（消除 $B$ 项）：
    $$
    1 = A(3-1) + B(0) \implies 1 = 2A \implies A = \frac{1}{2}
    $$

2.  **令 $x = 1$**（消除 $A$ 项）：
    $$
    1 = A(0) + B(1-3) \implies 1 = -2B \implies B = -\frac{1}{2}
    $$

### 2.3 分解结果
$$
f(x) = \frac{1}{2(x-3)} - \frac{1}{2(x-1)} = \frac{1}{2}\left( \frac{1}{x-3} - \frac{1}{x-1} \right)
$$

---

## 第三部分：计算不定积分与极限

### 3.1 计算原函数
$$
\begin{aligned}
\int f(x) dx &= \int \frac{1}{2}\left( \frac{1}{x-3} - \frac{1}{x-1} \right) dx \\
&= \frac{1}{2} \left( \int \frac{1}{x-3} dx - \int \frac{1}{x-1} dx \right) \\
&= \frac{1}{2} (\ln|x-3| - \ln|x-1|) + C \\
&= \frac{1}{2} \ln\left| \frac{x-3}{x-1} \right| + C
\end{aligned}
$$

> **注**：在积分区间 $[5, +\infty)$ 上，$x-3 > 0$ 且 $x-1 > 0$，因此绝对值符号可以直接去掉。

### 3.2 运用牛顿-莱布尼茨公式
根据广义积分定义：
$$
\begin{aligned}
I &= \lim_{t \to +\infty} \left[ \frac{1}{2} \ln\left( \frac{x-3}{x-1} \right) \right]_{5}^{t} \\
&= \lim_{t \to +\infty} \left( \frac{1}{2} \ln\left( \frac{t-3}{t-1} \right) - \frac{1}{2} \ln\left( \frac{5-3}{5-1} \right) \right)
\end{aligned}
$$

### 3.3 计算极限值
我们需要计算两部分的值。

**第一部分（上限极限）：**
$$
\lim_{t \to +\infty} \frac{t-3}{t-1} = \lim_{t \to +\infty} \frac{1 - 3/t}{1 - 1/t} = 1
$$
因此：
$$
\lim_{t \to +\infty} \frac{1}{2} \ln\left( \frac{t-3}{t-1} \right) = \frac{1}{2} \ln(1) = 0
$$

**第二部分（下限数值）：**
$$
\frac{5-3}{5-1} = \frac{2}{4} = \frac{1}{2}
$$
因此下限项为：
$$
\frac{1}{2} \ln\left( \frac{1}{2} \right) = \frac{1}{2} (\ln 1 - \ln 2) = -\frac{1}{2} \ln 2
$$

### 3.4 最终合并
$$
I = 0 - \left( -\frac{1}{2} \ln 2 \right) = \frac{1}{2} \ln 2
$$
或者写成 $\ln \sqrt{2}$。通常保留为 $\frac{1}{2} \ln 2$ 或 $\ln 2 / 2$ 形式。

---

## ✅ 最终答案

$$
\int_{5}^{+\infty}\frac{1}{x^{2}-4x+3}dx = \frac{1}{2}\ln 2
$$

**填空应当填写：**
$$
\frac{1}{2}\ln 2 \quad (\text{或 } \ln\sqrt{2})
$$

---

## 📌 关键步骤总结

1.  **因式分解**：识别分母为 $(x-1)(x-3)$ 是解题的突破口。
2.  **裂项相消**：通过部分分式分解将复杂分式转化为对数形式的积分。
3.  **对数性质**：利用 $\ln A - \ln B = \ln(A/B)$ 极大地简化了极限的计算过程。
4.  **无穷限处理**：当 $x \to \infty$ 时，有理分式 $\frac{x-a}{x-b} \to 1$，从而 $\ln(1)=0$，这是此类积分收敛的关键特征。