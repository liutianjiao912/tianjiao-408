## 🎯 问题回顾

**填空题**：
求极限 $\displaystyle \lim_{n\rightarrow\infty}\frac{1}{n^{2}}\left(\sin\frac{1}{n}+2\sin\frac{2}{n}+\cdots+n\sin\frac{n}{n}\right) =$ \_\_\_\_\_\_.

---

## 第一部分：结构识别与变形

### 1.1 识别题型

观察该极限表达式，它具有“$n$ 项和”的形式，且随着 $n \to \infty$，每一项都在变化。这是典型的**定积分定义（黎曼和）**类型的题目。

定积分的定义公式为：
$$
\lim_{n \to \infty} \frac{1}{n} \sum_{k=1}^{n} f\left(\frac{k}{n}\right) = \int_{0}^{1} f(x) \, dx
$$

### 1.2 凑成黎曼和形式

我们需要将原式变形，使其符合上述结构。

原式记为 $S$，通项公式为 $k \sin \frac{k}{n}$（其中 $k=1, 2, \dots, n$）。
将 $\frac{1}{n^2}$ 拆分为 $\frac{1}{n} \cdot \frac{1}{n}$，并重新组合：

$$
\begin{aligned}
S &= \lim_{n\rightarrow\infty}\frac{1}{n^{2}} \sum_{k=1}^{n} k \sin\frac{k}{n} \\
&= \lim_{n\rightarrow\infty} \frac{1}{n} \sum_{k=1}^{n} \frac{k}{n} \sin\frac{k}{n}
\end{aligned}
$$

### 1.3 确定被积函数与积分区间

对比标准形式：
- **积分变量**：令 $x_k = \frac{k}{n}$，则当 $n \to \infty$ 时，$x$ 的范围是从 $0$ 到 $1$。
- **微分元**：$\frac{1}{n}$ 对应 $dx$。
- **被积函数**：$\frac{k}{n} \sin \frac{k}{n}$ 对应 $x \sin x$。

因此，原极限转化为定积分：
$$
I = \int_{0}^{1} x \sin x \, dx
$$

---

## 第二部分：计算定积分

### 2.1 分部积分法

对于 $\int x \sin x \, dx$，我们采用**分部积分法**。
公式：$\int u \, dv = uv - \int v \, du$

令：
$$
\begin{cases}
u = x \\
dv = \sin x \, dx
\end{cases}
\implies
\begin{cases}
du = dx \\
v = -\cos x
\end{cases}
$$

### 2.2 执行计算

代入公式：

$$
\begin{aligned}
\int_{0}^{1} x \sin x \, dx &= \left[ -x \cos x \right]_{0}^{1} - \int_{0}^{1} (-\cos x) \, dx \\
&= \left( -1 \cdot \cos 1 - (-0 \cdot \cos 0) \right) + \int_{0}^{1} \cos x \, dx \\
&= -\cos 1 + \left[ \sin x \right]_{0}^{1}
\end{aligned}
$$

### 2.3 最终求值

$$
\begin{aligned}
&= -\cos 1 + (\sin 1 - \sin 0) \\
&= \sin 1 - \cos 1
\end{aligned}
$$

---

## ✅ 最终答案

$$
\sin 1 - \cos 1
$$

---

## 📌 关键要点总结

1.  **结构敏感度**：看到 $\lim_{n \to \infty} \sum$ 形式，首先联想**定积分定义**。
2.  **凑型技巧**：关键在于处理系数 $\frac{1}{n^2}$。将其拆解，使得 $\frac{1}{n}$ 作为 $dx$，剩下的 $\frac{1}{n}$ 与求和项中的 $k$ 结合成 $\frac{k}{n}$（即 $x$）。
3.  **计算准确性**：使用分部积分法时，注意正负号以及 $v = -\cos x$ 的处理。
4.  **特殊值**：$\sin 0 = 0$，计算时不要漏掉下限代入。