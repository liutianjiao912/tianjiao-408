## 🎯 问题回顾

**解答题**：
设矩阵 $A=\begin{pmatrix}1&1&1-a\\ 1&0&a\\ a+1&1&a+1\end{pmatrix}, \beta=\begin{pmatrix}0\\ 1\\ 2a-2\end{pmatrix}$，且方程组 $Ax=\beta$ 无解.
(I) 求 $a$ 的值;
(II) 求方程组 $A^{T}Ax=A^{T}\beta$ 的通解.

---

## 第一部分：确定参数 $a$ 的值

### 1.1 构建增广矩阵并化简

线性方程组 $Ax=\beta$ 无解的充要条件是：系数矩阵 $A$ 的秩小于增广矩阵 $\bar{A}=(A|\beta)$ 的秩，即 $r(A) < r(\bar{A})$。

首先写出增广矩阵 $\bar{A}$，并进行初等行变换化为行阶梯形：

$$
\bar{A} = \left( \begin{array}{ccc|c} 1 & 1 & 1-a & 0 \\ 1 & 0 & a & 1 \\ a+1 & 1 & a+1 & 2a-2 \end{array} \right)
$$

1.  消去第 1 列下方的元素：
    * $r_2 - r_1$
    * $r_3 - (a+1)r_1$

$$
\xrightarrow{\substack{r_2-r_1 \\ r_3-(a+1)r_1}} \left( \begin{array}{ccc|c} 1 & 1 & 1-a & 0 \\ 0 & -1 & 2a-1 & 1 \\ 0 & -a & (a+1) - (a+1)(1-a) & 2a-2 \end{array} \right)
$$

计算 $(3,3)$ 元素：$(a+1)[1-(1-a)] = (a+1)a = a^2+a$.
此时矩阵为：
$$
\left( \begin{array}{ccc|c} 1 & 1 & 1-a & 0 \\ 0 & -1 & 2a-1 & 1 \\ 0 & -a & a^2+a & 2a-2 \end{array} \right)
$$

2.  消去第 2 列下方的元素：
    * $r_3 - a r_2$

$$
\xrightarrow{r_3-ar_2} \left( \begin{array}{ccc|c} 1 & 1 & 1-a & 0 \\ 0 & -1 & 2a-1 & 1 \\ 0 & 0 & (a^2+a) - a(2a-1) & (2a-2) - a(1) \end{array} \right)
$$

计算 $(3,3)$ 元素：$a^2+a - 2a^2+a = 2a - a^2 = a(2-a)$.
计算 $(3,4)$ 元素：$2a-2-a = a-2$.

化简后的阶梯形矩阵为：
$$
\bar{A} \sim \left( \begin{array}{ccc|c} 1 & 1 & 1-a & 0 \\ 0 & -1 & 2a-1 & 1 \\ 0 & 0 & a(2-a) & a-2 \end{array} \right)
$$

### 1.2 讨论秩并求解 $a$

要使方程组无解，需满足 $r(A) < r(\bar{A})$。
观察阶梯形矩阵的最后一行：
* 系数矩阵 $A$ 的秩由前三列决定。若 $a(2-a) = 0$，则 $r(A) = 2$；否则 $r(A) = 3$。
* 若 $r(A) = 3$，则必有 $r(\bar{A}) = 3$，此时方程组有唯一解。
* 因此必须 $a(2-a) = 0$，即 $a=0$ 或 $a=2$。

**讨论两种情况：**
1.  **当 $a = 2$ 时**：
    最后一行变为 $(0, 0, 0 | 0)$。
    此时 $r(A) = 2, r(\bar{A}) = 2$，方程组有无穷多解。不合题意。
2.  **当 $a = 0$ 时**：
    最后一行变为 $(0, 0, 0 | -2)$。
    此时 $r(A) = 2$，而 $r(\bar{A}) = 3$。
    满足 $r(A) < r(\bar{A})$，方程组无解。

✅ **结论 (I)**：$a = 0$.

---

## 第二部分：求方程组的通解

### 2.1 代入 $a$ 值计算 $A^TA$ 与 $A^T\beta$

当 $a=0$ 时，
$$
A = \begin{pmatrix} 1 & 1 & 1 \\ 1 & 0 & 0 \\ 1 & 1 & 1 \end{pmatrix}, \quad \beta = \begin{pmatrix} 0 \\ 1 \\ -2 \end{pmatrix}
$$

**计算 $A^T$**：
$$
A^T = \begin{pmatrix} 1 & 1 & 1 \\ 1 & 0 & 1 \\ 1 & 0 & 1 \end{pmatrix}
$$

**计算系数矩阵 $A^TA$**：
$$
A^TA = \begin{pmatrix} 1 & 1 & 1 \\ 1 & 0 & 1 \\ 1 & 0 & 1 \end{pmatrix} \begin{pmatrix} 1 & 1 & 1 \\ 1 & 0 & 0 \\ 1 & 1 & 1 \end{pmatrix} = \begin{pmatrix} 3 & 2 & 2 \\ 2 & 2 & 2 \\ 2 & 2 & 2 \end{pmatrix}
$$

**计算常数项向量 $A^T\beta$**：
$$
A^T\beta = \begin{pmatrix} 1 & 1 & 1 \\ 1 & 0 & 1 \\ 1 & 0 & 1 \end{pmatrix} \begin{pmatrix} 0 \\ 1 \\ -2 \end{pmatrix} = \begin{pmatrix} -1 \\ -2 \\ -2 \end{pmatrix}
$$

### 2.2 求解线性方程组

我们需要求解方程组：
$$
\begin{pmatrix} 3 & 2 & 2 \\ 2 & 2 & 2 \\ 2 & 2 & 2 \end{pmatrix} \begin{pmatrix} x_1 \\ x_2 \\ x_3 \end{pmatrix} = \begin{pmatrix} -1 \\ -2 \\ -2 \end{pmatrix}
$$

构建增广矩阵并化简：
$$
\left( \begin{array}{ccc|c} 3 & 2 & 2 & -1 \\ 2 & 2 & 2 & -2 \\ 2 & 2 & 2 & -2 \end{array} \right)
$$

1.  显然第 3 行与第 2 行相同，可直接消去第 3 行；同时第 2 行除以 2：
$$
\xrightarrow{\substack{r_3-r_2 \\ r_2 \times \frac{1}{2}}} \left( \begin{array}{ccc|c} 3 & 2 & 2 & -1 \\ 1 & 1 & 1 & -1 \\ 0 & 0 & 0 & 0 \end{array} \right)
$$

2.  交换 $r_1, r_2$ 并消元：
$$
\xrightarrow{r_1 \leftrightarrow r_2} \left( \begin{array}{ccc|c} 1 & 1 & 1 & -1 \\ 3 & 2 & 2 & -1 \\ 0 & 0 & 0 & 0 \end{array} \right)
\xrightarrow{r_2-3r_1} \left( \begin{array}{ccc|c} 1 & 1 & 1 & -1 \\ 0 & -1 & -1 & 2 \\ 0 & 0 & 0 & 0 \end{array} \right)
$$

3.  化为行最简形：
    * $r_2 \times (-1)$
    * $r_1 - r_2$

$$
\xrightarrow{r_2 \times (-1)} \left( \begin{array}{ccc|c} 1 & 1 & 1 & -1 \\ 0 & 1 & 1 & -2 \\ 0 & 0 & 0 & 0 \end{array} \right)
\xrightarrow{r_1-r_2} \left( \begin{array}{ccc|c} 1 & 0 & 0 & 1 \\ 0 & 1 & 1 & -2 \\ 0 & 0 & 0 & 0 \end{array} \right)
$$

### 2.3 写出通解

对应的方程组为：
$$
\begin{cases}
x_1 = 1 \\
x_2 + x_3 = -2
\end{cases} \implies \begin{cases}
x_1 = 1 \\
x_2 = -2 - k \\
x_3 = k
\end{cases}
$$
令自由未知量 $x_3 = k$（$k$ 为任意常数），写成向量形式：
$$
x = \begin{pmatrix} 1 \\ -2 \\ 0 \end{pmatrix} + k \begin{pmatrix} 0 \\ -1 \\ 1 \end{pmatrix}
$$

✅ **结论 (II)**：方程组的通解为 $x = \begin{pmatrix} 1 \\ -2 \\ 0 \end{pmatrix} + k \begin{pmatrix} 0 \\ -1 \\ 1 \end{pmatrix}$，其中 $k$ 为任意常数.

---

## 📌 关键要点总结

1.  **无解判定**：线性方程组无解 $\iff r(A) \ne r(A|\beta)$。处理此类含参问题时，先化阶梯形，再分析秩的关系。
2.  **正规方程组**：方程 $A^TAx = A^T\beta$ 实际上是最小二乘法的正规方程。即使原方程 $Ax=\beta$ 无解，正规方程组通常是有解的（有无穷多解或唯一解）。
3.  **计算细节**：在计算 $A^TA$ 时利用对称性可减少计算量；行变换时注意不要算错符号。