## 🎯 问题回顾

**解答题**：
已知矩阵 $A=\begin{pmatrix}0&-1&1\\ 2&-3&0\\ 0&0&0\end{pmatrix}$.
(I) 求 $A^{99}$;
(II) 设3阶矩阵 $B=(\alpha_{1},\alpha_{2},\alpha_{3})$ 满足 $B^{2}=BA$. 记 $B^{100}=(\beta_{1},\beta_{2},\beta_{3})$，将 $\beta_{1},\beta_{2},\beta_{3}$ 分别表示为 $\alpha_{1},\alpha_{2},\alpha_{3}$ 的线性组合.

---

## 第一部分：求 $A^{99}$

### 1.1 计算 $A$ 的特征值
求矩阵 $A$ 的特征多项式 $|\lambda E - A|$：
$$
|\lambda E - A| = \begin{vmatrix} \lambda & 1 & -1 \\ -2 & \lambda+3 & 0 \\ 0 & 0 & \lambda \end{vmatrix}
$$
按第三行展开：
$$
= \lambda \cdot [ \lambda(\lambda+3) - 1(-2) ] = \lambda (\lambda^2 + 3\lambda + 2) = \lambda(\lambda+1)(\lambda+2)
$$
令 $|\lambda E - A| = 0$，解得特征值：
$$
\lambda_1 = 0, \quad \lambda_2 = -1, \quad \lambda_3 = -2
$$
由 Cayley-Hamilton 定理知 $A(A+E)(A+2E) = O$，即 $A^3 + 3A^2 + 2A = O$.

### 1.2 利用多项式除法计算 $A^{99}$
设 $\lambda^{99}$ 除以特征多项式 $\lambda(\lambda+1)(\lambda+2)$ 的余式为 $a\lambda^2 + b\lambda + c$，即：
$$
\lambda^{99} = q(\lambda)\lambda(\lambda+1)(\lambda+2) + a\lambda^2 + b\lambda + c
$$
将特征值分别代入：
1.  令 $\lambda = 0$，得 $0 = c$.
2.  令 $\lambda = -1$，得 $(-1)^{99} = a - b \implies a - b = -1$.
3.  令 $\lambda = -2$，得 $(-2)^{99} = 4a - 2b \implies 2a - b = -2^{98}$.

由 (2) 得 $b = a + 1$，代入 (3)：
$$
2a - (a+1) = -2^{98} \implies a = 1 - 2^{98}
$$
从而：
$$
b = (1 - 2^{98}) + 1 = 2 - 2^{98}
$$

于是有：
$$
A^{99} = (1 - 2^{98})A^2 + (2 - 2^{98})A
$$

### 1.3 计算 $A^2$ 及最终结果
计算 $A^2$：
$$
A^2 = \begin{pmatrix} 0&-1&1\\ 2&-3&0\\ 0&0&0\end{pmatrix} \begin{pmatrix} 0&-1&1\\ 2&-3&0\\ 0&0&0\end{pmatrix} = \begin{pmatrix} -2 & 3 & 0 \\ -6 & 7 & 2 \\ 0 & 0 & 0 \end{pmatrix}
$$

将 $A$ 和 $A^2$ 代入公式：
$$
\begin{aligned}
A^{99} &= (1 - 2^{98}) \begin{pmatrix} -2 & 3 & 0 \\ -6 & 7 & 2 \\ 0 & 0 & 0 \end{pmatrix} + (2 - 2^{98}) \begin{pmatrix} 0 & -1 & 1 \\ 2 & -3 & 0 \\ 0 & 0 & 0 \end{pmatrix} \\
&= \begin{pmatrix} -2(1-2^{98}) & 3(1-2^{98}) - (2-2^{98}) & (2-2^{98}) \\ -6(1-2^{98}) + 2(2-2^{98}) & 7(1-2^{98}) - 3(2-2^{98}) & 2(1-2^{98}) \\ 0 & 0 & 0 \end{pmatrix}
\end{aligned}
$$

逐个元素化简：
* $(1,1)$: $-2 + 2 \cdot 2^{98} = 2^{99} - 2$
* $(1,2)$: $3 - 3 \cdot 2^{98} - 2 + 2^{98} = 1 - 2 \cdot 2^{98} = 1 - 2^{99}$
* $(1,3)$: $2 - 2^{98}$
* $(2,1)$: $-6 + 6 \cdot 2^{98} + 4 - 2 \cdot 2^{98} = -2 + 4 \cdot 2^{98} = 2^{100} - 2$
* $(2,2)$: $7 - 7 \cdot 2^{98} - 6 + 3 \cdot 2^{98} = 1 - 4 \cdot 2^{98} = 1 - 2^{100}$
* $(2,3)$: $2 - 2 \cdot 2^{98} = 2 - 2^{99}$

✅ **(I) 结果**：
$$
A^{99} = \begin{pmatrix} 2^{99}-2 & 1-2^{99} & 2-2^{98} \\ 2^{100}-2 & 1-2^{100} & 2-2^{99} \\ 0 & 0 & 0 \end{pmatrix}
$$

---

## 第二部分：表示 $\beta_1, \beta_2, \beta_3$

### 2.1 推导 $B^{100}$ 与 $A$ 的关系
已知 $B^2 = BA$.
$$
B^3 = B \cdot B^2 = B(BA) = B^2 A = (BA)A = BA^2
$$
归纳可知，对任意 $k \ge 2$，有：
$$
B^k = BA^{k-1}
$$
令 $k=100$，则：
$$
B^{100} = BA^{99}
$$

### 2.2 利用矩阵分块乘法表示
设 $A^{99} = (m_{ij})_{3 \times 3}$，由矩阵乘法的列分块规则：
$$
B^{100} = (\beta_1, \beta_2, \beta_3) = (\alpha_1, \alpha_2, \alpha_3) \begin{pmatrix} m_{11} & m_{12} & m_{13} \\ m_{21} & m_{22} & m_{23} \\ m_{31} & m_{32} & m_{33} \end{pmatrix}
$$
即 $\beta_j = m_{1j}\alpha_1 + m_{2j}\alpha_2 + m_{3j}\alpha_3$.

由第一部分计算结果可知，$A^{99}$ 的第三行元素均为 0（即 $m_{31}=m_{32}=m_{33}=0$），因此 $\alpha_3$ 的系数均为 0.

将 $A^{99}$ 的具体元素代入：

1.  **对于 $\beta_1$** (使用 $A^{99}$ 第一列):
    $$
    \beta_1 = (2^{99}-2)\alpha_1 + (2^{100}-2)\alpha_2
    $$
2.  **对于 $\beta_2$** (使用 $A^{99}$ 第二列):
    $$
    \beta_2 = (1-2^{99})\alpha_1 + (1-2^{100})\alpha_2
    $$
3.  **对于 $\beta_3$** (使用 $A^{99}$ 第三列):
    $$
    \beta_3 = (2-2^{98})\alpha_1 + (2-2^{99})\alpha_2
    $$

✅ **(II) 结果**：
$$
\begin{cases}
\beta_{1} = (2^{99}-2)\alpha_{1} + (2^{100}-2)\alpha_{2} \\
\beta_{2} = (1-2^{99})\alpha_{1} + (1-2^{100})\alpha_{2} \\
\beta_{3} = (2-2^{98})\alpha_{1} + (2-2^{99})\alpha_{2}
\end{cases}
$$

---

## 📌 关键要点总结

1.  **高次幂计算**：对于 $3 \times 3$ 具体矩阵的高次幂，首选 **多项式除法（Cayley-Hamilton定理推广）** 或 **相似对角化**。本题特征值互异，两种方法均可，多项式除法在不需要求特征向量时通常计算量更小。
2.  **矩阵递推关系**：注意 $B^2=BA \implies B^n = BA^{n-1}$，这是解决第二问的关键桥梁。
3.  **矩阵乘法的列视图**：$C = AB \implies c_j = \sum_i b_{ij} a_i$（其中 $c_j, a_i$ 分别为矩阵 $C, A$ 的列向量）。这一观点在将列向量表示为线性组合时非常直接。