## 🎯 问题回顾

**填空题**：
曲线 $y=x\left(1+\arcsin\dfrac{2}{x}\right)$ 的斜渐近线方程为 \_\_\_\_\_\_.

---

## 第一部分：斜渐近线的判定公式

若直线 $y = kx + b$ 是曲线 $y = f(x)$ 当 $x \to \infty$（或 $x \to -\infty$）时的斜渐近线，则系数 $k$ 和 $b$ 需满足以下极限：

1.  **斜率 $k$**：
    $$
    k = \lim_{x \to \infty} \frac{f(x)}{x}
    $$
2.  **截距 $b$**：
    $$
    b = \lim_{x \to \infty} [f(x) - kx]
    $$

首先确定函数的定义域：$\left| \frac{2}{x} \right| \le 1 \implies |x| \ge 2$。因此 $x \to \pm \infty$ 是有意义的。我们统一计算 $x \to \infty$ 的情况（左右极限结果一致）。

---

## 第二部分：计算斜率 $k$

由公式 $k = \lim_{x \to \infty} \frac{y}{x}$，代入函数表达式：

$$
k = \lim_{x \to \infty} \frac{x\left(1+\arcsin\dfrac{2}{x}\right)}{x}
$$

约去 $x$：

$$
k = \lim_{x \to \infty} \left(1 + \arcsin\dfrac{2}{x}\right)
$$

当 $x \to \infty$ 时，$\dfrac{2}{x} \to 0$，而 $\arcsin 0 = 0$。

✅ **解得**：
$$
k = 1 + 0 = 1
$$

---

## 第三部分：计算截距 $b$

由公式 $b = \lim_{x \to \infty} (y - kx)$，代入 $k=1$：

$$
\begin{aligned}
b &= \lim_{x \to \infty} \left[ x\left(1+\arcsin\dfrac{2}{x}\right) - 1 \cdot x \right] \\
&= \lim_{x \to \infty} \left( x + x\arcsin\dfrac{2}{x} - x \right) \\
&= \lim_{x \to \infty} x \arcsin\dfrac{2}{x}
\end{aligned}
$$

**利用等价无穷小代换**：
令 $t = \dfrac{2}{x}$，当 $x \to \infty$ 时，$t \to 0$。
此时原极限转化为：
$$
b = \lim_{t \to 0} \frac{2}{t} \cdot \arcsin t = 2 \lim_{t \to 0} \frac{\arcsin t}{t}
$$

根据经典极限 $\lim_{t \to 0} \frac{\arcsin t}{t} = 1$，可得：

✅ **解得**：
$$
b = 2 \times 1 = 2
$$

---

## ✅ 最终答案

该曲线的斜渐近线方程为：

$$
y = x + 2
$$

---

## 📌 关键要点总结

1.  **公式记忆**：求斜渐近线严格遵循 $k = \lim y/x$ 和 $b = \lim (y-kx)$ 两步走。
2.  **等价无穷小**：在计算 $b$ 的极限过程中，遇到 $\infty \cdot 0$ 型极限（即 $x \cdot \arcsin(2/x)$），利用变量代换转换为 $0/0$ 型，并使用 $\arcsin t \sim t \, (t \to 0)$ 进行快速计算是解题关键。
3.  **定义域检查**：虽然本题计算中 $x \to \pm \infty$ 结果一致，但在涉及根号或指数函数时，需留意正负无穷方向是否产生不同的渐近线。