## 🎯 问题回顾

**选择题**：
设数列 $\{x_{n}\}$ 收敛，则 ( )

- (A) 若 $\lim_{n\rightarrow\infty}\sin x_{n}=0$，则 $\lim_{n\rightarrow\infty}x_{n}=0$
- (B) 若 $\lim_{n\rightarrow\infty}(x_{n}+\sqrt{|x_{n}|})=0$，则 $\lim_{n\rightarrow\infty}x_{n}=0$
- (C) 若 $\lim_{n\rightarrow\infty}(x_{n}+x_{n}^2)=0$，则 $\lim_{n\rightarrow\infty}x_{n}=0$
- (D) 若 $\lim_{n\rightarrow\infty}(x_{n}+\sin x_{n})=0$，则 $\lim_{n\rightarrow\infty}x_{n}=0$

---

## 第一部分：设定极限并构建方程

设数列 $\{x_n\}$ 的极限为 $A$，即 $\lim_{n\to\infty} x_n = A$。
由于各选项中的函数均为连续函数，根据极限的运算法则，我们可以将极限符号移入函数内部，得到关于 $A$ 的方程。
我们要判断的是：哪一个方程的解 **只能是** $A=0$。

---

## 第二部分：逐项排查

### 选项 (A)
**条件**：$\lim_{n\to\infty} \sin x_n = 0 \implies \sin A = 0$。
**分析**：方程 $\sin A = 0$ 的解为 $A = k\pi$ ($k \in \mathbb{Z}$)。
**反例**：取 $x_n = \pi$，则 $\lim x_n = \pi \neq 0$，但 $\lim \sin x_n = \sin \pi = 0$。
**结论**：推不出 $A=0$。❌

### 选项 (B)
**条件**：$\lim_{n\to\infty}(x_{n}+\sqrt{|x_{n}|})=0 \implies A + \sqrt{|A|} = 0$。
**分析**：若 $A < 0$，令 $A = -1$，则 $-1 + \sqrt{|-1|} = -1 + 1 = 0$。方程成立。
**反例**：取 $x_n = -1$，则 $\lim x_n = -1 \neq 0$，但满足条件。
**结论**：推不出 $A=0$。❌

### 选项 (C)
**条件**：$\lim_{n\to\infty}(x_{n}+x_{n}^2)=0 \implies A + A^2 = 0$。
**分析**：$A(1+A) = 0$ 解得 $A=0$ 或 $A=-1$。
**反例**：取 $x_n = -1$，则 $\lim x_n = -1 \neq 0$，但满足条件。
**结论**：推不出 $A=0$。❌

### 选项 (D)
**条件**：$\lim_{n\to\infty}(x_{n}+\sin x_{n})=0 \implies A + \sin A = 0$。
**分析**：构造函数 $f(x) = x + \sin x$。
求导得 $f'(x) = 1 + \cos x$。
因为 $-1 \le \cos x \le 1$，所以 $f'(x) \ge 0$，即 $f(x)$ 是单调递增函数。
我们来寻找 $f(x)=0$ 的根：
1.  显然 $x=0$ 是根。
2.  当 $x > 0$ 时，由不等式 $\sin x > -x$ (即 $|\sin x| < x$) 可知 $x + \sin x > 0$。
3.  当 $x < 0$ 时，由不等式 $\sin x < -x$ (即 $- \sin x < -x$) 可知 $x + \sin x < 0$。
**结论**：方程 $x + \sin x = 0$ 有且仅有唯一解 $x=0$。
因此，若 $A + \sin A = 0$，则必有 $A=0$。
即 $\lim_{n\to\infty} x_n = 0$ 成立。✅

---

## ✅ 最终答案

**(D)**

---

## 📌 关键要点总结

1.  **极限运算性质**：若函数 $f(x)$ 连续，则 $\lim f(x_n) = f(\lim x_n)$。解此类题即解方程 $f(A)=0$。
2.  **反例法**：对于前三个选项，只需找到一个非零常数数列满足极限条件即可排除。
3.  **函数性质法**：对于选项 (D)，通过导数判断函数的单调性，或利用基本不等式 $|\sin x| < |x| (x \ne 0)$ 来证明方程根的唯一性。