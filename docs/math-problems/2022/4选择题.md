## 🎯 问题回顾

已知 $f(t)$ 连续，令 $F(x,y)=\int_{0}^{x-y}(x-y-t)f(t)dt$，则 ( )

- (A) $\frac{\partial F}{\partial x}=\frac{\partial F}{\partial y}, \frac{\partial^{2}F}{\partial x^{2}}=\frac{\partial^{2}F}{\partial y^{2}}$
- (B) $\frac{\partial F}{\partial x}=\frac{\partial F}{\partial y}, \frac{\partial^{2}F}{\partial x^{2}}=-\frac{\partial^{2}F}{\partial y^{2}}$
- (C) $\frac{\partial F}{\partial x}=-\frac{\partial F}{\partial y}, \frac{\partial^{2}F}{\partial x^{2}}=\frac{\partial^{2}F}{\partial y^{2}}$
- (D) $\frac{\partial F}{\partial x}=-\frac{\partial F}{\partial y}, \frac{\partial^{2}F}{\partial x^{2}}=-\frac{\partial^{2}F}{\partial y^{2}}$

---

## 第一部分：解题策略分析

### 1.1 结构观察

题目给出的函数 $F(x,y)$ 是一个变上限积分，且积分上限和被积函数中都包含变量 $x$ 和 $y$。直接对积分形式求导（使用莱布尼茨变上限积分求导公式）容易出错，特别是被积函数 $(x-y-t)$ 中也含有参数时。

### 1.2 优化策略：换元法（整体代换）

观察发现，$x$ 和 $y$ 总是以 $(x-y)$ 的整体形式出现。
我们可以引入中间变量 $u = x - y$，将二元函数转化为一元函数处理，这样可以极大地简化求导过程，避免复杂的偏导数计算错误。

---

## 第二部分：引入中间变量化简

### 2.1 变量代换

令 $u = x - y$，则原函数可以记为 $G(u)$：
$$
F(x, y) = \int_{0}^{u} (u - t)f(t) dt = G(u)
$$

### 2.2 整理函数 $G(u)$

将被积函数展开，把与积分变量 $t$ 无关的项 $u$ 提出来：
$$
G(u) = \int_{0}^{u} u f(t) dt - \int_{0}^{u} t f(t) dt
$$
$$
G(u) = u \int_{0}^{u} f(t) dt - \int_{0}^{u} t f(t) dt
$$

---

## 第三部分：求 $G(u)$ 的一阶与二阶导数

利用**变上限积分求导公式**（微积分基本定理）：$\frac{d}{du}\int_0^u g(t)dt = g(u)$。

### 3.1 计算一阶导数 $G'(u)$

对 $G(u) = u \int_{0}^{u} f(t) dt - \int_{0}^{u} t f(t) dt$ 两边关于 $u$ 求导。
注意第一项是乘积形式，需使用**乘积求导法则** $(uv)' = u'v + uv'$。

$$
G'(u) = \left( u \int_{0}^{u} f(t) dt \right)' - \left( \int_{0}^{u} t f(t) dt \right)'
$$

$$
G'(u) = \left( 1 \cdot \int_{0}^{u} f(t) dt + u \cdot f(u) \right) - u f(u)
$$

消去 $u f(u)$ 和 $-u f(u)$：
$$
G'(u) = \int_{0}^{u} f(t) dt
$$

### 3.2 计算二阶导数 $G''(u)$

继续对 $G'(u)$ 求导：
$$
G''(u) = \frac{d}{du} \left( \int_{0}^{u} f(t) dt \right) = f(u)
$$

---

## 第四部分：利用链式法则求偏导数

已知 $u = x - y$，则 $u$ 对 $x, y$ 的偏导数为：
$$
\frac{\partial u}{\partial x} = 1, \quad \frac{\partial u}{\partial y} = -1
$$

利用复合函数求导法则（链式法则）：
$$
\frac{\partial F}{\partial x} = \frac{dG}{du} \cdot \frac{\partial u}{\partial x}
$$
$$
\frac{\partial F}{\partial y} = \frac{dG}{du} \cdot \frac{\partial u}{\partial y}
$$

### 4.1 计算一阶偏导数并比较

$$
\frac{\partial F}{\partial x} = G'(u) \cdot 1 = G'(u)
$$
$$
\frac{\partial F}{\partial y} = G'(u) \cdot (-1) = -G'(u)
$$

**结论 1**：
$$
\frac{\partial F}{\partial x} = - \frac{\partial F}{\partial y}
$$
（据此排除选项 A、B）

### 4.2 计算二阶偏导数并比较

$$
\frac{\partial^2 F}{\partial x^2} = \frac{\partial}{\partial x} \left( G'(u) \right) = G''(u) \cdot \frac{\partial u}{\partial x} = f(u) \cdot 1 = f(u)
$$

$$
\frac{\partial^2 F}{\partial y^2} = \frac{\partial}{\partial y} \left( -G'(u) \right) = -G''(u) \cdot \frac{\partial u}{\partial y} = -f(u) \cdot (-1) = f(u)
$$

**结论 2**：
$$
\frac{\partial^2 F}{\partial x^2} = \frac{\partial^2 F}{\partial y^2}
$$

---

## 第五部分：综合判断与选项选择

根据推导结果：
1.  一阶偏导关系：$\frac{\partial F}{\partial x} = -\frac{\partial F}{\partial y}$
2.  二阶偏导关系：$\frac{\partial^2 F}{\partial x^2} = \frac{\partial^2 F}{\partial y^2}$

对比选项：
- (A) $\dots$ (不符)
- (B) $\dots$ (不符)
- (C) $\frac{\partial F}{\partial x}=-\frac{\partial F}{\partial y}, \frac{\partial^{2}F}{\partial x^{2}}=\frac{\partial^{2}F}{\partial y^{2}}$ **(符合)**
- (D) $\dots$ (不符)

---

## ✅ 最终答案：(C)

---

## 📌 关键要点总结

1.  **整体换元思想**：当函数表达式中变量以线性组合（如 $x \pm y$）形式整体出现时，令 $u = x \pm y$ 将二元问题转化为一元问题，是解题的捷径。
2.  **变上限积分求导**：
    - 基本公式：$\frac{d}{dx}\int_a^x f(t)dt = f(x)$
    - 莱布尼茨公式（含参变量）：$\frac{d}{dx}\int_{a(x)}^{b(x)} f(x,t)dt = f(x, b(x))b'(x) - f(x, a(x))a'(x) + \int_{a(x)}^{b(x)} \frac{\partial f}{\partial x} dt$。本题用换元法避开了这个复杂的公式，降低了出错率。
3.  **链式法则细节**：求二阶导数时，不要忘记 $\frac{\partial u}{\partial y} = -1$ 这一项在求导过程中会再次出现，导致负负得正。