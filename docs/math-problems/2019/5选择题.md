## 🎯 问题回顾

已知平面区域 $D=\{(x,y) \mid |x|+|y|\le\frac{\pi}{2}\}$，比较以下三个积分的大小：
- $I_{1}=\iint_{D}\sqrt{x^{2}+y^{2}}dxdy$
- $I_{2}=\iint_{D}\sin\sqrt{x^{2}+y^{2}}dxdy$
- $I_{3}=\iint_{D}(1-\cos\sqrt{x^{2}+y^{2}})dxdy$

选项：
- (A) $I_{3}<I_{2}<I_{1}$
- (B) $I_{2}<I_{1}<I_{3}$
- (C) $I_{1}<I_{2}<I_{3}$
- (D) $I_{2}<I_{3}<I_{1}$

---

## 第一部分：分析积分变量的取值范围

### 1.1 令 $r = \sqrt{x^2+y^2}$
观察三个积分的被积函数，发现它们都是关于 $r = \sqrt{x^2+y^2}$ 的函数。因此，比较积分大小的问题可以转化为比较被积函数在积分区域 $D$ 内的大小。

### 1.2 确定 $r$ 的取值范围
区域 $D$ 是由不等式 $|x|+|y|\le\frac{\pi}{2}$ 定义的正方形区域（旋转了45度），其中心在原点。
- **最小值**：显然在原点 $(0,0)$ 处，$r_{min} = 0$。
- **最大值**：该区域离原点最远的点是四个顶点：$(\pm\frac{\pi}{2}, 0)$ 和 $(0, \pm\frac{\pi}{2})$。
  在这些点处，$r_{max} = \sqrt{(\frac{\pi}{2})^2 + 0} = \frac{\pi}{2}$。

因此，对于积分区域 $D$ 内的所有点，均有：
$$
0 \le \sqrt{x^2+y^2} \le \frac{\pi}{2}
$$
令 $u = \sqrt{x^2+y^2}$，则 $u \in [0, \frac{\pi}{2}]$。

---

## 第二部分：比较被积函数的大小

我们需要在区间 $u \in (0, \frac{\pi}{2}]$ 上比较以下三个函数的大小：
- $f_1(u) = u$
- $f_2(u) = \sin u$
- $f_3(u) = 1 - \cos u$

### 2.1 比较 $I_1$ 和 $I_2$（比较 $u$ 和 $\sin u$）
构造辅助函数 $\phi(u) = u - \sin u$。
求导得：
$$
\phi'(u) = 1 - \cos u
$$
当 $u \in (0, \frac{\pi}{2}]$ 时，$\cos u < 1$，故 $\phi'(u) > 0$。
函数单调递增，且 $\phi(0) = 0$，因此当 $u > 0$ 时：
$$
u > \sin u
$$
由此可得被积函数 $f_1(u) > f_2(u)$，根据二重积分的保号性：
$$
I_1 > I_2
$$

### 2.2 比较 $I_2$ 和 $I_3$（比较 $\sin u$ 和 $1 - \cos u$）
构造辅助函数 $\psi(u) = \sin u - (1 - \cos u) = \sin u + \cos u - 1$。
利用辅助角公式变形：
$$
\psi(u) = \sqrt{2}\sin\left(u + \frac{\pi}{4}\right) - 1
$$
当 $u \in (0, \frac{\pi}{2})$ 时，相位 $u + \frac{\pi}{4} \in (\frac{\pi}{4}, \frac{3\pi}{4})$。
在此范围内，正弦函数 $\sin\left(u + \frac{\pi}{4}\right)$ 的值在 $(\frac{\sqrt{2}}{2}, 1]$ 之间。
所以：
$$
\psi(u) > \sqrt{2} \cdot \frac{\sqrt{2}}{2} - 1 = 1 - 1 = 0
$$
即 $\sin u > 1 - \cos u$ 在 $D$ 的内部（除原点外）几乎处处成立。
即使在边界点 $u = \frac{\pi}{2}$ 处，$\sin \frac{\pi}{2} = 1$，$1 - \cos \frac{\pi}{2} = 1$，两者相等，但这不影响积分的严格不等式关系。
因此：
$$
I_2 > I_3
$$

> **补充验证（泰勒展开法）**：
> 当 $u \to 0^+$ 时：
> $f_1(u) = u$ （一阶无穷小）
> $f_2(u) = \sin u \approx u$ （一阶无穷小，略小于 $u$）
> $f_3(u) = 1 - \cos u \approx \frac{1}{2}u^2$ （二阶无穷小）
> 显然在 $u$ 很小时，$u > \sin u > 1 - \cos u$。

---

## 第三部分：综合结论

综合上述不等式关系：
$$
I_1 > I_2 > I_3
$$
即：
$$
I_3 < I_2 < I_1
$$

对照选项：
- (A) $I_{3}<I_{2}<I_{1}$
- (B) $I_{2}<I_{1}<I_{3}$
- (C) $I_{1}<I_{2}<I_{3}$
- (D) $I_{2}<I_{3}<I_{1}$

✅ **正确答案是：(A)**

---

## 📌 关键步骤总结

1.  **变量统一**：发现被积函数仅依赖于半径 $r$，将比较积分转化为比较一元函数。
2.  **确定区间**：通过区域边界确定自变量的范围是 $[0, \frac{\pi}{2}]$。
3.  **函数比较**：
    -   利用导数或几何意义判断 $x > \sin x$。
    -   利用三角公式 $\sin x + \cos x > 1$ (在锐角范围内) 判断 $\sin x > 1 - \cos x$。
4.  **保号性**：若 $f(x) > g(x)$ 在积分区域内成立，则 $\iint f > \iint g$。