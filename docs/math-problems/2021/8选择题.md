## 🎯 题目回顾

**二次型** $f(x_{1},x_{2},x_{3})=(x_{1}+x_{2})^{2}+(x_{2}+x_{3})^{2}-(x_{3}-x_{1})^{2}$

求该二次型的**正惯性指数**（$p$）与**负惯性指数**（$q$），并从以下选项中选择：

- (A) 2, 0
- (B) 1, 1
- (C) 2, 1
- (D) 1, 2

---

## 💡 解题思路概览

要确定二次型的惯性指数，我们需要找到该二次型的**标准形**（Canonical Form）。标准形是只包含平方项的表达式：
$$f = d_1 y_1^2 + d_2 y_2^2 + d_3 y_3^2$$

- **正惯性指数 ($p$)**：标准形中正系数 ($d_i > 0$) 的个数。
- **负惯性指数 ($q$)**：标准形中负系数 ($d_i < 0$) 的个数。

我们可以通过两种主要方法解决此问题：
1.  **矩阵法（特征值法）**：写出对称矩阵，求特征值的符号。
2.  **配方法（拉格朗日法）**：通过代数变形直接化简为平方和。

为了保证严谨和准确，我们将**首选矩阵法**进行详细推导，并用**配方法**进行验证。

---

## 第一部分：化简二次型表达式

首先，我们需要将题目中给出的混合项展开并合并，得到二次型的多项式形式。

$$
f = \underbrace{(x_{1}+x_{2})^{2}}_{\text{第一项}} + \underbrace{(x_{2}+x_{3})^{2}}_{\text{第二项}} - \underbrace{(x_{3}-x_{1})^{2}}_{\text{第三项}}
$$

### 1.1 展开各项
利用完全平方公式 $(a+b)^2 = a^2 + 2ab + b^2$ 和 $(a-b)^2 = a^2 - 2ab + b^2$：

1.  $(x_1 + x_2)^2 = x_1^2 + 2x_1x_2 + x_2^2$
2.  $(x_2 + x_3)^2 = x_2^2 + 2x_2x_3 + x_3^2$
3.  $(x_3 - x_1)^2 = x_3^2 - 2x_1x_3 + x_1^2$

### 1.2 合并同类项
将上述展开式代入原方程：
$$
\begin{aligned}
f &= (x_1^2 + 2x_1x_2 + x_2^2) + (x_2^2 + 2x_2x_3 + x_3^2) - (x_3^2 - 2x_1x_3 + x_1^2) \\
  &= x_1^2 + 2x_1x_2 + x_2^2 + x_2^2 + 2x_2x_3 + x_3^2 - x_3^2 + 2x_1x_3 - x_1^2
\end{aligned}
$$

**观察并消元：**
- $x_1^2$ 项：$x_1^2 - x_1^2 = 0$
- $x_2^2$ 项：$x_2^2 + x_2^2 = 2x_2^2$
- $x_3^2$ 项：$x_3^2 - x_3^2 = 0$
- 交叉项：保留 $2x_1x_2$, $2x_2x_3$, $2x_1x_3$

**化简结果：**
$$
f(x_1, x_2, x_3) = 2x_2^2 + 2x_1x_2 + 2x_2x_3 + 2x_1x_3
$$

---

## 第二部分：矩阵法求惯性指数

### 2.1 写出二次型的矩阵 $A$

二次型 $f = X^T A X$ 对应的对称矩阵 $A$ 的元素规则如下：
- 主对角线元素 $a_{ii}$ 对应 $x_i^2$ 的系数。
- 非对角线元素 $a_{ij}$ ($i \ne j$) 对应 $x_i x_j$ 系数的一半。

根据 $f = 0\cdot x_1^2 + 2x_2^2 + 0\cdot x_3^2 + 2x_1x_2 + 2x_1x_3 + 2x_2x_3$：

- $a_{11} = 0, \quad a_{22} = 2, \quad a_{33} = 0$
- $a_{12} = a_{21} = \frac{2}{2} = 1$
- $a_{13} = a_{31} = \frac{2}{2} = 1$
- $a_{23} = a_{32} = \frac{2}{2} = 1$

得到矩阵：
$$
A = \begin{pmatrix}
0 & 1 & 1 \\
1 & 2 & 1 \\
1 & 1 & 0
\end{pmatrix}
$$

### 2.2 计算特征值

求解特征方程 $|\lambda E - A| = 0$：

$$
|\lambda E - A| = \begin{vmatrix}
\lambda & -1 & -1 \\
-1 & \lambda - 2 & -1 \\
-1 & -1 & \lambda
\end{vmatrix}
$$

**计算行列式（使用沙路法则或展开法）：**

$$
\begin{aligned}
|\lambda E - A| &= \lambda [(\lambda - 2)\lambda - (-1)(-1)] - (-1)[-\lambda - 1] + (-1)[1 - (-1)(\lambda - 2)] \\
&= \lambda (\lambda^2 - 2\lambda - 1) + (\lambda + 1) - (1 + \lambda - 2) \\
&= \lambda^3 - 2\lambda^2 - \lambda + \lambda + 1 - \lambda + 1 \\
& \quad \text{此处计算较为繁琐，我们改用更清晰的行列式性质化简：}
\end{aligned}
$$

**重新计算行列式（行列变换法）：**

$$
D = \begin{vmatrix}
\lambda & -1 & -1 \\
-1 & \lambda - 2 & -1 \\
-1 & -1 & \lambda
\end{vmatrix}
$$

1.  **$c_2 - c_3$（第二列减去第三列）：**
    $$
    D = \begin{vmatrix}
    \lambda & 0 & -1 \\
    -1 & \lambda - 1 & -1 \\
    -1 & -1 - \lambda & \lambda
    \end{vmatrix}
    $$
2.  **按第一行展开：**
    $$
    \begin{aligned}
    D &= \lambda \left[ (\lambda-1)\lambda - (-1)(-1-\lambda) \right] + (-1) \left[ -1(-1-\lambda) - (\lambda-1)(-1) \right] \\
    &= \lambda \left[ \lambda^2 - \lambda - (1+\lambda) \right] - \left[ 1+\lambda + \lambda - 1 \right] \\
    &= \lambda (\lambda^2 - 2\lambda - 1) - (2\lambda) \quad \leftarrow \text{似乎还有更简单的因子提取方式}
    \end{aligned}
    $$
    
    *修正路径：直接观察矩阵的迹和行列式性质更快捷。*

    **特征多项式 $P(\lambda) = \lambda^3 - \text{tr}(A)\lambda^2 + S_2\lambda - |A|$**
    
    1.  **迹 $\text{tr}(A)$**：$0 + 2 + 0 = 2$。
    2.  **行列式 $|A|$**：
        $$
        |A| = \begin{vmatrix} 0 & 1 & 1 \\ 1 & 2 & 1 \\ 1 & 1 & 0 \end{vmatrix} = 0 - 1(0-1) + 1(1-2) = 1 - 1 = 0
        $$
        因为 $|A|=0$，必有一个特征值为 $0$。
    3.  **计算 $P(\lambda)$**：
        直接回到之前的展开：
        $$
        |\lambda E - A| = \lambda(\lambda-2)\lambda + (-1)(-1)(-1) + (-1)(-1)(-1) - [(-1)(\lambda-2)(-1) + \lambda(-1)(-1) + (-1)(-1)\lambda]
        $$
        $$
        = \lambda^2(\lambda-2) - 1 - 1 - [(\lambda-2) + \lambda + \lambda]
        $$
        $$
        = \lambda^3 - 2\lambda^2 - 2 - (3\lambda - 2)
        $$
        $$
        = \lambda^3 - 2\lambda^2 - 3\lambda
        $$
        提取公因式 $\lambda$：
        $$
        = \lambda(\lambda^2 - 2\lambda - 3)
        $$
        因式分解括号内二次项：
        $$
        = \lambda(\lambda - 3)(\lambda + 1)
        $$

### 2.3 确定特征值与惯性指数

根据特征方程 $\lambda(\lambda - 3)(\lambda + 1) = 0$，解得特征值为：
1.  $\lambda_1 = 3$ （正数）
2.  $\lambda_2 = -1$ （负数）
3.  $\lambda_3 = 0$ （零）

**根据惯性定律：**
- **正惯性指数 ($p$)** = 正特征值的个数 = **1**
- **负惯性指数 ($q$)** = 负特征值的个数 = **1**

---

## 第三部分：配方法验证（可选，但增强信心）

我们尝试将 $f = 2x_2^2 + 2x_1x_2 + 2x_2x_3 + 2x_1x_3$ 配成平方和。

1.  **整理含 $x_2$ 的项**（因为 $x_2$ 有平方项，以此为主元）：
    $$
    f = 2[x_2^2 + x_2(x_1 + x_3)] + 2x_1x_3
    $$
2.  **配方**：
    构建 $(x_2 + \frac{x_1+x_3}{2})^2$ 的形式。
    $$
    f = 2\left[ \left(x_2 + \frac{x_1+x_3}{2}\right)^2 - \left(\frac{x_1+x_3}{2}\right)^2 \right] + 2x_1x_3
    $$
3.  **展开化简**：
    $$
    f = 2\left(x_2 + \frac{x_1+x_3}{2}\right)^2 - 2 \cdot \frac{x_1^2 + 2x_1x_3 + x_3^2}{4} + 2x_1x_3
    $$
    $$
    f = 2\left(x_2 + \frac{x_1+x_3}{2}\right)^2 - \frac{1}{2}(x_1^2 + 2x_1x_3 + x_3^2) + 2x_1x_3
    $$
    $$
    f = 2\left(x_2 + \frac{x_1+x_3}{2}\right)^2 - \frac{1}{2}x_1^2 - x_1x_3 - \frac{1}{2}x_3^2 + 2x_1x_3
    $$
    $$
    f = 2\left(x_2 + \frac{x_1+x_3}{2}\right)^2 - \frac{1}{2}x_1^2 + x_1x_3 - \frac{1}{2}x_3^2
    $$
4.  **处理剩余项**：
    提取 $-\frac{1}{2}$：
    $$
    - \frac{1}{2}x_1^2 + x_1x_3 - \frac{1}{2}x_3^2 = -\frac{1}{2}(x_1^2 - 2x_1x_3 + x_3^2) = -\frac{1}{2}(x_1 - x_3)^2
    $$
5.  **最终标准形**：
    令 $y_1 = x_2 + \frac{x_1+x_3}{2}, \quad y_2 = x_1 - x_3$。
    $$
    f = 2y_1^2 - \frac{1}{2}y_2^2
    $$

**结论验证：**
- $y_1^2$ 的系数为 $2$ (正)。
- $y_2^2$ 的系数为 $-\frac{1}{2}$ (负)。
- 不存在 $y_3^2$ 项（系数为0）。

结果一致：**正惯性指数为 1，负惯性指数为 1。**

---

## 第四部分：选项判定

综合上述计算结果：
- 正惯性指数 $p = 1$
- 负惯性指数 $q = 1$

对应选项：
- (A) 2, 0 ❌
- **(B) 1, 1 ✅**
- (C) 2, 1 ❌
- (D) 1, 2 ❌

## ✅ 最终答案：(B)

---

## 📌 关键要点总结

1.  **化简优先**：题目给出的形式是完全平方的混合运算，**切勿直接对括号内的线性组合求秩**，必须先展开合并同类项得到 $x^T A x$ 的标准形式。
2.  **特征值物理意义**：对于实对称矩阵，正特征值的数量即正惯性指数，负特征值的数量即负惯性指数。
3.  **零特征值的影响**：零特征值意味着矩阵不满秩（行列式为0），它不贡献正负惯性指数，这解释了为什么指数之和（1+1=2）小于变量数（3）。
4.  **双重验证**：考试中若时间允许，可用配方法验证矩阵法的结果，确保万无一失。

**Would you like me to create a similar practice problem involving parameter discussion (e.g., determine the parameter $k$ for a specific inertia index)?**