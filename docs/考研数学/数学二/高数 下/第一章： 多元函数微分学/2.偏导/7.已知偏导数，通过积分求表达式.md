# 📚 二、知识小结：已知偏导数通过积分求原函数表达式

## ✅ 核心思想
当题目给出某函数的一阶或二阶偏导数表达式，并提供特定条件（如初始值），可通过**不定积分法**反向求出原函数 $ z = f(x, y) $ 的表达式。

> 💡 类比：一元函数中，若已知导数 $ f'(x) $，可积分得 $ f(x) = \int f'(x)\,dx + C $；  
> 多元函数中，类似地，由偏导积分可还原原函数。

---

## 🔹 一、题型特征

- **题目给出**：一阶或二阶偏导数的表达式；
- **附加条件**：在某个点处的函数值或偏导数值（如 $ z(0,0)=1 $、$ z_x(1,1)=2 $ 等）；
- **目标**：求出原函数 $ z = f(x, y) $ 的具体表达式。

📌 常见形式：
- 已知 $ \frac{\partial z}{\partial x} = f(x,y) $，求 $ z $
- 已知 $ \frac{\partial^2 z}{\partial x \partial y} = g(x,y) $，求 $ z $

---

## 🔹 二、积分规律（核心公式）

### ✅ 不同偏导对应的积分规则：

| 已知偏导 | 积分结果 |
|---------|----------|
| 若已知 $ \dfrac{\partial^2 z}{\partial x^2} $ | 则 $ \displaystyle \int \frac{\partial^2 z}{\partial x^2}\,\mathrm{d}x = \frac{\partial z}{\partial x} + \varphi(y) $<br>（对 $ x $ 积分，加关于 $ y $ 的任意函数） |
| 若已知 $ \dfrac{\partial^2 z}{\partial x \partial y} $ | 则 $ \displaystyle \int \frac{\partial^2 z}{\partial x \partial y}\,\mathrm{d}x = \frac{\partial z}{\partial y} + \varphi(y) $<br>或 $ \displaystyle \int \frac{\partial^2 z}{\partial x \partial y}\,\mathrm{d}y = \frac{\partial z}{\partial x} + \varphi(x) $ |
| 若已知 $ \dfrac{\partial^2 z}{\partial y^2} $ | 则 $ \displaystyle \int \frac{\partial^2 z}{\partial y^2}\,\mathrm{d}y = \frac{\partial z}{\partial y} + \varphi(x) $<br>（对 $ y $ 积分，加关于 $ x $ 的任意函数） |
| 若已知 $ \dfrac{\partial z}{\partial x} $ | 则 $ \displaystyle \int \frac{\partial z}{\partial x}\,\mathrm{d}x = z + \varphi(y) $<br>（对 $ x $ 积分，加关于 $ y $ 的任意函数） |
| 若已知 $ \dfrac{\partial z}{\partial y} $ | 则 $ \displaystyle \int \frac{\partial z}{\partial y}\,\mathrm{d}y = z + \varphi(x) $<br>（对 $ y $ 积分，加关于 $ x $ 的任意函数） |

> ✅ 关键原则：
> - 对哪个变量积分，就以该变量为积分变量；
> - 积分后增加的“任意函数”必须是**另一个变量的函数**；
> - 例如：对 $ x $ 积分，结果中加 $ \varphi(y) $，因为 $ y $ 是“常数”方向。

---

## 🔹 三、解题步骤（通用流程）

### ✅ 步骤一：确定积分方式
根据已知偏导的阶数和变量，选择合适的积分路径。

### ✅ 步骤二：进行积分
将偏导表达式代入积分公式，计算得到中间结果。

### ✅ 步骤三：整理表达式
将积分结果写成 $ z = \cdots + \varphi(\cdot) $ 的形式。

### ✅ 步骤四：利用条件确定未知函数
使用题目给出的初始条件（如 $ z(0,0)=1 $ 或 $ z_x(1,1)=3 $）来确定未知函数 $ \varphi(x) $ 或 $ \varphi(y) $。

### ✅ 步骤五：写出最终表达式
代入未知函数，得到完整的原函数表达式。

---

## 🔹 四、典型例题讲解

### 🌟 示例1：已知 $ \dfrac{\partial z}{\partial x} = 2x + y $，且 $ z(0,0) = 1 $，求 $ z $

**解**：
1. 对 $ x $ 积分：
   $$
   z = \int (2x + y)\,\mathrm{d}x = x^2 + xy + \varphi(y)
   $$
2. 用条件 $ z(0,0) = 1 $：
   $$
   z(0,0) = 0 + 0 + \varphi(0) = 1 \Rightarrow \varphi(0) = 1
   $$
   但无法确定 $ \varphi(y) $ 具体形式 → 需更多信息（如 $ \frac{\partial z}{\partial y} $）。

⚠️ 若只给一阶偏导，可能无法唯一确定原函数，需补充信息。

---

### 🌟 示例2：已知 $ \dfrac{\partial^2 z}{\partial x \partial y} = 2xy $，且 $ z(0,0) = 0 $，$ z_x(0,0) = 1 $，求 $ z $

**解**：
1. 对 $ x $ 积分：
   $$
   \int \frac{\partial^2 z}{\partial x \partial y}\,\mathrm{d}x = \frac{\partial z}{\partial y} = \int 2xy\,\mathrm{d}x = x^2 y + \varphi(y)
   $$
2. 对 $ y $ 积分：
   $$
   z = \int (x^2 y + \varphi(y))\,\mathrm{d}y = \frac{1}{2}x^2 y^2 + \int \varphi(y)\,\mathrm{d}y = \frac{1}{2}x^2 y^2 + \psi(y)
   $$
3. 求 $ z_x $：
   $$
   z_x = x y^2 + \psi'(y) \quad \text{（注意：这里 } \psi'(y) \text{ 是 } y \text{ 的函数）}
   $$
4. 用条件 $ z_x(0,0) = 1 $：
   $$
   z_x(0,0) = 0 + \psi'(0) = 1 \Rightarrow \psi'(0) = 1
   $$
   但仍需更多条件才能确定 $ \psi(y) $。

👉 实际考试中通常会给出足够条件，确保唯一解。

---

## 🔹 五、特殊情形处理

### ✅ 情况一：缺少部分偏导表达式
例如：已知 $ z(x,0) = 3x $，但未直接给出偏导。

**解决方法**：
- 将 $ y = 0 $ 看作常数，对 $ x $ 求导：
  $$
  \left.\frac{\partial z}{\partial x}\right|_{y=0} = \frac{d}{dx}(3x) = 3
  $$
- 即得 $ z_x(x,0) = 3 $，可用于后续积分。

---

## 🔹 六、公式推导与三种求偏导值方法对比

### ✅ 求某点偏导值的三种方法：

| 方法 | 公式/说明 | 适用场景 |
|------|-----------|----------|
| **方法一：直接计算** | $ f'_x(x_0,y_0) = \left.\frac{df(x,y_0)}{dx}\right|_{x=x_0} $<br>$ f'_y(x_0,y_0) = \left.\frac{df(x_0,y)}{dy}\right|_{y=y_0} $ | 函数表达式明确，可直接求导 |
| **方法二：极限定义** | $ f'_x(x_0,y_0) = \lim_{x \to x_0} \frac{f(x,y_0)-f(x_0,y_0)}{x-x_0} $<br>$ f'_y(x_0,y_0) = \lim_{y \to y_0} \frac{f(x_0,y)-f(x_0,y_0)}{y-y_0} $ | 分段函数、不可导点、无表达式时 |
| **方法三：公式变形** | 由方法二推导得出，用于特殊情况处理 | 缺少表达式时辅助分析 |

> ✅ 提示：方法一最常用；方法二用于验证或特殊点；方法三用于理论推导。

---

## 🔹 七、题目规律总结

### ✅ 数量关系：
- 一次偏导：通常有一个待定函数（如 $ \varphi(y) $）；
- 二次偏导：通常有两个待定函数（如 $ \varphi(x), \psi(y) $）；
- 题目中一般会给出两个条件（如一个函数值、一个偏导值）来确定这两个函数。

### ✅ 注意事项：
- 积分后加的函数必须是**另一个变量的函数**；
- 不能随意假设未知函数为常数；
- 必须结合题目条件列方程求解未知函数。

---

## 🧠 总结一句话：

> **已知偏导数求原函数，关键是“积分+补函数+代条件”。先对相应变量积分，结果中加上另一个变量的任意函数，再利用已知条件确定该函数，最终得到原函数表达式。**

