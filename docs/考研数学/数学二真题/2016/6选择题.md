## 🎯 问题回顾

已知函数 $f(x,y)=\frac{e^{x}}{x-y}$，判断其偏导数满足的关系式：

- (A) $f_{x}^{\prime}-f_{y}^{\prime}=0$
- (B) $f_{x}^{\prime}+f_{y}^{\prime}=0$
- (C) $f_{x}^{\prime}-f_{y}^{\prime}=f$
- (D) $f_{x}^{\prime}+f_{y}^{\prime}=f$

---

## 第一部分：计算偏导数

### 1.1 计算对 $x$ 的偏导数 $f_x^{\prime}$

将 $y$ 视为常数，利用商的求导法则 $(\frac{u}{v})' = \frac{u'v - uv'}{v^2}$，其中 $u=e^x, v=x-y$。

$$
\begin{aligned}
f_x^{\prime} &= \frac{\partial}{\partial x} \left( \frac{e^x}{x-y} \right) \\
&= \frac{(e^x)^{\prime}_x \cdot (x-y) - e^x \cdot (x-y)^{\prime}_x}{(x-y)^2} \\
&= \frac{e^x (x-y) - e^x \cdot 1}{(x-y)^2} \\
&= \frac{e^x(x-y-1)}{(x-y)^2}
\end{aligned}
$$

或者利用积的求导法则，将函数看作 $e^x \cdot (x-y)^{-1}$：
$$
f_x^{\prime} = e^x \cdot \frac{1}{x-y} + e^x \cdot \left[ -(x-y)^{-2} \right] = f(x,y) - \frac{e^x}{(x-y)^2}
$$

### 1.2 计算对 $y$ 的偏导数 $f_y^{\prime}$

将 $x$ 视为常数，此时 $e^x$ 是常数系数，函数看作 $e^x \cdot (x-y)^{-1}$。
利用复合函数求导法则：

$$
\begin{aligned}
f_y^{\prime} &= \frac{\partial}{\partial y} \left( e^x (x-y)^{-1} \right) \\
&= e^x \cdot (-1)(x-y)^{-2} \cdot (x-y)^{\prime}_y \\
&= e^x \cdot \frac{-1}{(x-y)^2} \cdot (-1) \\
&= \frac{e^x}{(x-y)^2}
\end{aligned}
$$

---

## 第二部分：验证选项

我们需要计算 $f_x^{\prime} \pm f_y^{\prime}$ 的值。

### 2.1 计算 $f_x^{\prime} + f_y^{\prime}$

将上述结果相加：

$$
\begin{aligned}
f_x^{\prime} + f_y^{\prime} &= \frac{e^x(x-y-1)}{(x-y)^2} + \frac{e^x}{(x-y)^2} \\
&= \frac{e^x(x-y) - e^x + e^x}{(x-y)^2} \\
&= \frac{e^x(x-y)}{(x-y)^2} \\
&= \frac{e^x}{x-y}
\end{aligned}
$$

### 2.2 结论对照

观察发现计算结果正好是原函数：
$$
\frac{e^x}{x-y} = f(x,y)
$$

即：
$$
f_x^{\prime} + f_y^{\prime} = f
$$

---

## ✅ 最终答案：**(D)**

---

## 📌 关键要点总结

1.  **偏导数计算**：求 $f_x$ 时需同时对分子分母求导（商法则），求 $f_y$ 时仅分母含变量（链式法则）。
2.  **代数化简**：在验证选项时，先不急着完全展开分子，而是观察能否通过合并同类项消去部分因子。
3.  **结构观察**：在步骤 1.1 中若采用 $f_x^{\prime} = f - \frac{e^x}{(x-y)^2}$ 的形式，结合 $f_y^{\prime} = \frac{e^x}{(x-y)^2}$，一眼即可看出两者相加等于 $f$。