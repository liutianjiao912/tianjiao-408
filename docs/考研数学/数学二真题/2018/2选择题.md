## 🎯 问题回顾

**选择题**：下列函数中，在 $x=0$ 处**不可导**的是 ( )

- (A) $f(x)=|x|\sin|x|$
- (B) $f(x)=|x|\sin\sqrt{|x|}$
- (C) $f(x)=\cos|x|$
- (D) $f(x)=\cos\sqrt{|x|}$

---

## 💡 解题核心思路：导数的定义

判断函数 $f(x)$ 在点 $x=0$ 处是否可导，最严谨且通用的方法是验证**导数的定义极限**是否存在。

函数 $f(x)$ 在 $x=0$ 处可导的充要条件是：
$$
f'(0) = \lim_{x \to 0} \frac{f(x) - f(0)}{x} \quad \text{存在且唯一}
$$

由于题目中包含绝对值符号 $|x|$，通常需要分别考察**左导数** ($x \to 0^-$) 和**右导数** ($x \to 0^+$)。
- 若 $f'_-(0) = f'_+(0) = A$（有限值），则函数可导。
- 若 $f'_-(0) \ne f'_+(0)$，或其中之一不存在，则函数不可导。

下面我们将逐一分析四个选项。

---

## 选项 (A) 的详细分析

### 1. 函数表达式
$$f(x) = |x|\sin|x|$$
首先计算 $f(0) = |0|\sin|0| = 0$。

### 2. 化简函数（去除绝对值）
我们利用三角函数的奇偶性：
- 当 $x > 0$ 时，$|x|=x$，则 $f(x) = x\sin x$。
- 当 $x < 0$ 时，$|x|=-x$，则 $f(x) = (-x)\sin(-x) = (-x)(-\sin x) = x\sin x$。

惊奇地发现，对于所有实数 $x$，都有 **$f(x) = x\sin x$**。
由于 $y=x$ 和 $y=\sin x$ 都是处处可导的初等函数，它们的乘积自然处处可导。但为了严谨，我们仍使用定义验证。

### 3. 使用导数定义计算
$$
\begin{aligned}
f'(0) &= \lim_{x \to 0} \frac{f(x) - f(0)}{x} \\
&= \lim_{x \to 0} \frac{x\sin x - 0}{x} \\
&= \lim_{x \to 0} (\sin x) \\
&= 0
\end{aligned}
$$

### 4. 结论
左极限等于右极限等于 0。
✅ **选项 (A) 在 $x=0$ 处可导，且 $f'(0)=0$。**

---

## 选项 (B) 的详细分析

### 1. 函数表达式
$$f(x) = |x|\sin\sqrt{|x|}$$
易知 $f(0) = 0$。

### 2. 使用导数定义计算
我们需要计算极限：
$$
L = \lim_{x \to 0} \frac{|x|\sin\sqrt{|x|} - 0}{x} = \lim_{x \to 0} \frac{|x|}{x} \cdot \sin\sqrt{|x|}
$$

此处 $\frac{|x|}{x}$ 是符号函数 $\text{sgn}(x)$，其值为 $\pm 1$。为了稳妥，我们分左右极限讨论。

#### (1) 右导数 ($x \to 0^+$)
当 $x > 0$ 时，$|x| = x$。
$$
f'_+(0) = \lim_{x \to 0^+} \frac{x \sin\sqrt{x}}{x} = \lim_{x \to 0^+} \sin\sqrt{x} = 0
$$

#### (2) 左导数 ($x \to 0^-$)
当 $x < 0$ 时，$|x| = -x$。
$$
f'_-(0) = \lim_{x \to 0^-} \frac{(-x)\sin\sqrt{-x}}{x} = \lim_{x \to 0^-} -\sin\sqrt{-x} = 0
$$

### 3. 结论
因为 $f'_+(0) = f'_-(0) = 0$。
✅ **选项 (B) 在 $x=0$ 处可导，且 $f'(0)=0$。**

---

## 选项 (C) 的详细分析

### 1. 函数表达式
$$f(x) = \cos|x|$$
易知 $f(0) = \cos(0) = 1$。

### 2. 利用偶函数性质化简
由余弦函数的偶函数性质 $\cos(-u) = \cos(u)$ 可知：
$$
\cos|x| \equiv \cos x
$$
因为 $\cos x$ 是基本初等函数，在全实数域上处处可导，所以它在 $x=0$ 处必然可导。
导数值为 $-\sin(0) = 0$。

### 3. 使用泰勒展开（严谨验证）
如果不使用上述性质，我们也可以用泰勒公式展开 $\cos u$。
令 $u = |x|$，则：
$$
\cos|x| = 1 - \frac{|x|^2}{2!} + \frac{|x|^4}{4!} - \cdots
$$
因为 $|x|^2 = x^2$，所以：
$$
f(x) = 1 - \frac{x^2}{2} + O(x^4)
$$
代入导数定义：
$$
\begin{aligned}
f'(0) &= \lim_{x \to 0} \frac{(1 - \frac{x^2}{2} + \cdots) - 1}{x} \\
&= \lim_{x \to 0} \frac{-\frac{x^2}{2}}{x} \\
&= \lim_{x \to 0} \left(-\frac{x}{2}\right) = 0
\end{aligned}
$$

### 4. 结论
✅ **选项 (C) 在 $x=0$ 处可导，且 $f'(0)=0$。**

---

## 选项 (D) 的详细分析

### 1. 函数表达式
$$f(x) = \cos\sqrt{|x|}$$
易知 $f(0) = \cos(0) = 1$。

### 2. 尝试使用泰勒展开（关键步骤）
我们利用 $\cos u \sim 1 - \frac{u^2}{2}$ (当 $u \to 0$)。
令 $u = \sqrt{|x|}$，则 $u^2 = |x|$。
$$
f(x) = \cos\sqrt{|x|} = 1 - \frac{(\sqrt{|x|})^2}{2!} + o((\sqrt{|x|})^2) = 1 - \frac{|x|}{2} + o(|x|)
$$

现在我们考察导数定义的极限：
$$
\frac{f(x) - f(0)}{x} = \frac{(1 - \frac{|x|}{2} + o(|x|)) - 1}{x} = \frac{-\frac{|x|}{2} + o(|x|)}{x}
$$
化简得：
$$
\frac{f(x) - f(0)}{x} = -\frac{1}{2} \cdot \frac{|x|}{x} + \frac{o(|x|)}{x}
$$
当 $x \to 0$ 时，$\frac{o(|x|)}{x} \to 0$（高阶无穷小），所以极限主要取决于第一项 **$-\frac{1}{2} \cdot \frac{|x|}{x}$**。

### 3. 分别计算左右导数

#### (1) 右导数 ($x \to 0^+$)
当 $x > 0$ 时，$\frac{|x|}{x} = \frac{x}{x} = 1$。
$$
f'_+(0) = \lim_{x \to 0^+} \left( -\frac{1}{2} \cdot 1 \right) = -\frac{1}{2}
$$

#### (2) 左导数 ($x \to 0^-$)
当 $x < 0$ 时，$\frac{|x|}{x} = \frac{-x}{x} = -1$。
$$
f'_-(0) = \lim_{x \to 0^-} \left( -\frac{1}{2} \cdot (-1) \right) = \frac{1}{2}
$$

### 4. 结论
$$
f'_+(0) = -\frac{1}{2} \quad \ne \quad f'_-(0) = \frac{1}{2}
$$
左右导数存在但不相等。
❌ **选项 (D) 在 $x=0$ 处不可导**（此处为“尖点”）。

---

## 📊 总结对比表

| 选项 | 函数 $f(x)$ | 导数计算关键 ($x \to 0$) | 左右导数情况 | 结论 |
| :--- | :--- | :--- | :--- | :--- |
| **(A)** | $|x|\sin|x|$ | 化简为 $x\sin x$ | $L=R=0$ | 可导 |
| **(B)** | $|x|\sin\sqrt{|x|}$ | 幅度受 $|x|$ 控制 | $L=R=0$ | 可导 |
| **(C)** | $\cos|x|$ | 偶函数，化简为 $\cos x$ | $L=R=0$ | 可导 |
| **(D)** | $\cos\sqrt{|x|}$ | **近似为 $1 - \frac{|x|}{2}$** | **$L = \frac{1}{2}, R = -\frac{1}{2}$** | **不可导** |

---

## ✅ 最终答案

综上所述，在 $x=0$ 处不可导的函数是：

**(D) $f(x)=\cos\sqrt{|x|}$**