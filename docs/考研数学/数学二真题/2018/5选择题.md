## 🎯 问题回顾

**题目：**
设：
$$
M = \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} \frac{(1+x)^{2}}{1+x^{2}} dx
$$
$$
N = \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} \frac{1+x}{e^{x}} dx
$$
$$
K = \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} (1+\sqrt{\cos x}) dx
$$
比较 $M, N, K$ 的大小，并选择正确选项：
- (A) $M>N>K$
- (B) $M>K>N$
- (C) $K>M>N$
- (D) $K>N>M$

---

## 第一部分：计算积分 $M$

### 1.1 展开被积函数
首先观察被积函数：
$$
\frac{(1+x)^2}{1+x^2} = \frac{1 + 2x + x^2}{1+x^2}
$$
我们可以将其拆分为两部分：
$$
\frac{1 + x^2}{1 + x^2} + \frac{2x}{1 + x^2} = 1 + \frac{2x}{1 + x^2}
$$

### 1.2 利用奇偶性简化积分
将积分 $M$ 写成两项之和：
$$
M = \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} 1 \, dx + \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} \frac{2x}{1+x^2} \, dx
$$

1.  **第一项**：
    $$
    \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} 1 \, dx = \frac{\pi}{2} - \left(-\frac{\pi}{2}\right) = \pi
    $$
2.  **第二项**：
    设 $f(x) = \dfrac{2x}{1+x^2}$。
    检验奇偶性：$f(-x) = \dfrac{2(-x)}{1+(-x)^2} = -\dfrac{2x}{1+x^2} = -f(x)$。
    由于 $f(x)$ 是奇函数，且积分区间 $[-\frac{\pi}{2}, \frac{\pi}{2}]$ 关于原点对称，故该项积分为 **0**。

### 1.3 $M$ 的结果
$$
M = \pi + 0 = \pi
$$

---

## 第二部分：估算积分 $K$

### 2.1 拆分积分
$$
K = \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} (1 + \sqrt{\cos x}) \, dx = \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} 1 \, dx + \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} \sqrt{\cos x} \, dx
$$

### 2.2 分析各项
1.  **第一项**：
    同上，值为 $\pi$。
2.  **第二项**：
    在区间 $(-\frac{\pi}{2}, \frac{\pi}{2})$ 内，$\cos x > 0$，因此 $\sqrt{\cos x} > 0$。
    被积函数严格大于 0，故积分值必然为正数：
    $$
    \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} \sqrt{\cos x} \, dx > 0
    $$

### 2.3 $K$ 与 $M$ 的比较
由上可知：
$$
K = \pi + (\text{正数}) > \pi
$$
即：
$$
K > M
$$

---

## 第三部分：计算积分 $N$

### 3.1 整理被积函数
$$
N = \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} (1+x)e^{-x} \, dx
$$
采用**分部积分法**。设 $u = 1+x$， $dv = e^{-x}dx$，则 $du = dx$， $v = -e^{-x}$。

### 3.2 执行积分
$$
\begin{aligned}
N &= \left[ -(1+x)e^{-x} \right]_{-\frac{\pi}{2}}^{\frac{\pi}{2}} - \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} (-e^{-x}) \, dx \\
&= \left[ -(1+x)e^{-x} \right]_{-\frac{\pi}{2}}^{\frac{\pi}{2}} + \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}} e^{-x} \, dx \\
&= \left[ -(1+x)e^{-x} - e^{-x} \right]_{-\frac{\pi}{2}}^{\frac{\pi}{2}} \\
&= \left[ -e^{-x}(x + 2) \right]_{-\frac{\pi}{2}}^{\frac{\pi}{2}}
\end{aligned}
$$

### 3.3 代入上下限计算
代入上限 $\frac{\pi}{2}$：
$$
-e^{-\frac{\pi}{2}}\left(\frac{\pi}{2} + 2\right)
$$
代入下限 $-\frac{\pi}{2}$：
$$
-e^{\frac{\pi}{2}}\left(-\frac{\pi}{2} + 2\right) = -e^{\frac{\pi}{2}}\left(2 - \frac{\pi}{2}\right)
$$
相减得到 $N$：
$$
N = -e^{-\frac{\pi}{2}}\left(\frac{\pi}{2} + 2\right) - \left[ -e^{\frac{\pi}{2}}\left(2 - \frac{\pi}{2}\right) \right]
$$
整理得：
$$
N = e^{\frac{\pi}{2}}\left(2 - \frac{\pi}{2}\right) - e^{-\frac{\pi}{2}}\left(2 + \frac{\pi}{2}\right)
$$

### 3.4 数值估算
取 $\pi \approx 3.14$，则 $\frac{\pi}{2} \approx 1.57$。
- 第一项系数：$2 - 1.57 = 0.43$ （正数）
- 第二项系数：$2 + 1.57 = 3.57$ （正数）
- 指数项：$e^{1.57} \approx 4.8$， $e^{-1.57} \approx 0.2$

估算 $N$ 的值：
$$
N \approx 4.8 \times 0.43 - 0.2 \times 3.57 \approx 2.06 - 0.71 = 1.35
$$

显然：
$$
1.35 < 3.14 \quad \Rightarrow \quad N < M
$$

---

## 第四部分：综合比较与结论

根据前三部分的计算结果：
1.  **$M = \pi$**
2.  **$K = \pi + \text{正数} \implies K > M$**
3.  **$N \approx 1.35 < \pi \implies M > N$**

将三者串联起来：
$$
K > M > N
$$

对应选项：
- (A) $M>N>K$
- (B) $M>K>N$
- **(C) $K>M>N$** ✅
- (D) $K>N>M$

---

## ✅ 最终答案：(C)

---

## 📌 关键要点总结

1.  **利用对称性**：计算 $M$ 时，识别出 $\frac{2x}{1+x^2}$ 是奇函数，在对称区间上积分为 0，大大简化了计算。
2.  **拆分比较法**：计算 $K$ 时，不需要算出 $\sqrt{\cos x}$ 的具体积分值（这是一个椭圆积分，很难算），只需知道它恒大于 0，即可得出 $K > \pi$ 的结论。
3.  **分部积分与估算**：计算 $N$ 时，通过分部积分得到精确表达式，再通过简单的数值估算判断其与 $\pi$ 的大小关系。
4.  **数值参考**：记住 $\pi \approx 3.14$, $e \approx 2.71$, $\sqrt{e} \approx 1.65$ 等常数有助于快速判断不等式方向。