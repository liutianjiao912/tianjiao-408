## 🎯 问题回顾

计算参数方程曲线：

$$
\begin{cases}
x = \cos^3 t \\
y = \sin^3 t
\end{cases}
$$

在 $t = \frac{\pi}{4}$ 对应点处的曲率 $\kappa = \_\_\_\_\_\_.$

---

## 第一部分：曲率计算公式

对于参数方程 $\begin{cases} x = x(t) \\ y = y(t) \end{cases}$，曲率计算公式为：

$$
\kappa = \frac{|x'(t)y''(t) - x''(t)y'(t)|}{\left[ (x'(t))^2 + (y'(t))^2 \right]^{\frac{3}{2}}}
$$

解题思路分为三步：
1.  求出 $x(t)$ 和 $y(t)$ 的一阶、二阶导数表达式；
2.  代入 $t = \frac{\pi}{4}$ 计算具体的数值；
3.  代入曲率公式计算最终结果。

---

## 第二部分：导数计算与数值代入

### 2.1 准备三角函数值
当 $t = \frac{\pi}{4}$ 时：
$$
\sin \frac{\pi}{4} = \frac{\sqrt{2}}{2}, \quad \cos \frac{\pi}{4} = \frac{\sqrt{2}}{2}
$$

### 2.2 计算 $x$ 的导数
**一阶导数 $x'(t)$**：
$$
x(t) = \cos^3 t
$$
$$
x'(t) = 3\cos^2 t \cdot (-\sin t) = -3\cos^2 t \sin t
$$
代入 $t = \frac{\pi}{4}$：
$$
x'\left(\frac{\pi}{4}\right) = -3 \cdot \left(\frac{\sqrt{2}}{2}\right)^2 \cdot \frac{\sqrt{2}}{2} = -3 \cdot \frac{1}{2} \cdot \frac{\sqrt{2}}{2} = -\frac{3\sqrt{2}}{4}
$$

**二阶导数 $x''(t)$**：
对 $x'(t) = -3\cos^2 t \sin t$ 求导（使用乘法法则）：
$$
\begin{aligned}
x''(t) &= -3 \left[ (\cos^2 t)' \sin t + \cos^2 t (\sin t)' \right] \\
&= -3 \left[ (-2\cos t \sin t) \sin t + \cos^2 t \cdot \cos t \right] \\
&= -3 \left[ -2\cos t \sin^2 t + \cos^3 t \right] \\
&= 6\cos t \sin^2 t - 3\cos^3 t
\end{aligned}
$$
代入 $t = \frac{\pi}{4}$（此时 $\sin t = \cos t = \frac{\sqrt{2}}{2}$）：
$$
\begin{aligned}
x''\left(\frac{\pi}{4}\right) &= 6 \left(\frac{\sqrt{2}}{2}\right)^3 - 3 \left(\frac{\sqrt{2}}{2}\right)^3 \\
&= 3 \left(\frac{\sqrt{2}}{2}\right)^3 = 3 \cdot \frac{2\sqrt{2}}{8} = \frac{3\sqrt{2}}{4}
\end{aligned}
$$

### 2.3 计算 $y$ 的导数
**一阶导数 $y'(t)$**：
$$
y(t) = \sin^3 t
$$
$$
y'(t) = 3\sin^2 t \cdot \cos t
$$
代入 $t = \frac{\pi}{4}$：
$$
y'\left(\frac{\pi}{4}\right) = 3 \cdot \left(\frac{\sqrt{2}}{2}\right)^2 \cdot \frac{\sqrt{2}}{2} = \frac{3\sqrt{2}}{4}
$$

**二阶导数 $y''(t)$**：
对 $y'(t) = 3\sin^2 t \cos t$ 求导：
$$
\begin{aligned}
y''(t) &= 3 \left[ (\sin^2 t)' \cos t + \sin^2 t (\cos t)' \right] \\
&= 3 \left[ (2\sin t \cos t) \cos t + \sin^2 t (-\sin t) \right] \\
&= 3 \left[ 2\sin t \cos^2 t - \sin^3 t \right] \\
&= 6\sin t \cos^2 t - 3\sin^3 t
\end{aligned}
$$
代入 $t = \frac{\pi}{4}$：
$$
\begin{aligned}
y''\left(\frac{\pi}{4}\right) &= 6 \left(\frac{\sqrt{2}}{2}\right)^3 - 3 \left(\frac{\sqrt{2}}{2}\right)^3 \\
&= 3 \left(\frac{\sqrt{2}}{2}\right)^3 = \frac{3\sqrt{2}}{4}
\end{aligned}
$$

---

## 第三部分：代入曲率公式计算

汇总我们在 $t=\frac{\pi}{4}$ 处计算出的数值：
* $x' = -\frac{3\sqrt{2}}{4}$
* $x'' = \frac{3\sqrt{2}}{4}$
* $y' = \frac{3\sqrt{2}}{4}$
* $y'' = \frac{3\sqrt{2}}{4}$

为了简化计算，令 $A = \frac{3\sqrt{2}}{4}$。则对应值为：$-A, A, A, A$。

### 3.1 计算分子
$$
\text{分子} = |x'y'' - x''y'| = |(-A)(A) - (A)(A)| = |-A^2 - A^2| = |-2A^2| = 2A^2
$$
计算 $2A^2$ 的值：
$$
2A^2 = 2 \left( \frac{3\sqrt{2}}{4} \right)^2 = 2 \cdot \frac{18}{16} = \frac{36}{16} = \frac{9}{4}
$$

### 3.2 计算分母
$$
\begin{aligned}
\text{底数} &= (x')^2 + (y')^2 = (-A)^2 + (A)^2 = 2A^2 = \frac{9}{4} \\
\text{分母} &= \left( \frac{9}{4} \right)^{\frac{3}{2}} = \left( \sqrt{\frac{9}{4}} \right)^3 = \left( \frac{3}{2} \right)^3 = \frac{27}{8}
\end{aligned}
$$

### 3.3 计算曲率 $\kappa$
$$
\kappa = \frac{\text{分子}}{\text{分母}} = \frac{9/4}{27/8} = \frac{9}{4} \times \frac{8}{27}
$$
进行约分：
$$
\kappa = \frac{1}{1} \times \frac{2}{3} = \frac{2}{3}
$$

---

## ✅ 最终答案

曲线在 $t=\frac{\pi}{4}$ 处的曲率为 \_\_ $\frac{2}{3}$ \_\_。

---

## 📌 关键步骤总结

1.  **公式记忆**：准确记忆参数方程的曲率公式 $\kappa = \frac{|x'y'' - x''y'|}{((x')^2 + (y')^2)^{3/2}}$。
2.  **导数计算**：熟练运用复合函数求导法则（链式法则）和乘法法则，计算二阶导数时需格外细心。
3.  **代值简化**：先代入 $t$ 的值求出具体的数字再代入公式，比带着复杂的三角函数式子化简要不容易出错。
4.  **数值运算**：注意分数繁分数的化简技巧。