## 🎯 问题回顾

设 $A$ 为 3 阶矩阵，$\alpha_{1},\alpha_{2},\alpha_{3}$ 为线性无关的向量组。若满足以下关系：
$$
\begin{cases}
A\alpha_1 = 2\alpha_1 + \alpha_2 + \alpha_3 \\
A\alpha_2 = \alpha_2 + 2\alpha_3 \\
A\alpha_{3} = -\alpha_2 + \alpha_3
\end{cases}
$$
求 $A$ 的**实特征值**。

---

## 第一部分：理论基础与矩阵构造

### 1.1 相似矩阵与特征值
题目给出的是线性变换 $A$ 在基底 $\{\alpha_1, \alpha_2, \alpha_3\}$ 下的作用关系。
根据线性代数理论，若矩阵 $A$ 与矩阵 $B$ 相似（即 $B$ 是 $A$ 在某组基下的矩阵表示），则它们拥有**相同的特征多项式**，从而拥有**相同的特征值**。

令矩阵 $P = (\alpha_1, \alpha_2, \alpha_3)$。由于向量组线性无关，矩阵 $P$ 可逆。
题目中的关系式可以写成矩阵乘法的形式：
$$
A(\alpha_1, \alpha_2, \alpha_3) = (\alpha_1, \alpha_2, \alpha_3) B
$$
即 $AP = PB \implies B = P^{-1}AP$。
因此，我们只需写出矩阵 $B$，并求出 $B$ 的特征值即可。

### 1.2 构造矩阵 $B$
矩阵 $B$ 的第 $j$ 列对应 $A\alpha_j$ 在基底 $\{\alpha_1, \alpha_2, \alpha_3\}$ 下的坐标。

1.  **分析 $A\alpha_1$**：
    $$
    A\alpha_1 = 2\alpha_1 + 1\alpha_2 + 1\alpha_3
    $$
    对应坐标向量为 $(2, 1, 1)^T$，即 $B$ 的**第 1 列**。

2.  **分析 $A\alpha_2$**：
    $$
    A\alpha_2 = 0\alpha_1 + 1\alpha_2 + 2\alpha_3
    $$
    对应坐标向量为 $(0, 1, 2)^T$，即 $B$ 的**第 2 列**。

3.  **分析 $A\alpha_3$**：
    $$
    A\alpha_3 = 0\alpha_1 - 1\alpha_2 + 1\alpha_3
    $$
    对应坐标向量为 $(0, -1, 1)^T$，即 $B$ 的**第 3 列**。

综上，矩阵 $B$ 为：
$$
B = \begin{pmatrix}
2 & 0 & 0 \\
1 & 1 & -1 \\
1 & 2 & 1
\end{pmatrix}
$$

---

## 第二部分：计算特征值

### 2.1 建立特征方程
特征值 $\lambda$ 需满足特征方程 $| \lambda E - B | = 0$。

$$
| \lambda E - B | = \begin{vmatrix}
\lambda - 2 & 0 & 0 \\
-1 & \lambda - 1 & 1 \\
-1 & -2 & \lambda - 1
\end{vmatrix} = 0
$$

### 2.2 行列式展开计算
观察第一行只有第一个元素非零，故按**第一行展开**：

$$
\begin{aligned}
| \lambda E - B | &= (\lambda - 2) \cdot (-1)^{1+1} \cdot \begin{vmatrix} \lambda - 1 & 1 \\ -2 & \lambda - 1 \end{vmatrix} \\
&= (\lambda - 2) \cdot \left[ (\lambda - 1)(\lambda - 1) - (1)(-2) \right] \\
&= (\lambda - 2) \cdot \left[ (\lambda - 1)^2 + 2 \right] \\
&= (\lambda - 2) (\lambda^2 - 2\lambda + 1 + 2) \\
&= (\lambda - 2) (\lambda^2 - 2\lambda + 3)
\end{aligned}
$$

### 2.3 求解特征根
令特征多项式为零：
$$
(\lambda - 2) (\lambda^2 - 2\lambda + 3) = 0
$$

这导出了两种情况：

1.  **第一部分**：
    $$
    \lambda - 2 = 0 \implies \lambda_1 = 2
    $$
    这是实数，符合题意。

2.  **第二部分**：
    $$
    \lambda^2 - 2\lambda + 3 = 0
    $$
    计算判别式 $\Delta$：
    $$
    \Delta = b^2 - 4ac = (-2)^2 - 4 \times 1 \times 3 = 4 - 12 = -8 < 0
    $$
    由于判别式小于零，该方程无实根，只有一对共轭复根 ($\lambda = 1 \pm \sqrt{2}i$)。

---

## 第三部分：结论

题目要求求解 $A$ 的**实特征值**。
在上述解出的根中，只有 $\lambda = 2$ 是实数。

✅ **最终答案**：
$$
2
$$

---

## 📌 关键步骤总结

1.  **坐标化**：将抽象的向量运算转化为具体的矩阵 $B$。注意列向量的排列顺序，不要混淆行列。
2.  **降阶计算**：利用行列式中零元素较多的行/列（本题第一行）进行展开，迅速降低计算量。
3.  **根的判别**：求解一元二次方程时，注意检查判别式 $\Delta$ 的符号，区分实根与复根。