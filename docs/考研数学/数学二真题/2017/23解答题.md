这是一道关于**二次型标准化**的经典解答题。它涵盖了**二次型的矩阵表示、特征值与特征向量的计算、实对称矩阵的正交对角化**等核心考点。

以下是详细的解题步骤。

---

## 🎯 题目回顾

设二次型：
$$
f(x_{1},x_{2},x_{3})=2x_{1}^{2}-x_{2}^{2}+ax_{3}^{2}+2x_{1}x_{2}-8x_{1}x_{3}+2x_{2}x_{3}
$$
在正交变换 $x=Qy$ 下的标准形为 $\lambda_{1}y_{1}^{2}+\lambda_{2}y_{2}^{2}$。

**求：**
1.  参数 $a$ 的值；
2.  一个正交矩阵 $Q$。

---

## 第一部分：确定参数 $a$ 的值

### 1.1 写出二次型的矩阵 $A$

二次型 $f(x) = x^T A x$，其中 $A$ 为实对称矩阵。
矩阵 $A$ 的元素确定规则如下：
* **对角线元素** $(a_{ii})$：对应平方项 $x_i^2$ 的系数。
* **非对角线元素** $(a_{ij}, i \ne j)$：对应交叉项 $x_i x_j$ 系数的一半。

由此可得矩阵 $A$：
$$
A = \begin{pmatrix}
2 & 1 & -4 \\
1 & -1 & 1 \\
-4 & 1 & a
\end{pmatrix}
$$

### 1.2 利用标准形特征确定 $a$

题目指出标准形为 $\lambda_{1}y_{1}^{2}+\lambda_{2}y_{2}^{2}$。
注意到标准形中**缺少 $y_3^2$ 项**，这意味着矩阵 $A$ 的三个特征值中，**必然有一个为 0**。

根据矩阵性质：**矩阵特征值的乘积等于矩阵的行列式**。
即 $\lambda_1 \cdot \lambda_2 \cdot \lambda_3 = |A|$。
因为存在一个特征值为 0，所以：
$$
|A| = 0
$$

### 1.3 计算行列式并求解 $a$

计算 $|A|$：
$$
|A| = \begin{vmatrix}
2 & 1 & -4 \\
1 & -1 & 1 \\
-4 & 1 & a
\end{vmatrix}
$$

利用代数余子式展开（或沙路法）：
$$
\begin{aligned}
|A| &= 2 \cdot [(-1)(a) - (1)(1)] - 1 \cdot [(1)(a) - (1)(-4)] + (-4) \cdot [(1)(1) - (-1)(-4)] \\
&= 2(-a - 1) - 1(a + 4) - 4(1 - 4) \\
&= -2a - 2 - a - 4 + 12 \\
&= -3a + 6
\end{aligned}
$$

令 $|A| = 0$，解得：
$$
-3a + 6 = 0 \implies a = 2
$$

✅ **第一步结论：$a = 2$。**

此时矩阵 $A$ 为：
$$
A = \begin{pmatrix}
2 & 1 & -4 \\
1 & -1 & 1 \\
-4 & 1 & 2
\end{pmatrix}
$$

---

## 第二部分：求特征值与特征向量

我们需要通过正交变换 $x=Qy$ 将 $A$ 对角化，因此必须求出 $A$ 的所有特征值和对应的特征向量。

### 2.1 求解特征值

计算特征多项式 $|\lambda E - A| = 0$：
$$
|\lambda E - A| = \begin{vmatrix}
\lambda - 2 & -1 & 4 \\
-1 & \lambda + 1 & -1 \\
4 & -1 & \lambda - 2
\end{vmatrix}
$$

**计算技巧（利用行变换化简）：**
观察第 1 行和第 3 行形式接近，用第 3 行减去第 1 行（$r_3 - r_1$）：
$$
\begin{vmatrix}
\lambda - 2 & -1 & 4 \\
-1 & \lambda + 1 & -1 \\
4 - (\lambda - 2) & 0 & (\lambda - 2) - 4
\end{vmatrix}
= \begin{vmatrix}
\lambda - 2 & -1 & 4 \\
-1 & \lambda + 1 & -1 \\
6 - \lambda & 0 & \lambda - 6
\end{vmatrix}
$$

提取第 3 行的公因子 $(\lambda - 6)$：
$$
= (\lambda - 6) \begin{vmatrix}
\lambda - 2 & -1 & 4 \\
-1 & \lambda + 1 & -1 \\
-1 & 0 & 1
\end{vmatrix}
$$

将第 3 列加到第 1 列（$c_1 + c_3$）：
$$
= (\lambda - 6) \begin{vmatrix}
\lambda + 2 & -1 & 4 \\
-2 & \lambda + 1 & -1 \\
0 & 0 & 1
\end{vmatrix}
$$

按第 3 行展开：
$$
\begin{aligned}
|\lambda E - A| &= (\lambda - 6) \cdot [1 \cdot ((\lambda + 2)(\lambda + 1) - (-2)(-1))] \\
&= (\lambda - 6) (\lambda^2 + 3\lambda + 2 - 2) \\
&= (\lambda - 6) (\lambda^2 + 3\lambda) \\
&= \lambda(\lambda - 6)(\lambda + 3)
\end{aligned}
$$

令 $|\lambda E - A| = 0$，解得三个特征值：
$$
\lambda_1 = -3, \quad \lambda_2 = 6, \quad \lambda_3 = 0
$$

> **💡 检查点**：
> 特征值之和 $\sum \lambda = -3 + 6 + 0 = 3$。
> 矩阵迹 $\text{tr}(A) = 2 + (-1) + 2 = 3$。
> 两者相等，计算无误。

### 2.2 求解特征向量

对每个特征值 $\lambda_i$，求解齐次线性方程组 $(A - \lambda_i E)x = 0$（即 $(\lambda_i E - A)x = 0$）。

#### (1) 对于 $\lambda_1 = -3$
解方程 $(-3E - A)x = 0 \Rightarrow (A + 3E)x = 0$：
$$
A + 3E = \begin{pmatrix}
5 & 1 & -4 \\
1 & 2 & 1 \\
-4 & 1 & 5
\end{pmatrix}
$$
进行初等行变换：
$$
\xrightarrow{r_1 \leftrightarrow r_2} \begin{pmatrix} 1 & 2 & 1 \\ 5 & 1 & -4 \\ -4 & 1 & 5 \end{pmatrix}
\xrightarrow{\substack{r_2 - 5r_1 \\ r_3 + 4r_1}} \begin{pmatrix} 1 & 2 & 1 \\ 0 & -9 & -9 \\ 0 & 9 & 9 \end{pmatrix}
\xrightarrow{r_2 \div (-9)} \begin{pmatrix} 1 & 2 & 1 \\ 0 & 1 & 1 \\ 0 & 0 & 0 \end{pmatrix}
$$
方程组等价于：
$$
\begin{cases} x_1 + 2x_2 + x_3 = 0 \\ x_2 + x_3 = 0 \end{cases} \implies \begin{cases} x_2 = -x_3 \\ x_1 = -2x_2 - x_3 = 2x_3 - x_3 = x_3 \end{cases}
$$
令 $x_3 = 1$，得特征向量：
$$
\xi_1 = \begin{pmatrix} 1 \\ -1 \\ 1 \end{pmatrix}
$$

#### (2) 对于 $\lambda_2 = 6$
解方程 $(6E - A)x = 0$：
$$
6E - A = \begin{pmatrix}
4 & -1 & 4 \\
-1 & 7 & -1 \\
4 & -1 & 4
\end{pmatrix}
$$
进行初等行变换：
$$
\xrightarrow{r_3 - r_1} \begin{pmatrix} 4 & -1 & 4 \\ -1 & 7 & -1 \\ 0 & 0 & 0 \end{pmatrix}
\xrightarrow{r_1 + 4r_2} \begin{pmatrix} 0 & 27 & 0 \\ -1 & 7 & -1 \\ 0 & 0 & 0 \end{pmatrix}
$$
由第一行得 $27x_2 = 0 \Rightarrow x_2 = 0$。代入第二行得 $-x_1 - x_3 = 0 \Rightarrow x_1 = -x_3$。
令 $x_3 = 1$，得特征向量：
$$
\xi_2 = \begin{pmatrix} -1 \\ 0 \\ 1 \end{pmatrix}
$$

#### (3) 对于 $\lambda_3 = 0$
解方程 $Ax = 0$：
$$
A = \begin{pmatrix}
2 & 1 & -4 \\
1 & -1 & 1 \\
-4 & 1 & 2
\end{pmatrix}
$$
进行初等行变换：
$$
\xrightarrow{r_1 \leftrightarrow r_2} \begin{pmatrix} 1 & -1 & 1 \\ 2 & 1 & -4 \\ -4 & 1 & 2 \end{pmatrix}
\xrightarrow{\substack{r_2 - 2r_1 \\ r_3 + 4r_1}} \begin{pmatrix} 1 & -1 & 1 \\ 0 & 3 & -6 \\ 0 & -3 & 6 \end{pmatrix}
\xrightarrow{r_2 \div 3} \begin{pmatrix} 1 & -1 & 1 \\ 0 & 1 & -2 \\ 0 & 0 & 0 \end{pmatrix}
$$
方程组等价于：
$$
\begin{cases} x_1 - x_2 + x_3 = 0 \\ x_2 - 2x_3 = 0 \end{cases} \implies \begin{cases} x_2 = 2x_3 \\ x_1 = x_2 - x_3 = 2x_3 - x_3 = x_3 \end{cases}
$$
令 $x_3 = 1$，得特征向量：
$$
\xi_3 = \begin{pmatrix} 1 \\ 2 \\ 1 \end{pmatrix}
$$

---

## 第三部分：构造正交矩阵 $Q$

### 3.1 验证正交性
实对称矩阵属于不同特征值的特征向量必相互正交。
验证：
* $\xi_1 \cdot \xi_2 = 1(-1) + (-1)(0) + 1(1) = 0$ ✅
* $\xi_1 \cdot \xi_3 = 1(1) + (-1)(2) + 1(1) = 0$ ✅
* $\xi_2 \cdot \xi_3 = -1(1) + 0(2) + 1(1) = 0$ ✅

无需施密特正交化，直接进行单位化即可。

### 3.2 单位化特征向量
* $||\xi_1|| = \sqrt{1^2 + (-1)^2 + 1^2} = \sqrt{3}$
    $$
    q_1 = \frac{1}{\sqrt{3}} \begin{pmatrix} 1 \\ -1 \\ 1 \end{pmatrix}
    $$
* $||\xi_2|| = \sqrt{(-1)^2 + 0^2 + 1^2} = \sqrt{2}$
    $$
    q_2 = \frac{1}{\sqrt{2}} \begin{pmatrix} -1 \\ 0 \\ 1 \end{pmatrix}
    $$
* $||\xi_3|| = \sqrt{1^2 + 2^2 + 1^2} = \sqrt{6}$
    $$
    q_3 = \frac{1}{\sqrt{6}} \begin{pmatrix} 1 \\ 2 \\ 1 \end{pmatrix}
    $$

### 3.3 写出矩阵 $Q$
将单位化后的列向量拼成矩阵 $Q = (q_1, q_2, q_3)$。
注意：题目要求的标准形为 $\lambda_1 y_1^2 + \lambda_2 y_2^2$，这意味着非零特征值对应的向量应放在前两列。我们将 $\lambda=-3$ 和 $\lambda=6$ 对应的向量排在前面，$\lambda=0$ 的放在最后。

$$
Q = \begin{pmatrix}
\frac{1}{\sqrt{3}} & -\frac{1}{\sqrt{2}} & \frac{1}{\sqrt{6}} \\
-\frac{1}{\sqrt{3}} & 0 & \frac{2}{\sqrt{6}} \\
\frac{1}{\sqrt{3}} & \frac{1}{\sqrt{2}} & \frac{1}{\sqrt{6}}
\end{pmatrix}
$$

此时对应的标准形为 $-3y_1^2 + 6y_2^2$。

---

## ✅ 最终答案

1.  **参数 $a$ 的值**：
    $$
    a = 2
    $$

2.  **正交矩阵 $Q$**：
    $$
    Q = \begin{pmatrix}
    \frac{1}{\sqrt{3}} & -\frac{1}{\sqrt{2}} & \frac{1}{\sqrt{6}} \\
    -\frac{1}{\sqrt{3}} & 0 & \frac{2}{\sqrt{6}} \\
    \frac{1}{\sqrt{3}} & \frac{1}{\sqrt{2}} & \frac{1}{\sqrt{6}}
    \end{pmatrix}
    $$
    *(注：$Q$ 的列向量顺序可以交换，只要与标准形中系数的顺序对应即可)*

---

## 📌 关键步骤复盘

1.  **参数求解**：根据标准形缺项（特征值为0）推导出 $|A|=0$，这是解题的突破口。
2.  **特征值计算**：利用行列式的行变换制造“0”元素，避免直接展开三次多项式带来的计算错误。
3.  **特征向量求解**：解齐次方程组时，务必细心进行行变换，确保基础解系正确。
4.  **正交矩阵构造**：实对称矩阵的不同特征值对应的向量自动正交，只需单位化；若有重根则需进行施密特正交化（本题无重根）。