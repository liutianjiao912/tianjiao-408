## 🎯 问题回顾

设矩阵
$$
A = \begin{pmatrix} 4 & 1 & -2 \\ 1 & 2 & a \\ 3 & 1 & -1 \end{pmatrix}
$$
其一个特征向量为
$$
\mathbf{x} = \begin{pmatrix} 1 \\ 1 \\ 2 \end{pmatrix}
$$
求 $a$ 的值。

---

## 第一部分：特征值与特征向量的定义

### 1.1 定义回顾

若非零向量 $\mathbf{x}$ 是矩阵 $A$ 的特征向量，则存在一个标量 $\lambda$（特征值），使得满足以下特征方程：

$$
A\mathbf{x} = \lambda \mathbf{x}
$$

### 1.2 建立方程组

将已知矩阵 $A$ 和向量 $\mathbf{x}$ 代入定义式：

$$
\begin{pmatrix} 4 & 1 & -2 \\ 1 & 2 & a \\ 3 & 1 & -1 \end{pmatrix} \begin{pmatrix} 1 \\ 1 \\ 2 \end{pmatrix} = \lambda \begin{pmatrix} 1 \\ 1 \\ 2 \end{pmatrix}
$$

---

## 第二部分：矩阵运算与求解

### 2.1 计算等式左边 $A\mathbf{x}$

我们将矩阵与向量相乘：

$$
A\mathbf{x} = \begin{pmatrix} 
4 \times 1 + 1 \times 1 + (-2) \times 2 \\ 
1 \times 1 + 2 \times 1 + a \times 2 \\ 
3 \times 1 + 1 \times 1 + (-1) \times 2 
\end{pmatrix}
$$

逐行计算：
1. **第一行**：$4 + 1 - 4 = 1$
2. **第二行**：$1 + 2 + 2a = 3 + 2a$
3. **第三行**：$3 + 1 - 2 = 2$

于是左边为：
$$
A\mathbf{x} = \begin{pmatrix} 1 \\ 3 + 2a \\ 2 \end{pmatrix}
$$

### 2.2 计算等式右边 $\lambda \mathbf{x}$

$$
\lambda \mathbf{x} = \begin{pmatrix} \lambda \\ \lambda \\ 2\lambda \end{pmatrix}
$$

### 2.3 联立方程组求解

比较等式左右两边的对应分量，得到如下方程组：

$$
\begin{cases}
1 = \lambda & \text{...... (1)} \\
3 + 2a = \lambda & \text{...... (2)} \\
2 = 2\lambda & \text{...... (3)}
\end{cases}
$$

#### ✅ 步骤 1：确定特征值 $\lambda$

由方程 (1) 直接得出：
$$
\lambda = 1
$$

（注：方程 (3) $2 = 2\lambda \Rightarrow \lambda = 1$ 与方程 (1) 结果一致，说明特征向量 $\mathbf{x}$ 选取无误，系统相容。）

#### ✅ 步骤 2：求解参数 $a$

将 $\lambda = 1$ 代入方程 (2)：
$$
3 + 2a = 1
$$

解该一元一次方程：
$$
2a = 1 - 3
$$
$$
2a = -2
$$
$$
a = -1
$$

---

## 第三部分：验证（可选但推荐）

为了确保结果准确，我们将 $a = -1$ 代入原矩阵，验证 $A\mathbf{x} = 1 \cdot \mathbf{x}$ 是否成立。

矩阵变为：
$$
A = \begin{pmatrix} 4 & 1 & -2 \\ 1 & 2 & -1 \\ 3 & 1 & -1 \end{pmatrix}
$$

计算 $A\mathbf{x}$：
$$
\begin{pmatrix} 4 & 1 & -2 \\ 1 & 2 & -1 \\ 3 & 1 & -1 \end{pmatrix} \begin{pmatrix} 1 \\ 1 \\ 2 \end{pmatrix} = \begin{pmatrix} 4+1-4 \\ 1+2-2 \\ 3+1-2 \end{pmatrix} = \begin{pmatrix} 1 \\ 1 \\ 2 \end{pmatrix}
$$

结果正好等于 $1 \cdot \mathbf{x}$。

✅ **验证通过**。

---

## ✅ 最终答案

$$
a = -1
$$

---

## 📌 关键要点总结

1.  **定义即解法**：解决此类问题的核心是直接使用 $A\mathbf{x} = \lambda \mathbf{x}$。
2.  **利用已知分量求 $\lambda$**：观察向量 $\mathbf{x}$ 和 $A\mathbf{x}$ 的结果，通常会有不含未知数 $a$ 的行（本题第一、三行），可直接求出特征值 $\lambda$。
3.  **回代求解**：求出 $\lambda$ 后，代入包含未知数 $a$ 的方程即可求得最终结果。
4.  **系统相容性**：虽然只需一行即可求 $\lambda$，但检查其他已知行是否一致（如本题行1和行3）是防止计算错误的有效手段。