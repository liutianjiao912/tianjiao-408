## 🎯 问题回顾

**选择题**：
设 $A$ 为 3 阶矩阵，$P=(\alpha_{1},\alpha_{2},\alpha_{3})$ 为可逆矩阵，使得 $P^{-1}AP=\begin{pmatrix}0&0&0\\ 0&1&0\\ 0&0&2\end{pmatrix}$，则 $A(\alpha_{1}+\alpha_{2}+\alpha_{3})=$ ( )

- (A) $\alpha_{1}+\alpha_{2}$
- (B) $\alpha_{2}+2\alpha_{3}$
- (C) $\alpha_{2}+\alpha_{3}$
- (D) $\alpha_{1}+2\alpha_{2}$

---

## 第一部分：特征值与特征向量的定义解析

### 1.1 矩阵相似对角化的等式变形
已知 $P^{-1}AP = \Lambda$，其中 $\Lambda = \begin{pmatrix}0&0&0\\ 0&1&0\\ 0&0&2\end{pmatrix}$。
对等式两边左乘矩阵 $P$，得到：
$$
AP = P\Lambda
$$

### 1.2 将矩阵方程转化为向量关系
将 $P=(\alpha_{1},\alpha_{2},\alpha_{3})$ 代入上式：
$$
A(\alpha_{1},\alpha_{2},\alpha_{3}) = (\alpha_{1},\alpha_{2},\alpha_{3}) \begin{pmatrix}0&0&0\\ 0&1&0\\ 0&0&2\end{pmatrix}
$$

利用分块矩阵乘法规则（列向量视角）：
-   **左边**：$(A\alpha_{1}, A\alpha_{2}, A\alpha_{3})$
-   **右边**：
    -   第 1 列：$0 \cdot \alpha_{1} + 0 \cdot \alpha_{2} + 0 \cdot \alpha_{3} = \vec{0}$
    -   第 2 列：$0 \cdot \alpha_{1} + 1 \cdot \alpha_{2} + 0 \cdot \alpha_{3} = \alpha_{2}$
    -   第 3 列：$0 \cdot \alpha_{1} + 0 \cdot \alpha_{2} + 2 \cdot \alpha_{3} = 2\alpha_{3}$
    即 $(\vec{0}, \alpha_{2}, 2\alpha_{3})$。

对应列相等，可得特征值关系：
$$
\begin{cases}
A\alpha_{1} = 0 \\
A\alpha_{2} = \alpha_{2} \\
A\alpha_{3} = 2\alpha_{3}
\end{cases}
$$
这说明 $\alpha_1, \alpha_2, \alpha_3$ 分别是对应于特征值 $0, 1, 2$ 的特征向量。

---

## 第二部分：计算目标向量

利用矩阵乘法的线性性质（分配律）：
$$
\begin{aligned}
A(\alpha_{1}+\alpha_{2}+\alpha_{3}) &= A\alpha_{1} + A\alpha_{2} + A\alpha_{3} \\
&= 0 + \alpha_{2} + 2\alpha_{3} \\
&= \alpha_{2} + 2\alpha_{3}
\end{aligned}
$$

---

## 第三部分：选项比对

- (A) $\alpha_{1}+\alpha_{2}$
- (B) $\alpha_{2}+2\alpha_{3}$  ✅
- (C) $\alpha_{2}+\alpha_{3}$
- (D) $\alpha_{1}+2\alpha_{2}$

---

## ✅ 最终答案

**(B)**

---

## 📌 关键要点总结

1.  **相似对角化的本质**：等式 $P^{-1}AP = \text{diag}(\lambda_1, \lambda_2, \lambda_3)$ 等价于 $AP = P \text{diag}(\lambda_1, \lambda_2, \lambda_3)$，其列向量关系即为特征值定义 $A\alpha_i = \lambda_i \alpha_i$。
2.  **线性性质**：矩阵作用于向量的和等于矩阵作用于各向量结果的和，即 $A(u+v) = Au + Av$。