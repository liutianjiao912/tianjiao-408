## 🎯 问题回顾

设 $D$ 是第一象限中由曲线 $2xy=1, 4xy=1$ 与直线 $y=x, y=\sqrt{3}x$ 围成的平面区域，函数 $f(x,y)$ 在 $D$ 上连续，则 $\iint_{D}f(x,y)dxdy$ 等于：

- (A) $\displaystyle \int_{\frac{\pi}{4}}^{\frac{\pi}{3}}d\theta\int_{\frac{1}{2\sin 2\theta}}^{\frac{1}{\sin 2\theta}}f(r\cos\theta,r\sin\theta)rdr$
- (B) $\displaystyle \int_{\frac{\pi}{4}}^{\frac{\pi}{3}}d\theta\int_{\frac{1}{\sqrt{2\sin 2\theta}}}^{\frac{1}{\sqrt{\sin 2\theta}}}f(r\cos\theta,r\sin\theta)rdr$
- (C) $\displaystyle \int_{\frac{\pi}{4}}^{\frac{\pi}{3}}d\theta\int_{\frac{1}{2\sin 2\theta}}^{\frac{1}{\sin 2\theta}}f(r\cos\theta,r\sin\theta)dr$
- (D) $\displaystyle \int_{\frac{\pi}{4}}^{\frac{\pi}{3}}d\theta\int_{\frac{1}{\sqrt{2\sin 2\theta}}}^{\frac{1}{\sqrt{\sin 2\theta}}}f(r\cos\theta,r\sin\theta)dr$

---

## 第一部分：坐标系选择与角度范围

观察被积区域 $D$ 的边界条件和选项形式，明显需要将直角坐标 $(x, y)$ 转换为 **极坐标 $(r, \theta)$**。

### 1.1 极坐标变换公式
$$
\begin{cases}
x = r\cos\theta \\
y = r\sin\theta
\end{cases}
$$

### 1.2 确定角度 $\theta$ 的范围

区域 $D$ 被两条过原点的直线截断，这两条直线决定了极角的上下限。

1.  **直线 $y=x$**：
    $$
    \tan\theta = \frac{y}{x} = 1 \implies \theta = \frac{\pi}{4}
    $$
2.  **直线 $y=\sqrt{3}x$**：
    $$
    \tan\theta = \frac{y}{x} = \sqrt{3} \implies \theta = \frac{\pi}{3}
    $$

因此，$\theta$ 的积分区间为：
$$
\theta \in \left[\frac{\pi}{4}, \frac{\pi}{3}\right]
$$

> 💡 **检查选项**：所有选项 (A)(B)(C)(D) 的角度积分限均为 $\frac{\pi}{4}$ 到 $\frac{\pi}{3}$，此步无法排除选项，但确认了积分顺序。

---

## 第二部分：确定径向 $r$ 的范围

区域 $D$ 在径向方向上由两条双曲线边界限定。我们需要将双曲线方程 $xy = k$ 转化为极坐标形式。

### 2.1 坐标代换推导
$$
xy = (r\cos\theta)(r\sin\theta) = r^2 \cos\theta \sin\theta
$$
利用倍角公式 $\sin 2\theta = 2\sin\theta\cos\theta$，即 $\sin\theta\cos\theta = \frac{1}{2}\sin 2\theta$，得：
$$
xy = \frac{1}{2}r^2 \sin 2\theta
$$

### 2.2 计算 $r$ 的上下限

1.  **内边界（下限）：曲线 $4xy = 1$**
    即 $xy = \frac{1}{4}$。代入极坐标方程：
    $$
    \frac{1}{2}r^2 \sin 2\theta = \frac{1}{4}
    $$
    解出 $r$：
    $$
    r^2 = \frac{1}{2\sin 2\theta} \implies r = \frac{1}{\sqrt{2\sin 2\theta}}
    $$

2.  **外边界（上限）：曲线 $2xy = 1$**
    即 $xy = \frac{1}{2}$。代入极坐标方程：
    $$
    \frac{1}{2}r^2 \sin 2\theta = \frac{1}{2}
    $$
    解出 $r$：
    $$
    r^2 = \frac{1}{\sin 2\theta} \implies r = \frac{1}{\sqrt{\sin 2\theta}}
    $$

因此，$r$ 的积分区间为：
$$
r \in \left[\frac{1}{\sqrt{2\sin 2\theta}}, \frac{1}{\sqrt{\sin 2\theta}}\right]
$$

> ⚠️ **注意平方根**：解 $r^2$ 时必须开根号。这一步可以排除选项 (A) 和 (C)，因为它们缺少根号。

---

## 第三部分：面积元素与被积函数

### 3.1 雅可比行列式（Jacobian）

在二重积分的变量代换中，面积元素 $dxdy$ 需要乘以雅可比行列式的绝对值 $|J|$。
对于极坐标变换，雅可比因子为 $r$：
$$
dxdy = r \, dr \, d\theta
$$

### 3.2 完整积分表达式

将被积函数 $f(x,y)$ 变换为 $f(r\cos\theta, r\sin\theta)$，并结合上述所有部分：

$$
\iint_{D}f(x,y)dxdy = \int_{\frac{\pi}{4}}^{\frac{\pi}{3}} d\theta \int_{\frac{1}{\sqrt{2\sin 2\theta}}}^{\frac{1}{\sqrt{\sin 2\theta}}} f(r\cos\theta, r\sin\theta) \cdot \mathbf{r} \, dr
$$

---

## 第四部分：选项分析与最终结论

| 选项 | 内容 | 是否正确 | 理由 |
|------|------|----------|------|
| (A) | $\displaystyle \int \dots \int_{\frac{1}{2\sin 2\theta}}^{\frac{1}{\sin 2\theta}} \dots r dr$ | ❌ | $r$ 上下限未开根号（漏掉 $\sqrt{}$） |
| (B) | $\displaystyle \int \dots \int_{\frac{1}{\sqrt{2\sin 2\theta}}}^{\frac{1}{\sqrt{\sin 2\theta}}} \dots r dr$ | ✅ | 角度、径向限（含根号）、雅可比因子 $r$ 均正确 |
| (C) | $\displaystyle \int \dots \int_{\frac{1}{2\sin 2\theta}}^{\frac{1}{\sin 2\theta}} \dots dr$ | ❌ | $r$ 上下限未开根号，且漏掉雅可比因子 $r$ |
| (D) | $\displaystyle \int \dots \int_{\frac{1}{\sqrt{2\sin 2\theta}}}^{\frac{1}{\sqrt{\sin 2\theta}}} \dots dr$ | ❌ | 漏掉雅可比因子 $r$（应为 $r dr$，只有 $dr$） |

---

## ✅ 最终答案：**(B)**

---

## 📌 关键要点总结

1.  **极坐标变换必乘 $r$**：这是最常见的陷阱，二重积分 $dxdy \to r dr d\theta$，必须检查被积式中是否有 $r$。
2.  **解 $r$ 时别忘开根号**：双曲线 $xy=c$ 对应 $r^2 \propto c$，因此 $r \propto \sqrt{c}$。
3.  **倍角公式的应用**：处理 $xy$ 项时，使用 $xy = \frac{1}{2}r^2\sin 2\theta$ 能极大简化计算。
4.  **积分次序**：由于 $r$ 的边界依赖于 $\theta$（非圆弧边界），必须先积 $r$，后积 $\theta$（即 $d\theta$ 在外层）。