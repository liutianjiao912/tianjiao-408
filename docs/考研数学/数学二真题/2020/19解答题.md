这是一道典型的二重积分计算题。由于被积函数中含有 $\sqrt{x^2+y^2}$ 项，且积分区域由直线构成，本题既可以用**直角坐标**直接计算，也可以转换为**极坐标**计算。

考虑到被积函数的几何意义和形式，使用**极坐标变换**虽然在边界处理上稍显复杂，但能极大简化被积函数的结构。不过，对于这种梯形区域，直角坐标下的计算逻辑往往更直观（尽管积分过程略繁琐）。

为了展示最通用且严谨的数学解题能力，我们将采用**极坐标法**进行详细解析，因为这是处理 $\sqrt{x^2+y^2}$ 的标准高级技巧，同时也会涉及 $\int \sec^3 \theta d\theta$ 这一经典高阶积分的计算。

---

## 🎯 问题回顾

**计算二重积分：**
$$
I = \iint_{D}\frac{\sqrt{x^{2}+y^{2}}}{x}dxdy
$$

**积分区域 $D$：**
由直线 $x=1, x=2, y=x$ 及 $x$ 轴（$y=0$）围成。

---

## 第一部分：区域分析与坐标变换



### 1.1 确定积分区域 $D$

在直角坐标系下，区域 $D$ 可以表示为：
$$
D = \{ (x, y) \mid 1 \le x \le 2, \quad 0 \le y \le x \}
$$
这是一个位于第一象限的直角梯形区域。

### 1.2 极坐标变换策略

令：
$$
\begin{cases}
x = r \cos \theta \\
y = r \sin \theta
\end{cases}
$$
面积元素（雅可比行列式）：
$$
dx dy = r dr d\theta
$$

### 1.3 转化边界条件

我们需要将 $x, y$ 的边界转化为 $r, \theta$ 的边界：

1.  **角度 $\theta$ 的范围**：
    * 下边界 $y=0$ 对应 $\theta = 0$。
    * 上边界 $y=x$ 对应 $\tan \theta = 1$，即 $\theta = \frac{\pi}{4}$。
    * 所以：$0 \le \theta \le \frac{\pi}{4}$。

2.  **半径 $r$ 的范围**（这是本题难点）：
    * 左边界 $x=1 \implies r\cos\theta = 1 \implies r = \sec\theta$。
    * 右边界 $x=2 \implies r\cos\theta = 2 \implies r = 2\sec\theta$。
    * 所以，对于任意固定的 $\theta$，射线从 $x=1$ 穿入，从 $x=2$ 穿出。
    * 即：$\sec\theta \le r \le 2\sec\theta$。

### 1.4 转化被积函数

$$
f(x, y) = \frac{\sqrt{x^2+y^2}}{x} = \frac{r}{r\cos\theta} = \frac{1}{\cos\theta} = \sec\theta
$$

---

## 第二部分：建立并计算积分

### 2.1 写出极坐标下的累次积分

$$
I = \int_{0}^{\frac{\pi}{4}} d\theta \int_{\sec\theta}^{2\sec\theta} (\sec\theta) \cdot r \, dr
$$

> ⚠️ **注意**：不要漏掉面积元素中的 $r$。现在的被积函数实际上是 $\sec\theta \cdot r$。

### 2.2 计算内层积分（关于 $r$）

$$
\begin{aligned}
\int_{\sec\theta}^{2\sec\theta} r \sec\theta \, dr &= \sec\theta \cdot \left[ \frac{1}{2}r^2 \right]_{\sec\theta}^{2\sec\theta} \\
&= \frac{1}{2} \sec\theta \cdot \left( (2\sec\theta)^2 - (\sec\theta)^2 \right) \\
&= \frac{1}{2} \sec\theta \cdot (4\sec^2\theta - \sec^2\theta) \\
&= \frac{1}{2} \sec\theta \cdot (3\sec^2\theta) \\
&= \frac{3}{2} \sec^3\theta
\end{aligned}
$$

此时，原积分转化为：
$$
I = \frac{3}{2} \int_{0}^{\frac{\pi}{4}} \sec^3\theta \, d\theta
$$

---

## 第三部分：计算 $\int \sec^3\theta \, d\theta$

这是微积分中的一个经典难点积分，通常使用**分部积分法**求解。

### 3.1 分部积分推导

设 $J = \int \sec^3\theta \, d\theta = \int \sec\theta \cdot \sec^2\theta \, d\theta$。
令：
$$
u = \sec\theta \implies du = \sec\theta\tan\theta \, d\theta
$$
$$
dv = \sec^2\theta \, d\theta \implies v = \tan\theta
$$

根据分部积分公式 $\int u dv = uv - \int v du$：
$$
\begin{aligned}
J &= \sec\theta\tan\theta - \int \tan\theta \cdot (\sec\theta\tan\theta) \, d\theta \\
&= \sec\theta\tan\theta - \int \sec\theta \tan^2\theta \, d\theta
\end{aligned}
$$

利用恒等式 $\tan^2\theta = \sec^2\theta - 1$：
$$
\begin{aligned}
J &= \sec\theta\tan\theta - \int \sec\theta (\sec^2\theta - 1) \, d\theta \\
&= \sec\theta\tan\theta - \int \sec^3\theta \, d\theta + \int \sec\theta \, d\theta \\
J &= \sec\theta\tan\theta - J + \ln|\sec\theta + \tan\theta|
\end{aligned}
$$

移项解出 $J$（即 $2J = \dots$）：
$$
J = \frac{1}{2} \left( \sec\theta\tan\theta + \ln|\sec\theta + \tan\theta| \right)
$$

### 3.2 代入上下限计算定积分

回到原题，我们需要计算 $\int_{0}^{\frac{\pi}{4}} \sec^3\theta \, d\theta$：

$$
\begin{aligned}
\int_{0}^{\frac{\pi}{4}} \sec^3\theta \, d\theta &= \left[ \frac{1}{2} (\sec\theta\tan\theta + \ln|\sec\theta + \tan\theta|) \right]_{0}^{\frac{\pi}{4}}
\end{aligned}
$$

**代入上限制 $\frac{\pi}{4}$**：
已知 $\sec\frac{\pi}{4} = \sqrt{2}, \quad \tan\frac{\pi}{4} = 1$。
$$
\text{上值} = \frac{1}{2} (\sqrt{2} \cdot 1 + \ln|\sqrt{2} + 1|) = \frac{1}{2}(\sqrt{2} + \ln(1+\sqrt{2}))
$$

**代入下限 $0$**：
已知 $\sec 0 = 1, \quad \tan 0 = 0$。
$$
\text{下值} = \frac{1}{2} (1 \cdot 0 + \ln|1 + 0|) = 0
$$

因此：
$$
\int_{0}^{\frac{\pi}{4}} \sec^3\theta \, d\theta = \frac{1}{2}(\sqrt{2} + \ln(1+\sqrt{2}))
$$

---

## 第四部分：最终结果

将第三部分的结果代回第二部分末尾的公式：

$$
\begin{aligned}
I &= \frac{3}{2} \int_{0}^{\frac{\pi}{4}} \sec^3\theta \, d\theta \\
&= \frac{3}{2} \cdot \left[ \frac{1}{2}(\sqrt{2} + \ln(1+\sqrt{2})) \right] \\
&= \frac{3}{4} \left( \sqrt{2} + \ln(1+\sqrt{2}) \right)
\end{aligned}
$$

---

## ✅ 最终答案

$$
\iint_{D}\frac{\sqrt{x^{2}+y^{2}}}{x}dxdy = \frac{3}{4}\left[ \sqrt{2} + \ln(\sqrt{2}+1) \right]
$$