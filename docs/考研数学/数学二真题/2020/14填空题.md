这是一道经典的 $4 \times 4$ 行列式计算题。这类题目通常可以通过观察行列式中元素的对称性或互补性，利用**行变换（Row Operations）**来构造“零元素”，从而简化计算。

我们将采用最通用的**行变换化简法**进行详细解析，并在最后提供一种基于分块矩阵的高级视角。

---

## 🎯 问题回顾

计算行列式的值：

$$
D = \begin{vmatrix}
a & 0 & -1 & 1 \\
0 & a & 1 & -1 \\
-1 & 1 & a & 0 \\
1 & -1 & 0 & a
\end{vmatrix}
$$

---

## 第一部分：观察与行变换

### 1.1 观察矩阵结构

仔细观察矩阵的元素分布：
- **第 1 行** $(a, 0, -1, 1)$ 与 **第 2 行** $(0, a, 1, -1)$：
    - 后两列的元素互为相反数（$-1$ 对 $1$，$1$ 对 $-1$）。
- **第 3 行** $(-1, 1, a, 0)$ 与 **第 4 行** $(1, -1, 0, a)$：
    - 前两列的元素互为相反数（$-1$ 对 $1$，$1$ 对 $-1$）。

### 1.2 执行行变换构造“零块”

利用上述观察，我们可以通过行加法消去部分元素。

**步骤 1：将第 2 行加到第 1 行 ($r_1 + r_2$)**

$$
\begin{aligned}
r_1' &= r_1 + r_2 \\
&= (a, 0, -1, 1) + (0, a, 1, -1) \\
&= (a+0, 0+a, -1+1, 1-1) \\
&= (a, a, 0, 0)
\end{aligned}
$$

**步骤 2：将第 3 行加到第 4 行 ($r_4 + r_3$)**

$$
\begin{aligned}
r_4' &= r_4 + r_3 \\
&= (1, -1, 0, a) + (-1, 1, a, 0) \\
&= (1-1, -1+1, 0+a, a+0) \\
&= (0, 0, a, a)
\end{aligned}
$$

**步骤 3：写出变换后的行列式**

$$
D = \begin{vmatrix}
a & a & 0 & 0 \\
0 & a & 1 & -1 \\
-1 & 1 & a & 0 \\
0 & 0 & a & a
\end{vmatrix}
$$

此时行列式结构大大简化，第一行和第四行出现了连续的两个零。

---

## 第二部分：降阶计算

为了进一步简化，我们可以提取公因子，然后利用拉普拉斯展开（Laplace Expansion）。

### 2.1 提取公因子

- 从 **第 1 行** 提取公因子 $a$。
- 从 **第 4 行** 提取公因子 $a$。

$$
D = a \cdot a \cdot \begin{vmatrix}
1 & 1 & 0 & 0 \\
0 & a & 1 & -1 \\
-1 & 1 & a & 0 \\
0 & 0 & 1 & 1
\end{vmatrix}
= a^2 \begin{vmatrix}
1 & 1 & 0 & 0 \\
0 & a & 1 & -1 \\
-1 & 1 & a & 0 \\
0 & 0 & 1 & 1
\end{vmatrix}
$$

### 2.2 按第一行展开

我们按第 1 行展开行列式。第 1 行只有前两个元素非零。

$$
\det(\text{Inner}) = 1 \cdot (-1)^{1+1} M_{11} + 1 \cdot (-1)^{1+2} M_{12}
$$
即：
$$
\det(\text{Inner}) = M_{11} - M_{12}
$$

其中 $M_{ij}$ 是余子式。

**计算 $M_{11}$：**
去掉第 1 行、第 1 列：
$$
M_{11} = \begin{vmatrix}
a & 1 & -1 \\
1 & a & 0 \\
0 & 1 & 1
\end{vmatrix}
$$
按第 3 行展开：
$$
\begin{aligned}
M_{11} &= -0 \cdot (\dots) + 1 \cdot (-1)^{3+2} \begin{vmatrix} a & -1 \\ 1 & 0 \end{vmatrix} + 1 \cdot (-1)^{3+3} \begin{vmatrix} a & 1 \\ 1 & a \end{vmatrix} \\
&= -1(0 - (-1)) + 1(a^2 - 1) \\
&= -1(1) + a^2 - 1 \\
&= a^2 - 2
\end{aligned}
$$

**计算 $M_{12}$：**
去掉第 1 行、第 2 列：
$$
M_{12} = \begin{vmatrix}
0 & 1 & -1 \\
-1 & a & 0 \\
0 & 1 & 1
\end{vmatrix}
$$
按第 1 列展开：
$$
\begin{aligned}
M_{12} &= 0 \cdot (\dots) + (-1) \cdot (-1)^{2+1} \begin{vmatrix} 1 & -1 \\ 1 & 1 \end{vmatrix} + 0 \cdot (\dots) \\
&= (-1)(-1) (1 - (-1)) \\
&= 1 \cdot 2 \\
&= 2
\end{aligned}
$$

### 2.3 汇总结果

将余子式的值代回：

$$
\det(\text{Inner}) = M_{11} - M_{12} = (a^2 - 2) - 2 = a^2 - 4
$$

最后乘以前面提取的系数 $a^2$：

$$
D = a^2 (a^2 - 4)
$$

或者写成因式分解形式：
$$
D = a^2 (a-2)(a+2)
$$

---

## 🧠 另解：分块矩阵法（秒杀法）



若你能识别出矩阵的分块结构，可以利用公式快速求解。

### 3.1 识别分块结构

原矩阵可以写成：
$$
D = \begin{pmatrix} A & B \\ B^T & A \end{pmatrix}
$$
其中：
- $A = \begin{pmatrix} a & 0 \\ 0 & a \end{pmatrix} = aI_2$ （数量矩阵）
- $B = \begin{pmatrix} -1 & 1 \\ 1 & -1 \end{pmatrix}$ （注意：此处左下角实际上是 $B$，因为 $B$ 是对称矩阵 $B^T=B$）

实际上，本题矩阵形状更为特殊，属于 $\begin{pmatrix} A & B \\ B & A \end{pmatrix}$ 类型，其中 $A$ 与 $B$ 可交换（因为 $A=aI$ 是数量矩阵，与任何矩阵可交换）。

### 3.2 套用公式

对于分块矩阵 $\begin{pmatrix} A & B \\ B & A \end{pmatrix}$，若 $AB=BA$，则行列式为：
$$
D = \det(A+B) \cdot \det(A-B)
$$

**计算 $A+B$：**
$$
A+B = \begin{pmatrix} a & 0 \\ 0 & a \end{pmatrix} + \begin{pmatrix} -1 & 1 \\ 1 & -1 \end{pmatrix} = \begin{pmatrix} a-1 & 1 \\ 1 & a-1 \end{pmatrix}
$$
$$
\det(A+B) = (a-1)^2 - 1 = a^2 - 2a
$$

**计算 $A-B$：**
$$
A-B = \begin{pmatrix} a & 0 \\ 0 & a \end{pmatrix} - \begin{pmatrix} -1 & 1 \\ 1 & -1 \end{pmatrix} = \begin{pmatrix} a+1 & -1 \\ -1 & a+1 \end{pmatrix}
$$
$$
\det(A-B) = (a+1)^2 - (-1)^2 = (a+1)^2 - 1 = a^2 + 2a
$$

**最终结果：**
$$
D = (a^2 - 2a)(a^2 + 2a) = a(a-2) \cdot a(a+2) = a^2(a^2 - 4)
$$

此方法与第一部分结果完全一致。

---

## ✅ 最终答案

$$
a^2(a^2 - 4) \quad \text{或} \quad a^4 - 4a^2
$$