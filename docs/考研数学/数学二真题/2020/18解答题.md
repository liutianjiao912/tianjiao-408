这是一道包含**解函数方程**与**定积分应用（旋转体体积）**的综合题。

我们将分两步进行解析：首先利用代换消元法求出 $f(x)$ 的解析式，然后利用“柱壳法”计算旋转体的体积。

---

## 🎯 问题回顾

**已知**：
1.  定义域：$(0, +\infty)$
2.  函数方程：$2f(x) + x^2 f\left(\frac{1}{x}\right) = \frac{x^2+2x}{\sqrt{1+x^2}}$

**求解**：
1.  求 $f(x)$ 的表达式。
2.  求由曲线 $y=f(x)$，直线 $y=\frac{1}{2}$，直线 $y=\frac{\sqrt{3}}{2}$ 及 $y$ 轴围成的图形，绕 $x$ 轴旋转所得旋转体的体积 $V$。

---

## 第一部分：求 $f(x)$ 的解析式

这是一个典型的含有 $f(x)$ 和 $f\left(\frac{1}{x}\right)$ 的函数方程。解决此类问题的通用方法是利用倒数代换构造方程组。

### 1.1 构造方程组

**方程 ①**（原方程）：
$$
2f(x) + x^2 f\left(\frac{1}{x}\right) = \frac{x^2+2x}{\sqrt{1+x^2}} \quad \cdots ①
$$

**方程 ②**（进行代换）：
将方程 ① 中的 $x$ 替换为 $\frac{1}{x}$（因为 $x \in (0, +\infty)$，所以 $\frac{1}{x}$ 也在定义域内）：

$$
2f\left(\frac{1}{x}\right) + \left(\frac{1}{x}\right)^2 f(x) = \frac{(\frac{1}{x})^2 + 2(\frac{1}{x})}{\sqrt{1+(\frac{1}{x})^2}}
$$

化简方程 ② 的右边：
$$
\text{右边} = \frac{\frac{1+2x}{x^2}}{\sqrt{\frac{x^2+1}{x^2}}} = \frac{\frac{1+2x}{x^2}}{\frac{\sqrt{x^2+1}}{x}} = \frac{1+2x}{x^2} \cdot \frac{x}{\sqrt{x^2+1}} = \frac{2x+1}{x\sqrt{1+x^2}}
$$

整理得到方程 ②：
$$
\frac{1}{x^2}f(x) + 2f\left(\frac{1}{x}\right) = \frac{2x+1}{x\sqrt{1+x^2}} \quad \cdots ②
$$

### 1.2 消去 $f\left(\frac{1}{x}\right)$

我们的目标是解出 $f(x)$，因此需要消去 $f\left(\frac{1}{x}\right)$。
观察系数：方程 ① 中 $f\left(\frac{1}{x}\right)$ 的系数是 $x^2$，方程 ② 中是 $2$。

我们可以将 **方程 ② 乘以 $\frac{x^2}{2}$**，使其 $f\left(\frac{1}{x}\right)$ 的系数变为 $x^2$：

$$
\frac{x^2}{2} \cdot \left[ \frac{1}{x^2}f(x) + 2f\left(\frac{1}{x}\right) \right] = \frac{x^2}{2} \cdot \frac{2x+1}{x\sqrt{1+x^2}}
$$

$$
\frac{1}{2}f(x) + x^2 f\left(\frac{1}{x}\right) = \frac{x(2x+1)}{2\sqrt{1+x^2}} = \frac{2x^2+x}{2\sqrt{1+x^2}} \quad \cdots ③
$$

### 1.3 求解 $f(x)$

用 **方程 ① 减去 方程 ③**：

$$
\left[ 2f(x) + x^2 f\left(\frac{1}{x}\right) \right] - \left[ \frac{1}{2}f(x) + x^2 f\left(\frac{1}{x}\right) \right] = \frac{x^2+2x}{\sqrt{1+x^2}} - \frac{2x^2+x}{2\sqrt{1+x^2}}
$$

左边化简：
$$
\frac{3}{2}f(x)
$$

右边通分化简：
$$
\frac{2(x^2+2x) - (2x^2+x)}{2\sqrt{1+x^2}} = \frac{2x^2+4x-2x^2-x}{2\sqrt{1+x^2}} = \frac{3x}{2\sqrt{1+x^2}}
$$

即：
$$
\frac{3}{2}f(x) = \frac{3x}{2\sqrt{1+x^2}}
$$

两边消去系数 $\frac{3}{2}$，得：
$$
f(x) = \frac{x}{\sqrt{1+x^2}}
$$

---

## 第二部分：求旋转体体积



### 2.1 分析围成图形的区域

由题意，图形由以下曲线围成：
1.  曲线 $y = f(x) = \frac{x}{\sqrt{1+x^2}}$
2.  直线 $y = \frac{1}{2}$
3.  直线 $y = \frac{\sqrt{3}}{2}$
4.  $y$ 轴（即 $x=0$）

由于 $f(x) = \frac{x}{\sqrt{1+x^2}}$ 在 $x>0$ 时单调递增，且值域为 $(0, 1)$。
我们需要求出直线 $y=\frac{1}{2}$ 和 $y=\frac{\sqrt{3}}{2}$ 对应的 $x$ 值（积分限）。

由 $y = \frac{x}{\sqrt{1+x^2}}$ 反解 $x$：
$$
y^2 = \frac{x^2}{1+x^2} \implies y^2 + x^2 y^2 = x^2 \implies x^2 (1-y^2) = y^2 \implies x = \frac{y}{\sqrt{1-y^2}}
$$

确定积分的 $y$ 上下限对应的 $x$ 无需计算，直接使用 $y$ 作为积分变量更为方便。
积分区间为 $y \in [\frac{1}{2}, \frac{\sqrt{3}}{2}]$。

### 2.2 选择积分方法：柱壳法（Cylindrical Shells）

我们要求的是**绕 $x$ 轴旋转**的体积。
观察图形，区域是紧贴 $y$ 轴，被上下两条水平线截断的条状区域。

* **如果用切片法（圆盘法）**：需要对 $x$ 积分，分为两段（矩形段和曲边段），计算较繁琐。
* **如果用柱壳法**：我们取一个水平的微元带（宽度为 $dy$），绕 $x$ 轴旋转会形成一个圆柱壳。
    * **半径**：$r = y$
    * **高度（圆柱壳的长度）**：即曲线到 $y$ 轴的水平距离 $x(y) = \frac{y}{\sqrt{1-y^2}}$
    * **微元体积**：$dV = 2\pi y \cdot x(y) \, dy$

体积公式为：
$$
V = \int_{y_1}^{y_2} 2\pi y \cdot x(y) \, dy
$$

### 2.3 计算积分

代入表达式：
$$
V = \int_{\frac{1}{2}}^{\frac{\sqrt{3}}{2}} 2\pi y \cdot \frac{y}{\sqrt{1-y^2}} \, dy = 2\pi \int_{\frac{1}{2}}^{\frac{\sqrt{3}}{2}} \frac{y^2}{\sqrt{1-y^2}} \, dy
$$

这是一个典型的三角代换积分。

**Step 1: 换元**
令 $y = \sin t$，则 $dy = \cos t \, dt$。
变换积分限：
* 当 $y = \frac{1}{2}$ 时，$t = \frac{\pi}{6}$；
* 当 $y = \frac{\sqrt{3}}{2}$ 时，$t = \frac{\pi}{3}$。

**Step 2: 代入化简**
$$
\frac{y^2}{\sqrt{1-y^2}} dy = \frac{\sin^2 t}{\sqrt{1-\sin^2 t}} \cdot \cos t \, dt = \frac{\sin^2 t}{\cos t} \cdot \cos t \, dt = \sin^2 t \, dt
$$
(注：在 $[\frac{\pi}{6}, \frac{\pi}{3}]$ 上 $\cos t > 0$)

于是体积 $V$ 变为：
$$
V = 2\pi \int_{\frac{\pi}{6}}^{\frac{\pi}{3}} \sin^2 t \, dt
$$

**Step 3: 降幂积分**
利用公式 $\sin^2 t = \frac{1 - \cos 2t}{2}$：
$$
\begin{aligned}
V &= 2\pi \int_{\frac{\pi}{6}}^{\frac{\pi}{3}} \frac{1 - \cos 2t}{2} \, dt \\
&= \pi \int_{\frac{\pi}{6}}^{\frac{\pi}{3}} (1 - \cos 2t) \, dt \\
&= \pi \left[ t - \frac{1}{2}\sin 2t \right]_{\frac{\pi}{6}}^{\frac{\pi}{3}}
\end{aligned}
$$

**Step 4: 代入上下限计算**
$$
\begin{aligned}
V &= \pi \left\{ \left( \frac{\pi}{3} - \frac{1}{2}\sin\frac{2\pi}{3} \right) - \left( \frac{\pi}{6} - \frac{1}{2}\sin\frac{\pi}{3} \right) \right\} \\
&= \pi \left\{ \left( \frac{\pi}{3} - \frac{1}{2} \cdot \frac{\sqrt{3}}{2} \right) - \left( \frac{\pi}{6} - \frac{1}{2} \cdot \frac{\sqrt{3}}{2} \right) \right\} \\
&= \pi \left\{ \frac{\pi}{3} - \frac{\sqrt{3}}{4} - \frac{\pi}{6} + \frac{\sqrt{3}}{4} \right\} \\
&= \pi \left( \frac{\pi}{3} - \frac{\pi}{6} \right) \\
&= \pi \cdot \frac{\pi}{6} \\
&= \frac{\pi^2}{6}
\end{aligned}
$$

---

## ✅ 最终答案

1.  函数表达式为：
    $$
    f(x) = \frac{x}{\sqrt{1+x^2}}
    $$

2.  旋转体的体积为：
    $$
    V = \frac{\pi^2}{6}
    $$