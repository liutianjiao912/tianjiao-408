这是对该选择题的详细解析。我们将严格按照多元微积分中偏导数、混合偏导数、重极限（联合极限）和累次极限的定义，逐一分析这四个结论的真伪。

---

## 🎯 题目回顾

**题目**：
关于函数 $f(x,y)=\begin{cases}xy,&xy\ne0,\\ x,&y=0,\\ y,&x=0,\end{cases}$ 给出下列结论：
   ① $\frac{\partial f}{\partial x}|_{(0,0)}=1$;
   ② $\frac{\partial^{2}f}{\partial x\partial y}|_{(0,0)}=1$;
   ③ $\lim_{(x,y)\rightarrow(0,0)}f(x,y)=0$;
   ④ $\lim_{y\rightarrow0}\lim_{x\rightarrow0}f(x,y)=0$.

其中正确的个数为 ( )
   (A) 4
   (B) 3
   (C) 2
   (D) 1

---

## 🔍 解题思路与详细步骤

### 1. 分析结论 ①：$\frac{\partial f}{\partial x}|_{(0,0)}=1$

**定义回顾**：
偏导数 $\frac{\partial f}{\partial x}(0,0)$ 的定义为：
$$
\frac{\partial f}{\partial x}(0,0) = \lim_{x \to 0} \frac{f(x,0) - f(0,0)}{x - 0}
$$

**计算过程**：
1.  **求 $f(0,0)$**：
    根据题目定义，当 $y=0$ 时 $f(x,0)=x$，故 $f(0,0)=0$。（或者由 $x=0$ 时 $f(0,y)=y$ 也能得到 $f(0,0)=0$）。
2.  **求 $f(x,0)$**：
    根据题目中当 $y=0$ 时的定义，$f(x,0) = x$。
3.  **代入极限计算**：
    $$
    \lim_{x \to 0} \frac{x - 0}{x} = \lim_{x \to 0} 1 = 1
    $$

**结论**：
$\frac{\partial f}{\partial x}|_{(0,0)}=1$。
**结论 ① 正确**。

---

### 2. 分析结论 ②：$\frac{\partial^{2}f}{\partial x\partial y}|_{(0,0)}=1$

**定义回顾**：
混合偏导数 $\frac{\partial^{2}f}{\partial x\partial y}(0,0)$ 表示先对 $y$ 求偏导，再对 $x$ 求偏导：
$$
\frac{\partial^{2}f}{\partial x\partial y}(0,0) = \frac{\partial}{\partial x}\left(\frac{\partial f}{\partial y}\right)\Bigg|_{(0,0)} = \lim_{x \to 0} \frac{\frac{\partial f}{\partial y}(x,0) - \frac{\partial f}{\partial y}(0,0)}{x}
$$

**计算过程**：
我们需要计算 $\frac{\partial f}{\partial y}(0,0)$ 和 $\frac{\partial f}{\partial y}(x,0)$（其中 $x \ne 0$）。

1.  **计算 $\frac{\partial f}{\partial y}(0,0)$**：
    $$
    \frac{\partial f}{\partial y}(0,0) = \lim_{y \to 0} \frac{f(0,y) - f(0,0)}{y}
    $$
    根据定义，当 $x=0$ 时 $f(0,y)=y$，故：
    $$
    \lim_{y \to 0} \frac{y - 0}{y} = 1
    $$
    所以 $\frac{\partial f}{\partial y}(0,0) = 1$。

2.  **计算 $\frac{\partial f}{\partial y}(x,0)$ （$x \ne 0$）**：
    $$
    \frac{\partial f}{\partial y}(x,0) = \lim_{y \to 0} \frac{f(x,y) - f(x,0)}{y}
    $$
    * 当 $y \to 0$ (且 $y \ne 0$) 时，且已知 $x \ne 0$，此时 $xy \ne 0$，根据定义 $f(x,y) = xy$。
    * 根据定义，当 $y=0$ 时，$f(x,0) = x$。
    代入极限：
    $$
    \lim_{y \to 0} \frac{xy - x}{y} = \lim_{y \to 0} x \cdot \frac{y-1}{y}
    $$
    由于 $x$ 是非零常数，当 $y \to 0$ 时，$\frac{y-1}{y} \to \infty$，故该极限**不存在**。

**结论**：
由于函数在 $x$ 轴上非原点处对 $y$ 的偏导数 $\frac{\partial f}{\partial y}(x,0)$ 不存在，因此无法计算其关于 $x$ 的进一步导数。
**结论 ② 错误**。

---

### 3. 分析结论 ③：$\lim_{(x,y)\rightarrow(0,0)}f(x,y)=0$

**思路**：
检查当动点 $(x,y)$ 以任意方式趋于 $(0,0)$ 时，函数值是否都趋于 0。

**分析**：
我们将函数分为三个区域来看：
1.  **在 x 轴上 ($y=0$)**：
    $f(x,0) = x$。当 $x \to 0$ 时，极限为 0。
2.  **在 y 轴上 ($x=0$)**：
    $f(0,y) = y$。当 $y \to 0$ 时，极限为 0。
3.  **在坐标轴之外 ($xy \ne 0$)**：
    $f(x,y) = xy$。当 $(x,y) \to (0,0)$ 时，乘积 $xy \to 0$。

**严格证明**：
对于任意给定的 $\epsilon > 0$，取 $\delta = \min(\epsilon, \sqrt{\epsilon})$。
当 $0 < \sqrt{x^2+y^2} < \delta$ 时：
* 若 $y=0$，则 $|f(x,y)| = |x| \le \sqrt{x^2+y^2} < \delta \le \epsilon$。
* 若 $x=0$，则 $|f(x,y)| = |y| \le \sqrt{x^2+y^2} < \delta \le \epsilon$。
* 若 $xy \ne 0$，则 $|f(x,y)| = |xy| \le \frac{x^2+y^2}{2} < \frac{\delta^2}{2} \le \frac{\epsilon}{2} < \epsilon$。
综上，总有 $|f(x,y) - 0| < \epsilon$。

**结论**：
重极限存在且为 0。
**结论 ③ 正确**。

---

### 4. 分析结论 ④：$\lim_{y\rightarrow0}\lim_{x\rightarrow0}f(x,y)=0$

**定义回顾**：
累次极限（先求内层，再求外层）。
先计算 $g(y) = \lim_{x \to 0} f(x,y)$，再计算 $\lim_{y \to 0} g(y)$。

**计算过程**：
1.  **计算内层极限 $\lim_{x \to 0} f(x,y)$**：
    此处视 $y$ 为固定值。
    * **情形 1：$y \ne 0$**。
        当 $x \to 0$ (且 $x \ne 0$) 时，$xy \ne 0$，故 $f(x,y) = xy$。
        $\lim_{x \to 0} xy = 0$。
    * **情形 2：$y = 0$**。
        此时函数在 x 轴上，根据定义 $f(x,0) = x$。
        $\lim_{x \to 0} x = 0$。
    综上，对于任意 $y$，都有 $g(y) = \lim_{x \to 0} f(x,y) = 0$。

2.  **计算外层极限 $\lim_{y \to 0} g(y)$**：
    $$
    \lim_{y \to 0} 0 = 0
    $$

**结论**：
累次极限存在且为 0。
**结论 ④ 正确**。

---

## ✅ 最终统计与答案

* 结论 ①：**正确**
* 结论 ②：**错误**
* 结论 ③：**正确**
* 结论 ④：**正确**

正确的结论共有 **3** 个（①、③、④）。

**正确答案是 (B)**