这是一个经典的常微分方程（ODE）与广义积分结合的填空题。为了确保每一个步骤都清晰明了，我们将解题过程分为三个主要部分：求解微分方程、计算广义积分、以及一种更为巧妙的验证方法。

---

## 🎯 问题回顾

**题目**：
设 $y=y(x)$ 满足 $y^{\prime\prime}+2y^{\prime}+y=0$，且 $y(0)=0, y^{\prime}(0)=1$，求 $\displaystyle \int_{0}^{+\infty}y(x)dx$ 的值。

---

## 第一部分：求解二阶常系数齐次线性微分方程

我们需要先求出函数 $y(x)$ 的具体表达式。

### 1.1 写出特征方程

给定微分方程：
$$
y^{\prime\prime} + 2y^{\prime} + y = 0
$$

对应的**特征方程**为：
$$
r^2 + 2r + 1 = 0
$$

### 1.2 求解特征根

利用完全平方公式进行因式分解：
$$
(r + 1)^2 = 0
$$

解得特征根为两个相等的实根（二重根）：
$$
r_1 = r_2 = -1
$$

### 1.3 写出通解结构

当特征根为二重实根 $r$ 时，二阶常系数齐次线性微分方程的通解形式为：
$$
y(x) = (C_1 + C_2 x)e^{rx}
$$

代入 $r = -1$，得通解：
$$
y(x) = (C_1 + C_2 x)e^{-x}
$$

其中 $C_1, C_2$ 为待定常数。

### 1.4 利用初始条件确定常数

**条件 1：利用 $y(0) = 0$**

将 $x=0$ 代入通解公式：
$$
y(0) = (C_1 + C_2 \cdot 0)e^{-0} = C_1 \cdot 1 = C_1
$$
由已知 $y(0)=0$，故解得：
$$
C_1 = 0
$$

此时函数表达式简化为：
$$
y(x) = C_2 x e^{-x}
$$

**条件 2：利用 $y^{\prime}(0) = 1$**

首先对简化后的 $y(x)$ 求导。利用**乘积求导法则** $(uv)' = u'v + uv'$：
设 $u = C_2 x$，$v = e^{-x}$，则：
$$
\begin{aligned}
y^{\prime}(x) &= (C_2 x)^{\prime} \cdot e^{-x} + (C_2 x) \cdot (e^{-x})^{\prime} \\
&= C_2 \cdot e^{-x} + C_2 x \cdot (-e^{-x}) \\
&= C_2 e^{-x} (1 - x)
\end{aligned}
$$

将 $x=0$ 代入导数表达式：
$$
y^{\prime}(0) = C_2 e^{0} (1 - 0) = C_2 \cdot 1 \cdot 1 = C_2
$$
由已知 $y^{\prime}(0)=1$，故解得：
$$
C_2 = 1
$$

### 1.5 得到特解

将 $C_1=0, C_2=1$ 代入通解，得最终函数表达式：
$$
y(x) = x e^{-x}
$$

---

## 第二部分：计算广义积分

现在我们需要计算积分 $I = \displaystyle \int_{0}^{+\infty} x e^{-x} dx$。

### 2.1 积分的定义

$$
I = \lim_{b \to +\infty} \int_{0}^{b} x e^{-x} dx
$$

### 2.2 使用分部积分法（Integration by Parts）

分部积分公式为：$\displaystyle \int u \, dv = uv - \int v \, du$。

在本题中，令：
- $u = x \implies du = dx$
- $dv = e^{-x} dx \implies v = \displaystyle \int e^{-x} dx = -e^{-x}$

代入公式：
$$
\begin{aligned}
\int x e^{-x} dx &= x(-e^{-x}) - \int (-e^{-x}) dx \\
&= -xe^{-x} + \int e^{-x} dx \\
&= -xe^{-x} - e^{-x} \\
&= -e^{-x}(x + 1)
\end{aligned}
$$

### 2.3 代入限并计算极限

将上述原函数代入定积分计算：

$$
\begin{aligned}
\int_{0}^{+\infty} x e^{-x} dx &= \left[ -e^{-x}(x + 1) \right]_{0}^{+\infty} \\
&= \left( \lim_{x \to +\infty} \frac{-(x+1)}{e^x} \right) - \left( -e^{-0}(0 + 1) \right)
\end{aligned}
$$

**步骤 A：计算下限值（$x=0$）**
$$
-e^{-0}(0 + 1) = -1 \cdot 1 = -1
$$

**步骤 B：计算上限极限（$x \to +\infty$）**
我们需要求 $\displaystyle \lim_{x \to +\infty} \frac{-(x+1)}{e^x}$。
这是一个 $\frac{\infty}{\infty}$ 型的不定式，使用**洛必达法则（L'Hôpital's Rule）**：

$$
\lim_{x \to +\infty} \frac{-(x+1)}{e^x} = \lim_{x \to +\infty} \frac{\frac{d}{dx}(-(x+1))}{\frac{d}{dx}(e^x)} = \lim_{x \to +\infty} \frac{-1}{e^x} = 0
$$

**步骤 C：合并结果**
$$
I = 0 - (-1) = 1
$$

---

## 第三部分：另解（利用微分方程性质）

> 💡 **进阶技巧**：如果我们不想进行繁琐的分部积分计算，可以直接利用原微分方程的性质求解。这种方法在填空题中非常高效。

### 3.1 积分方程两边

已知 $y^{\prime\prime} + 2y^{\prime} + y = 0$。
我们将等式两端在 $[0, +\infty)$ 区间上积分：

$$
\int_{0}^{+\infty} (y^{\prime\prime} + 2y^{\prime} + y) dx = \int_{0}^{+\infty} 0 \, dx
$$

利用积分的线性性质拆分：

$$
\int_{0}^{+\infty} y^{\prime\prime} dx + 2\int_{0}^{+\infty} y^{\prime} dx + \int_{0}^{+\infty} y(x) dx = 0
$$

### 3.2 计算各项积分

根据牛顿-莱布尼茨公式 $\int_{a}^{b} f'(x) dx = f(b) - f(a)$：

1.  **第一项**：
    $$
    \int_{0}^{+\infty} y^{\prime\prime} dx = y^{\prime}(x) \Big|_{0}^{+\infty} = y^{\prime}(+\infty) - y^{\prime}(0)
    $$
2.  **第二项**：
    $$
    2\int_{0}^{+\infty} y^{\prime} dx = 2 \left[ y(x) \Big|_{0}^{+\infty} \right] = 2[y(+\infty) - y(0)]
    $$
3.  **第三项**：
    $$
    \int_{0}^{+\infty} y(x) dx \quad (\text{这是我们要的答案，设为 } I)
    $$

### 3.3 确定边界值

根据我们在第一部分求得的特解 $y(x) = x e^{-x}$ 和 $y^{\prime}(x) = e^{-x}(1-x)$：

- 当 $x \to +\infty$ 时：
    - $\displaystyle \lim_{x \to +\infty} y(x) = \lim_{x \to +\infty} \frac{x}{e^x} = 0$ (由洛必达法则或增长速度比较)
    - $\displaystyle \lim_{x \to +\infty} y^{\prime}(x) = \lim_{x \to +\infty} \frac{1-x}{e^x} = 0$
- 已知初始条件：
    - $y(0) = 0$
    - $y^{\prime}(0) = 1$

### 3.4 代入方程求解

将上述边界值代入 3.1 中的方程：

$$
\underbrace{[0 - 1]}_{\text{第一项}} + \underbrace{2[0 - 0]}_{\text{第二项}} + I = 0
$$

$$
-1 + 0 + I = 0
$$

$$
I = 1
$$

此方法无需进行分部积分，直接利用方程结构得出结果，既快又准。

---

## ✅ 最终答案

$$
\int_{0}^{+\infty}y(x)dx = 1
$$