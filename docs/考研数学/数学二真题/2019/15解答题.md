这是对给定解答题的详细解析。我们将严格按照数学分析的标准步骤，先求导函数，再通过导数的符号变化判断单调性，最后求出极值。

---

## 🎯 题目回顾

**题目**：
已知函数
$$
f(x) = \begin{cases}
x^{2x}, & x > 0 \\
xe^{x} + 1, & x \le 0
\end{cases}
$$
求 $f^{\prime}(x)$，并求 $f(x)$ 的极值。

---

## 🔍 解题思路概览

1.  **求分段导数**：分别在 $x > 0$ 和 $x < 0$ 的区间内利用求导法则计算导数。
2.  **考察分界点**：检查函数在 $x = 0$ 处的连续性和可导性，确定 $f'(x)$ 的完整表达式。
3.  **寻找驻点与不可导点**：令 $f'(x) = 0$ 求驻点，结合不可导点确定所有极值怀疑点。
4.  **单调性分析**：利用列表法或符号分析法，确定极值性质（极大值还是极小值）。
5.  **计算极值**：代入函数求出具体数值。

---

## 第一部分：求导函数 $f'(x)$

### 1.1 当 $x < 0$ 时求导

此时函数表达式为 $f(x) = xe^x + 1$。
利用**乘积求导法则** $(uv)' = u'v + uv'$：

$$
f'(x) = (x)' \cdot e^x + x \cdot (e^x)' + (1)'
$$
$$
f'(x) = 1 \cdot e^x + x \cdot e^x + 0
$$
$$
f'(x) = e^x(1+x)
$$

### 1.2 当 $x > 0$ 时求导

此时函数表达式为 $f(x) = x^{2x}$。
这是一个**幂指函数**，需使用**对数求导法**。

1.  两边取对数：
    $$
    \ln [f(x)] = \ln(x^{2x}) = 2x \ln x
    $$
2.  两边对 $x$ 求导：
    $$
    \frac{1}{f(x)} \cdot f'(x) = (2x)' \ln x + 2x (\ln x)'
    $$
    $$
    \frac{f'(x)}{f(x)} = 2 \ln x + 2x \cdot \frac{1}{x}
    $$
    $$
    \frac{f'(x)}{f(x)} = 2 \ln x + 2 = 2(\ln x + 1)
    $$
3.  解出 $f'(x)$：
    $$
    f'(x) = f(x) \cdot 2(\ln x + 1)
    $$
    $$
    f'(x) = 2x^{2x}(\ln x + 1)
    $$

### 1.3 考察分界点 $x = 0$ 的可导性

要判断 $x=0$ 处是否可导，首先需判断其连续性，再计算左右导数。

**步骤 1：检查连续性**
* **函数值**：$f(0) = 0 \cdot e^0 + 1 = 1$。
* **左极限**：
    $$
    \lim_{x \to 0^-} f(x) = \lim_{x \to 0^-} (xe^x + 1) = 0 + 1 = 1
    $$
* **右极限**：
    $$
    \lim_{x \to 0^+} f(x) = \lim_{x \to 0^+} x^{2x}
    $$
    这是 $0^0$ 型极限。令 $y = x^{2x}$，则 $\ln y = 2x \ln x = \frac{2\ln x}{1/x}$。
    由洛必达法则：
    $$
    \lim_{x \to 0^+} \frac{2\ln x}{1/x} = \lim_{x \to 0^+} \frac{2/x}{-1/x^2} = \lim_{x \to 0^+} (-2x) = 0
    $$
    所以 $\lim_{x \to 0^+} \ln y = 0 \implies \lim_{x \to 0^+} y = e^0 = 1$。

由于 $\lim_{x \to 0^-} f(x) = \lim_{x \to 0^+} f(x) = f(0) = 1$，**函数在 $x=0$ 处连续**。

**步骤 2：检查导数定义（左右导数）**
* **左导数** $f'_-(0)$：
    $$
    f'_-(0) = \lim_{x \to 0^-} \frac{f(x) - f(0)}{x - 0} = \lim_{x \to 0^-} \frac{(xe^x + 1) - 1}{x} = \lim_{x \to 0^-} e^x = 1
    $$
    或者直接代入 $x<0$ 的导数公式极限：$\lim_{x \to 0^-} e^x(1+x) = 1 \cdot 1 = 1$。

* **右导数** $f'_+(0)$：
    考察 $x > 0$ 时的导数极限：
    $$
    \lim_{x \to 0^+} f'(x) = \lim_{x \to 0^+} 2x^{2x}(\ln x + 1)
    $$
    我们知道当 $x \to 0^+$ 时，$x^{2x} \to 1$，而 $(\ln x + 1) \to -\infty$。
    因此：
    $$
    \lim_{x \to 0^+} f'(x) = -\infty
    $$
    即右导数不存在。

**结论**：函数在 $x=0$ 处**不可导**。

### 1.4 导函数 $f'(x)$ 总结

$$
f'(x) = \begin{cases}
2x^{2x}(\ln x + 1), & x > 0 \\
e^x(1+x), & x < 0 \\
\text{不存在}, & x = 0
\end{cases}
$$

---

## 第二部分：求函数的极值

我们需要分析导数的符号变化来寻找极值点。
可能的极值点包括：
1.  **驻点**（$f'(x) = 0$ 的点）。
2.  **不可导点**（$x = 0$）。

### 2.1 寻找驻点

1.  **当 $x < 0$ 时**：
    令 $f'(x) = e^x(1+x) = 0$。
    因为 $e^x > 0$ 恒成立，故 $1+x=0 \implies x = -1$。
    这是一点驻点。

2.  **当 $x > 0$ 时**：
    令 $f'(x) = 2x^{2x}(\ln x + 1) = 0$。
    因为指数函数 $x^{2x} > 0$ 恒成立，故 $\ln x + 1 = 0$。
    $$
    \ln x = -1 \implies x = e^{-1} = \frac{1}{e}
    $$
    这是另一点驻点。

### 2.2 列表分析单调性

我们有三个关键点将定义域分为四个区间：$x = -1$，$x = 0$，$x = \frac{1}{e}$。

| 区间/点 | $(-\infty, -1)$ | $x = -1$ | $(-1, 0)$ | $x = 0$ | $(0, \frac{1}{e})$ | $x = \frac{1}{e}$ | $(\frac{1}{e}, +\infty)$ |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| **$f'(x)$ 符号** | **$-$** | **$0$** | **$+$** | **不存在** | **$-$** | **$0$** | **$+$** |
| **单调性** | $\searrow$ (减) | **极小值** | $\nearrow$ (增) | **极大值** | $\searrow$ (减) | **极小值** | $\nearrow$ (增) |

**符号判定细节**：
* $x < -1$（如 $x=-2$）：$e^{-2}(1-2) < 0$。
* $-1 < x < 0$（如 $x=-0.5$）：$e^{-0.5}(1-0.5) > 0$。
* $0 < x < \frac{1}{e}$（如 $x=e^{-2}$）：$2x^{2x}(\ln(e^{-2})+1) = 2x^{2x}(-1) < 0$。
* $x > \frac{1}{e}$（如 $x=e$）：$2x^{2x}(\ln e + 1) = 2x^{2x}(2) > 0$。
* **关于 $x=0$**：函数在左侧递增，右侧递减，且在 $x=0$ 处连续，因此 $x=0$ 是极大值点。

### 2.3 计算极值数值

1.  **极小值点 $x = -1$**：
    $$
    f(-1) = (-1)e^{-1} + 1 = 1 - \frac{1}{e}
    $$

2.  **极大值点 $x = 0$**：
    $$
    f(0) = 0 \cdot e^0 + 1 = 1
    $$

3.  **极小值点 $x = \frac{1}{e}$**：
    $$
    f\left(\frac{1}{e}\right) = \left(e^{-1}\right)^{2e^{-1}} = \left(e^{-1}\right)^{\frac{2}{e}} = e^{-\frac{2}{e}}
    $$

---

## ✅ 最终答案

**1. 导函数**
$$
f'(x) = \begin{cases}
2x^{2x}(\ln x + 1), & x > 0 \\
e^x(1+x), & x < 0
\end{cases}
$$
（注：函数在 $x=0$ 处不可导）

**2. 极值**
* **极小值**：
    * 在 $x = -1$ 处取得，值为 $1 - \frac{1}{e}$。
    * 在 $x = \frac{1}{e}$ 处取得，值为 $e^{-\frac{2}{e}}$。
* **极大值**：
    * 在 $x = 0$ 处取得，值为 $1$。