## 🎯 题目解析

**题目**：
设函数 $f(x,y)$ 可微，且满足以下两个方程：
1.  $f(x+1,e^{x})=x(x+1)^{2}$
2.  $f(x,x^{2})=2x^{2}\ln x$
求 $df(1,1)$ 并选择正确选项。

**选项**：
(A) $dx+dy$
(B) $dx-dy$
(C) $dy$
(D) $-dy$

---

## 第一部分：全微分的定义与解题策略

### 1.1 全微分公式
函数 $z = f(x, y)$ 在点 $(x_0, y_0)$ 处的全微分定义为：
$$
df(x_0, y_0) = \frac{\partial f}{\partial x}(x_0, y_0) dx + \frac{\partial f}{\partial y}(x_0, y_0) dy
$$

### 1.2 解题策略
本题的目标是求 $df(1,1)$，因此我们需要求出偏导数 $f'_x(1,1)$ 和 $f'_y(1,1)$ 的值。
为此，我们将题目给出的两个复合函数方程两边分别对变量 $x$ 求导（利用**复合函数求导法则/链式法则**），然后代入特定的 $x$ 值，建立关于 $f'_x(1,1)$ 和 $f'_y(1,1)$ 的方程组进行求解。

设 $A = f'_x(1,1)$，$B = f'_y(1,1)$。

---

## 第二部分：利用第一个方程建立关系式

### 2.1 方程分析
已知方程：
$$
f(x+1, e^x) = x(x+1)^2
$$
我们要考察的点是 $(1,1)$。
令中间变量 $u = x+1, v = e^x$。
当 $(u, v) = (1, 1)$ 时，由 $\begin{cases} x+1=1 \\ e^x=1 \end{cases}$ 解得 **$x=0$**。

### 2.2 两边对 $x$ 求导
利用链式法则：
$$
\frac{\partial f}{\partial u} \cdot \frac{du}{dx} + \frac{\partial f}{\partial v} \cdot \frac{dv}{dx} = \frac{d}{dx} \left[ x(x+1)^2 \right]
$$

**计算左边（LHS）：**
$$
\begin{aligned}
LHS &= f'_1(x+1, e^x) \cdot (x+1)' + f'_2(x+1, e^x) \cdot (e^x)' \\
&= f'_1(x+1, e^x) \cdot 1 + f'_2(x+1, e^x) \cdot e^x
\end{aligned}
$$
*(注：$f'_1, f'_2$ 分别表示 $f$ 对第一个变量和第二个变量的偏导数)*

**计算右边（RHS）：**
$$
\begin{aligned}
RHS &= \frac{d}{dx} [x(x^2 + 2x + 1)] \\
&= \frac{d}{dx} (x^3 + 2x^2 + x) \\
&= 3x^2 + 4x + 1
\end{aligned}
$$

**得到等式：**
$$
f'_1(x+1, e^x) + e^x \cdot f'_2(x+1, e^x) = 3x^2 + 4x + 1
$$

### 2.3 代入 $x=0$
将 $x=0$ 代入上式，此时对应点为 $(0+1, e^0) = (1, 1)$：
$$
f'_1(1, 1) + e^0 \cdot f'_2(1, 1) = 3(0)^2 + 4(0) + 1
$$
即：
$$
f'_x(1, 1) + f'_y(1, 1) = 1 \quad \dots\dots \text{①}
$$
或者写成：**$A + B = 1$**

---

## 第三部分：利用第二个方程建立关系式

### 3.1 方程分析
已知方程：
$$
f(x, x^2) = 2x^2 \ln x
$$
我们要考察的点是 $(1,1)$。
令 $u = x, v = x^2$。
当 $(u, v) = (1, 1)$ 时，由 $\begin{cases} x=1 \\ x^2=1 \end{cases}$ 解得 **$x=1$**。

### 3.2 两边对 $x$ 求导
利用链式法则：
$$
\frac{\partial f}{\partial u} \cdot \frac{du}{dx} + \frac{\partial f}{\partial v} \cdot \frac{dv}{dx} = \frac{d}{dx} (2x^2 \ln x)
$$

**计算左边（LHS）：**
$$
\begin{aligned}
LHS &= f'_1(x, x^2) \cdot (x)' + f'_2(x, x^2) \cdot (x^2)' \\
&= f'_1(x, x^2) \cdot 1 + f'_2(x, x^2) \cdot 2x
\end{aligned}
$$

**计算右边（RHS）：**
利用乘积求导法则 $(uv)' = u'v + uv'$：
$$
\begin{aligned}
RHS &= (2x^2)' \cdot \ln x + 2x^2 \cdot (\ln x)' \\
&= 4x \ln x + 2x^2 \cdot \frac{1}{x} \\
&= 4x \ln x + 2x
\end{aligned}
$$

**得到等式：**
$$
f'_1(x, x^2) + 2x \cdot f'_2(x, x^2) = 4x \ln x + 2x
$$

### 3.3 代入 $x=1$
将 $x=1$ 代入上式，此时对应点为 $(1, 1^2) = (1, 1)$：
$$
f'_1(1, 1) + 2(1) \cdot f'_2(1, 1) = 4(1) \ln 1 + 2(1)
$$
因为 $\ln 1 = 0$，所以：
$$
f'_x(1, 1) + 2f'_y(1, 1) = 0 + 2
$$
即：
$$
f'_x(1, 1) + 2f'_y(1, 1) = 2 \quad \dots\dots \text{②}
$$
或者写成：**$A + 2B = 2$**

---

## 第四部分：联立求解与结论

### 4.1 解方程组
联立方程 ① 和 ②：
$$
\begin{cases}
A + B = 1 \quad \dots\dots \text{①} \\
A + 2B = 2 \quad \dots\dots \text{②}
\end{cases}
$$

用 ② 减去 ①：
$$
(A + 2B) - (A + B) = 2 - 1
$$
$$
B = 1
$$

将 $B=1$ 代入 ①：
$$
A + 1 = 1 \implies A = 0
$$

所以，偏导数值为：
$$
f'_x(1, 1) = 0, \quad f'_y(1, 1) = 1
$$

### 4.2 写出全微分
根据全微分公式：
$$
\begin{aligned}
df(1, 1) &= f'_x(1, 1) dx + f'_y(1, 1) dy \\
&= 0 \cdot dx + 1 \cdot dy \\
&= dy
\end{aligned}
$$

---

## ✅ 最终答案

根据计算结果 $df(1, 1) = dy$，对应选项 **(C)**。