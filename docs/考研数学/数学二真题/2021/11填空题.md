## 🎯 问题回顾

**计算广义积分**：

$$
\int_{-\infty}^{+\infty} |x| e^{-x^{2}} dx
$$

---

## 第一部分：被积函数的性质分析

在开始积分计算之前，我们首先分析被积函数 $f(x) = |x|e^{-x^2}$ 的性质，这将极大地简化运算过程。

### 1.1 奇偶性分析

定义函数 $f(x)$：
$$
f(x) = |x|e^{-x^2}
$$

考察 $f(-x)$：
1.  **绝对值部分**：$|-x| = |x|$
2.  **指数部分**：$e^{-(-x)^2} = e^{-x^2}$

将两部分结合：
$$
f(-x) = |-x|e^{-(-x)^2} = |x|e^{-x^2} = f(x)
$$

**结论**：
该函数 $f(x)$ 满足 $f(-x) = f(x)$，因此它是**偶函数**。

### 1.2 利用对称性简化积分区间

根据偶函数在对称区间上的积分性质：
$$
\int_{-a}^{a} f(x) dx = 2 \int_{0}^{a} f(x) dx
$$
对于广义积分（区间为 $(-\infty, +\infty)$），该性质同样适用。

因此，原积分 $I$ 可以转化为：
$$
I = \int_{-\infty}^{+\infty} |x| e^{-x^{2}} dx = 2 \int_{0}^{+\infty} |x| e^{-x^{2}} dx
$$

由于积分区间变为 $[0, +\infty)$，在此区间内 $x \ge 0$，因此 $|x| = x$。
积分表达式进一步简化为：
$$
I = 2 \int_{0}^{+\infty} x e^{-x^{2}} dx
$$

> 💡 **策略优势**：通过对称性分析，我们将原本包含绝对值的全轴积分，转化为了不含绝对值的半轴积分，避免了分段讨论的繁琐。

---

## 第二部分：广义积分的计算

我们需要计算核心积分部分：
$$
J = \int_{0}^{+\infty} x e^{-x^{2}} dx
$$
然后最终结果为 $I = 2J$。

### 2.1 方法选择：凑微分法 (Substitution Method)

观察被积函数 $x e^{-x^2}$，我们发现因式 $x$ 正好是指数部分 $-x^2$ 导数的一部分。
$$
\frac{d}{dx}(-x^2) = -2x \implies x dx = -\frac{1}{2} d(-x^2)
$$

### 2.2 详细计算步骤

我们将积分写为极限形式（广义积分的定义）：
$$
J = \lim_{b \to +\infty} \int_{0}^{b} x e^{-x^{2}} dx
$$

#### 步骤 1：寻找原函数
利用凑微分公式：
$$
\begin{aligned}
\int x e^{-x^2} dx &= \int e^{-x^2} \cdot (x dx) \\
&= \int e^{-x^2} \cdot \left( -\frac{1}{2} d(-x^2) \right) \\
&= -\frac{1}{2} \int e^u du \quad (\text{令 } u = -x^2) \\
&= -\frac{1}{2} e^u + C \\
&= -\frac{1}{2} e^{-x^2} + C
\end{aligned}
$$

#### 步骤 2：代入上下限计算定积分
$$
\begin{aligned}
\int_{0}^{b} x e^{-x^{2}} dx &= \left[ -\frac{1}{2} e^{-x^2} \right]_{0}^{b} \\
&= \left( -\frac{1}{2} e^{-b^2} \right) - \left( -\frac{1}{2} e^{-0^2} \right) \\
&= -\frac{1}{2} e^{-b^2} + \frac{1}{2} e^{0} \\
&= \frac{1}{2} - \frac{1}{2} e^{-b^2}
\end{aligned}
$$

#### 步骤 3：求极限
现在让 $b \to +\infty$：
$$
J = \lim_{b \to +\infty} \left( \frac{1}{2} - \frac{1}{2} e^{-b^2} \right)
$$
由于指数函数的性质：
$$
\lim_{b \to +\infty} e^{-b^2} = \lim_{t \to +\infty} \frac{1}{e^t} = 0
$$
所以：
$$
J = \frac{1}{2} - 0 = \frac{1}{2}
$$

✅ **核心积分结果**：
$$
\int_{0}^{+\infty} x e^{-x^{2}} dx = \frac{1}{2}
$$

---

## 第三部分：综合结论

### 3.1 最终计算

回到第一部分建立的公式：
$$
I = 2 \int_{0}^{+\infty} x e^{-x^{2}} dx
$$

将 $J = \frac{1}{2}$ 代入：
$$
I = 2 \times \frac{1}{2} = 1
$$

### 3.2 验证（可选视角：Gamma 函数）

我们可以利用 Gamma 函数 $\Gamma(s) = \int_0^{+\infty} t^{s-1}e^{-t} dt$ 来验证。
对于 $\int_{0}^{+\infty} x e^{-x^{2}} dx$，令 $t = x^2$，则 $dt = 2x dx \implies x dx = \frac{1}{2}dt$。
$$
\int_{0}^{+\infty} x e^{-x^{2}} dx = \int_{0}^{+\infty} e^{-t} \frac{1}{2} dt = \frac{1}{2} \int_{0}^{+\infty} t^{1-1} e^{-t} dt = \frac{1}{2} \Gamma(1)
$$
已知 $\Gamma(1) = 0! = 1$，故结果为 $\frac{1}{2}$。
最终结果 $I = 2 \times \frac{1}{2} = 1$。
**验证无误。**

---

## ✅ 最终答案

$$
\int_{-\infty}^{+\infty}|x|e^{-x^{2}}dx = 1
$$

---

## 📌 关键要点总结

1.  **对称性优先**：观察到被积函数是**偶函数**，立即将区间 $(-\infty, +\infty)$ 转化为 $2 \times [0, +\infty)$，这是解题最关键的一步，去掉了棘手的绝对值符号。
2.  **凑微分技巧**：处理形如 $\int x e^{g(x)} dx$ 的积分时，若 $g(x)$ 是二次项，通常利用 $d(x^2) = 2x dx$ 进行凑微分。
3.  **广义积分定义**：严格来说，无穷限积分是变上限积分的极限，注意 $\lim_{x \to +\infty} e^{-x^2} = 0$ 的应用。
4.  **系数管理**：在凑微分（$-\frac{1}{2}$）和利用对称性（$\times 2$）的过程中，要小心处理常数系数，本题中它们恰好抵消。

Would you like me to help you solve another integral or explain a specific calculus concept?