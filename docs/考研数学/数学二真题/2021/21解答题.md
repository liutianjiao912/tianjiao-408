这是一道经典的二重积分计算题，涉及**极坐标变换**。观察被积区域的边界方程 $(x^2+y^2)^2 = x^2-y^2$，这是典型的双纽线（Lemniscate）方程，因此使用极坐标进行计算是最简便的方法。

下面是详细的解析步骤。

---

## 🎯 题目回顾

计算二重积分：
$$
I = \iint_{D} xy \, dxdy
$$
其中区域 $D$ 由曲线 $(x^{2}+y^{2})^{2}=x^{2}-y^{2}$ 与 $x$ 轴在第一象限（$x\ge0, y\ge0$）围成。

---

## 第一部分：区域分析与坐标变换

### 1.1 极坐标变换
由于方程中包含 $x^2+y^2$，我们采用极坐标变换：
$$
\begin{cases}
x = r\cos\theta \\
y = r\sin\theta
\end{cases}
$$
此时，面积元素（雅可比行列式）为：
$$
dxdy = r \, dr d\theta
$$

### 1.2 边界方程转化
将 $x, y$ 代入曲线方程 $(x^{2}+y^{2})^{2}=x^{2}-y^{2}$：

$$
\begin{aligned}
(r^2)^2 &= (r\cos\theta)^2 - (r\sin\theta)^2 \\
r^4 &= r^2(\cos^2\theta - \sin^2\theta) \\
r^4 &= r^2\cos(2\theta)
\end{aligned}
$$

当 $r \ne 0$ 时，两边消去 $r^2$，得到极坐标下的边界方程：
$$
r^2 = \cos(2\theta) \quad \implies \quad r = \sqrt{\cos(2\theta)}
$$

### 1.3 确定积分限
1.  **角度 $\theta$ 的范围**：
    * 题目限制在第一象限：$x \ge 0, y \ge 0 \implies 0 \le \theta \le \frac{\pi}{2}$。
    * 由方程 $r = \sqrt{\cos(2\theta)}$ 可知，根号内必须非负：$\cos(2\theta) \ge 0$。
    * 在 $[0, \frac{\pi}{2}]$ 范围内，$\cos(2\theta) \ge 0$ 意味着 $0 \le 2\theta \le \frac{\pi}{2}$，即 **$0 \le \theta \le \frac{\pi}{4}$**。
    * 这与题目描述的“与 $x$ 轴围成”（即 $\theta$ 从 0 开始）相符。

2.  **半径 $r$ 的范围**：
    * 从原点出发（$r=0$）直到曲线边界（$r=\sqrt{\cos(2\theta)}$）。
    * 故 **$0 \le r \le \sqrt{\cos(2\theta)}$**。



---

## 第二部分：二重积分的计算

### 2.1 写出定积分表达式
将被积函数 $xy$ 转化为极坐标形式：
$$
xy = (r\cos\theta)(r\sin\theta) = r^2\cos\theta\sin\theta = \frac{1}{2}r^2\sin(2\theta)
$$

代入二重积分公式：
$$
I = \int_{0}^{\frac{\pi}{4}} d\theta \int_{0}^{\sqrt{\cos(2\theta)}} \left( \frac{1}{2}r^2\sin(2\theta) \right) \cdot r \, dr
$$
整理得：
$$
I = \frac{1}{2} \int_{0}^{\frac{\pi}{4}} \sin(2\theta) \left[ \int_{0}^{\sqrt{\cos(2\theta)}} r^3 \, dr \right] d\theta
$$

### 2.2 计算内层积分（关于 $r$）
$$
\begin{aligned}
\int_{0}^{\sqrt{\cos(2\theta)}} r^3 \, dr &= \left[ \frac{1}{4}r^4 \right]_{0}^{\sqrt{\cos(2\theta)}} \\
&= \frac{1}{4} \left( (\sqrt{\cos(2\theta)})^4 - 0 \right) \\
&= \frac{1}{4} \cos^2(2\theta)
\end{aligned}
$$

### 2.3 计算外层积分（关于 $\theta$）
将内层结果代回原式：
$$
\begin{aligned}
I &= \frac{1}{2} \int_{0}^{\frac{\pi}{4}} \sin(2\theta) \cdot \left( \frac{1}{4}\cos^2(2\theta) \right) d\theta \\
&= \frac{1}{8} \int_{0}^{\frac{\pi}{4}} \cos^2(2\theta) \sin(2\theta) \, d\theta
\end{aligned}
$$

**凑微分法（换元）**：
令 $u = \cos(2\theta)$，则 $du = -2\sin(2\theta)d\theta$，即 $\sin(2\theta)d\theta = -\frac{1}{2}du$。

积分限变换：
* 当 $\theta = 0$ 时，$u = \cos(0) = 1$。
* 当 $\theta = \frac{\pi}{4}$ 时，$u = \cos(\frac{\pi}{2}) = 0$。

代入计算：
$$
\begin{aligned}
I &= \frac{1}{8} \int_{1}^{0} u^2 \left( -\frac{1}{2} du \right) \\
&= -\frac{1}{16} \int_{1}^{0} u^2 \, du \\
&= \frac{1}{16} \int_{0}^{1} u^2 \, du \quad \text{(交换积分限变号)} \\
&= \frac{1}{16} \left[ \frac{1}{3}u^3 \right]_{0}^{1} \\
&= \frac{1}{16} \cdot \frac{1}{3} \\
&= \frac{1}{48}
\end{aligned}
$$

---

## 📌 总结与最终答案

1.  **坐标变换**：识别出区域为双纽线的一部分，果断使用极坐标 $(r, \theta)$。
2.  **定限**：准确分析出 $\theta \in [0, \frac{\pi}{4}]$ 和 $r \in [0, \sqrt{\cos(2\theta)}]$ 是解题关键。
3.  **计算**：利用 $\sin(2\theta)d\theta$ 与 $\cos(2\theta)$ 的导数关系进行凑微分，大大简化运算。

✅ **最终答案：**
$$
\iint_{D}xydxdy = \frac{1}{48}
$$