这是一道关于**矩阵特征值理论**与**相似对角化**的经典综合题。解题过程分为两个主要步骤：首先利用特征值和对角化条件确定参数 $a, b$ 的值，然后针对每组解分别求解特征向量以构造可逆矩阵 $P$。

下面我将分步为您详细解析。

---

## 🎯 题目回顾

已知矩阵 $A=\begin{pmatrix}2&1&0\\ 1&2&0\\ 1&a&b\end{pmatrix}$。
条件：
1.  $A$ 仅有两个不同的特征值；
2.  $A$ 相似于对角矩阵（即 $A$ 可对角化）。

求：$a, b$ 的值，以及使 $P^{-1}AP = \Lambda$ 的可逆矩阵 $P$。

---

## 第一部分：确定参数 $a$ 和 $b$ 的值

### 1.1 计算 $A$ 的特征多项式

特征多项式为 $|\lambda E - A|$：
$$
\begin{aligned}
|\lambda E - A| &= \begin{vmatrix} \lambda-2 & -1 & 0 \\ -1 & \lambda-2 & 0 \\ -1 & -a & \lambda-b \end{vmatrix} \\
&= (\lambda-b) \cdot (-1)^{3+3} \cdot \begin{vmatrix} \lambda-2 & -1 \\ -1 & \lambda-2 \end{vmatrix} \quad \text{（按第三列展开）} \\
&= (\lambda-b) \left[ (\lambda-2)^2 - 1 \right] \\
&= (\lambda-b) (\lambda^2 - 4\lambda + 3) \\
&= (\lambda-b)(\lambda-1)(\lambda-3)
\end{aligned}
$$

令 $|\lambda E - A| = 0$，解得 $A$ 的特征值为：
$$
\lambda_1 = 1, \quad \lambda_2 = 3, \quad \lambda_3 = b
$$

### 1.2 利用条件确定 $b$

题目要求“仅有两个不同的特征值”，因此 $\lambda_3 = b$ 必须与 $\lambda_1$ 或 $\lambda_2$ 重合。
所以，$b$ 有两种可能的情况：
1.  **情形一**：$b = 1$（此时特征值为 $1, 1, 3$）；
2.  **情形二**：$b = 3$（此时特征值为 $1, 3, 3$）。

### 1.3 利用“可对角化”条件确定 $a$

矩阵 $A$ 可对角化的充要条件是：对于每个特征值，其**几何重数**（线性无关特征向量的个数）等于**代数重数**（重根次数）。
即：对于 $k$ 重特征值 $\lambda^*$，必须满足 $n - \text{rank}(A - \lambda^* E) = k$。
在本题中 $n=3$，重数 $k=2$，故需满足：
$$
3 - \text{rank}(A - \lambda^* E) = 2 \implies \text{rank}(A - \lambda^* E) = 1
$$

**情形一：当 $b = 1$ 时**
特征值为 $1, 1, 3$。需验证重根 $\lambda = 1$ 的秩条件。
代入 $b=1, \lambda=1$：
$$
A - E = \begin{pmatrix} 2-1 & 1 & 0 \\ 1 & 2-1 & 0 \\ 1 & a & 1-1 \end{pmatrix} = \begin{pmatrix} 1 & 1 & 0 \\ 1 & 1 & 0 \\ 1 & a & 0 \end{pmatrix}
$$
要使 $\text{rank}(A-E) = 1$，则矩阵的所有行向量必须线性相关。
显然第一行和第二行相同。第三行 $(1, a, 0)$ 必须与第一行 $(1, 1, 0)$ 成比例。
即 $a = 1$。

**情形二：当 $b = 3$ 时**
特征值为 $1, 3, 3$。需验证重根 $\lambda = 3$ 的秩条件。
代入 $b=3, \lambda=3$：
$$
A - 3E = \begin{pmatrix} 2-3 & 1 & 0 \\ 1 & 2-3 & 0 \\ 1 & a & 3-3 \end{pmatrix} = \begin{pmatrix} -1 & 1 & 0 \\ 1 & -1 & 0 \\ 1 & a & 0 \end{pmatrix}
$$
要使 $\text{rank}(A-3E) = 1$，所有行向量线性相关。
第一行与第二行互为相反数。第三行 $(1, a, 0)$ 必须与第二行 $(1, -1, 0)$ 成比例。
即 $a = -1$。

---

## 第二部分：求可逆矩阵 $P$ 与对角矩阵 $\Lambda$

我们将分别针对上述两种情形求解。

### 情形一：$a=1, b=1$

此时特征值为 $1, 1, 3$，对角矩阵 $\Lambda = \text{diag}(1, 1, 3)$。
$A = \begin{pmatrix} 2 & 1 & 0 \\ 1 & 2 & 0 \\ 1 & 1 & 1 \end{pmatrix}$。

1.  **求解 $\lambda = 1$ 的特征向量**
    解方程 $(A - E)x = 0$：
    $$
    A - E = \begin{pmatrix} 1 & 1 & 0 \\ 1 & 1 & 0 \\ 1 & 1 & 0 \end{pmatrix} \xrightarrow{\text{行变换}} \begin{pmatrix} 1 & 1 & 0 \\ 0 & 0 & 0 \\ 0 & 0 & 0 \end{pmatrix}
    $$
    方程为 $x_1 + x_2 = 0$。含两个自由变量 $x_2, x_3$。
    - 取 $x_2=-1, x_3=0 \implies x_1=1$，得 $\xi_1 = (1, -1, 0)^T$；
    - 取 $x_2=0, x_3=1 \implies x_1=0$，得 $\xi_2 = (0, 0, 1)^T$。

2.  **求解 $\lambda = 3$ 的特征向量**
    解方程 $(A - 3E)x = 0$：
    $$
    A - 3E = \begin{pmatrix} -1 & 1 & 0 \\ 1 & -1 & 0 \\ 1 & 1 & -2 \end{pmatrix} \xrightarrow{\text{行变换}} \begin{pmatrix} -1 & 1 & 0 \\ 0 & 0 & 0 \\ 0 & 2 & -2 \end{pmatrix} \sim \begin{pmatrix} -1 & 1 & 0 \\ 0 & 1 & -1 \\ 0 & 0 & 0 \end{pmatrix}
    $$
    方程组为 $\begin{cases} -x_1 + x_2 = 0 \\ x_2 - x_3 = 0 \end{cases} \implies x_1 = x_2 = x_3$。
    - 取 $x_3=1$，得 $\xi_3 = (1, 1, 1)^T$。

**结果 1**：
$$
a=1, b=1; \quad P = \begin{pmatrix} 1 & 0 & 1 \\ -1 & 0 & 1 \\ 0 & 1 & 1 \end{pmatrix}, \quad P^{-1}AP = \begin{pmatrix} 1 & 0 & 0 \\ 0 & 1 & 0 \\ 0 & 0 & 3 \end{pmatrix}
$$

---

### 情形二：$a=-1, b=3$

此时特征值为 $1, 3, 3$，对角矩阵 $\Lambda = \text{diag}(1, 3, 3)$。
$A = \begin{pmatrix} 2 & 1 & 0 \\ 1 & 2 & 0 \\ 1 & -1 & 3 \end{pmatrix}$。

1.  **求解 $\lambda = 1$ 的特征向量**
    解方程 $(A - E)x = 0$：
    $$
    A - E = \begin{pmatrix} 1 & 1 & 0 \\ 1 & 1 & 0 \\ 1 & -1 & 2 \end{pmatrix} \xrightarrow{\text{行变换}} \begin{pmatrix} 1 & 1 & 0 \\ 0 & 0 & 0 \\ 0 & -2 & 2 \end{pmatrix} \sim \begin{pmatrix} 1 & 1 & 0 \\ 0 & 1 & -1 \\ 0 & 0 & 0 \end{pmatrix}
    $$
    方程组为 $\begin{cases} x_1 + x_2 = 0 \\ x_2 - x_3 = 0 \end{cases} \implies x_2 = x_3, x_1 = -x_2$。
    - 取 $x_3=1$，得 $\xi_1 = (-1, 1, 1)^T$。

2.  **求解 $\lambda = 3$ 的特征向量**
    解方程 $(A - 3E)x = 0$：
    $$
    A - 3E = \begin{pmatrix} -1 & 1 & 0 \\ 1 & -1 & 0 \\ 1 & -1 & 0 \end{pmatrix} \xrightarrow{\text{行变换}} \begin{pmatrix} -1 & 1 & 0 \\ 0 & 0 & 0 \\ 0 & 0 & 0 \end{pmatrix}
    $$
    方程为 $-x_1 + x_2 = 0 \implies x_1 = x_2$。含两个自由变量 $x_2, x_3$。
    - 取 $x_2=1, x_3=0 \implies x_1=1$，得 $\xi_2 = (1, 1, 0)^T$；
    - 取 $x_2=0, x_3=1 \implies x_1=0$，得 $\xi_3 = (0, 0, 1)^T$。

**结果 2**：
$$
a=-1, b=3; \quad P = \begin{pmatrix} -1 & 1 & 0 \\ 1 & 1 & 0 \\ 1 & 0 & 1 \end{pmatrix}, \quad P^{-1}AP = \begin{pmatrix} 1 & 0 & 0 \\ 0 & 3 & 0 \\ 0 & 0 & 3 \end{pmatrix}
$$

---

## ✅ 最终总结

本题有两组解：

1.  **第一组解**：
    $$
    a = 1, \quad b = 1
    $$
    此时：
    $$
    P = \begin{pmatrix} 1 & 0 & 1 \\ -1 & 0 & 1 \\ 0 & 1 & 1 \end{pmatrix}
    $$

2.  **第二组解**：
    $$
    a = -1, \quad b = 3
    $$
    此时：
    $$
    P = \begin{pmatrix} -1 & 1 & 0 \\ 1 & 1 & 0 \\ 1 & 0 & 1 \end{pmatrix}
    $$

*(注：$P$ 中的列向量顺序可以交换，对应的对角矩阵 $\Lambda$ 元素顺序也随之改变。上述答案给出的是一种标准形式)*