## 🎯 问题回顾

若当 $x\rightarrow0$ 时，$\alpha(x), \beta(x)$ 是非零无穷小量，则以下的命题中，
① 若 $\alpha(x)\sim\beta(x)$，则 $\alpha^{2}(x)\sim\beta^{2}(x)$;
② 若 $\alpha^{2}(x)\sim\beta^{2}(x)$，则 $\alpha(x)\sim\beta(x)$;
③ 若 $\alpha(x)\sim\beta(x)$，则 $\alpha(x)-\beta(x)=o(\alpha(x))$;
④ 若 $\alpha(x)-\beta(x)=o(\alpha(x))$，则 $\alpha(x)\sim\beta(x)$.

真命题的序号为 ( )

- (A) ①③
- (B) ①④
- (C) ①③④
- (D) ②③④

---

## 第一部分：预备知识与定义回顾

在分析具体命题之前，我们需要明确“等价无穷小”和“高阶无穷小”的严格数学定义。设 $\alpha(x), \beta(x)$ 为 $x \to 0$ 时的非零无穷小量。

### 1.1 等价无穷小 ($\sim$) 的定义
称 $\alpha(x)$ 与 $\beta(x)$ 是**等价无穷小**，记作 $\alpha(x) \sim \beta(x)$，当且仅当：
$$
\lim_{x \to 0} \frac{\alpha(x)}{\beta(x)} = 1
$$

### 1.2 高阶无穷小 ($o$) 的定义
称 $\alpha(x)$ 是 $\beta(x)$ 的**高阶无穷小**，记作 $\alpha(x) = o(\beta(x))$，当且仅当：
$$
\lim_{x \to 0} \frac{\alpha(x)}{\beta(x)} = 0
$$

我们将利用极限的四则运算法则来逐一验证命题。

---

## 第二部分：命题逐项详细解析

### 2.1 命题 ① 的分析

**命题内容**：若 $\alpha(x)\sim\beta(x)$，则 $\alpha^{2}(x)\sim\beta^{2}(x)$。

**证明步骤**：
1.  **已知条件**：由 $\alpha(x) \sim \beta(x)$ 可知
    $$
    \lim_{x \to 0} \frac{\alpha(x)}{\beta(x)} = 1
    $$
2.  **目标验证**：我们需要验证 $\lim_{x \to 0} \frac{\alpha^2(x)}{\beta^2(x)}$ 是否等于 1。
3.  **计算极限**：
    利用极限的乘法运算法则（或幂运算连续性）：
    $$
    \lim_{x \to 0} \frac{\alpha^2(x)}{\beta^2(x)} = \lim_{x \to 0} \left( \frac{\alpha(x)}{\beta(x)} \right)^2 = \left( \lim_{x \to 0} \frac{\alpha(x)}{\beta(x)} \right)^2
    $$
    代入已知极限值：
    $$
    = (1)^2 = 1
    $$
4.  **结论**：根据定义，极限为 1，故 $\alpha^2(x) \sim \beta^2(x)$ 成立。

✅ **命题 ① 正确**。

---

### 2.2 命题 ② 的分析

**命题内容**：若 $\alpha^{2}(x)\sim\beta^{2}(x)$，则 $\alpha(x)\sim\beta(x)$。

**分析步骤**：
1.  **已知条件**：
    $$
    \lim_{x \to 0} \frac{\alpha^2(x)}{\beta^2(x)} = 1 \implies \lim_{x \to 0} \left( \frac{\alpha(x)}{\beta(x)} \right)^2 = 1
    $$
2.  **目标推导**：这是否意味着 $\lim_{x \to 0} \frac{\alpha(x)}{\beta(x)} = 1$？
3.  **逻辑漏洞**：
    若 $y^2 \to 1$，则 $y$ 可能趋于 1，也可能趋于 -1。如果极限是 -1，则两个无穷小量不“等价”（等价要求比值为 +1）。
4.  **构造反例**：
    取 $\alpha(x) = x$，$\beta(x) = -x$。
    - 验证前提：
      $$
      \alpha^2(x) = x^2, \quad \beta^2(x) = (-x)^2 = x^2
      $$
      $$
      \lim_{x \to 0} \frac{\alpha^2(x)}{\beta^2(x)} = \lim_{x \to 0} \frac{x^2}{x^2} = 1 \quad (\text{满足前提})
      $$
    - 验证结论：
      $$
      \lim_{x \to 0} \frac{\alpha(x)}{\beta(x)} = \lim_{x \to 0} \frac{x}{-x} = -1 \ne 1
      $$
      故 $\alpha(x)$ 与 $\beta(x)$ 不等价。

❌ **命题 ② 错误**。

---

### 2.3 命题 ③ 的分析

**命题内容**：若 $\alpha(x)\sim\beta(x)$，则 $\alpha(x)-\beta(x)=o(\alpha(x))$。

**证明步骤**：
1.  **已知条件**：
    $$
    \lim_{x \to 0} \frac{\beta(x)}{\alpha(x)} = 1 \quad (\text{注：等价具有对称性，}\frac{\alpha}{\beta} \to 1 \iff \frac{\beta}{\alpha} \to 1)
    $$
2.  **目标验证**：验证 $\alpha(x) - \beta(x)$ 是否是 $\alpha(x)$ 的高阶无穷小，即检查极限：
    $$
    L = \lim_{x \to 0} \frac{\alpha(x) - \beta(x)}{\alpha(x)}
    $$
3.  **拆分计算**：
    $$
    L = \lim_{x \to 0} \left( \frac{\alpha(x)}{\alpha(x)} - \frac{\beta(x)}{\alpha(x)} \right) = \lim_{x \to 0} \left( 1 - \frac{\beta(x)}{\alpha(x)} \right)
    $$
4.  **代入极限**：
    $$
    = 1 - \lim_{x \to 0} \frac{\beta(x)}{\alpha(x)} = 1 - 1 = 0
    $$
5.  **结论**：根据定义，极限为 0，即 $\alpha(x) - \beta(x) = o(\alpha(x))$ 成立。

✅ **命题 ③ 正确**。

---

### 2.4 命题 ④ 的分析

**命题内容**：若 $\alpha(x)-\beta(x)=o(\alpha(x))$，则 $\alpha(x)\sim\beta(x)$。

**证明步骤**：
1.  **已知条件**：
    $$
    \lim_{x \to 0} \frac{\alpha(x) - \beta(x)}{\alpha(x)} = 0
    $$
2.  **变形处理**：
    $$
    \lim_{x \to 0} \left( 1 - \frac{\beta(x)}{\alpha(x)} \right) = 0
    $$
3.  **推导极限**：
    $$
    1 - \lim_{x \to 0} \frac{\beta(x)}{\alpha(x)} = 0 \implies \lim_{x \to 0} \frac{\beta(x)}{\alpha(x)} = 1
    $$
4.  **结论**：
    因为 $\lim_{x \to 0} \frac{\beta(x)}{\alpha(x)} = 1$，即 $\beta(x) \sim \alpha(x)$。根据对称性，这等价于 $\alpha(x) \sim \beta(x)$。

✅ **命题 ④ 正确**。

---

## 第三部分：综合筛选与结论

### 3.1 命题真假汇总表

| 序号 | 命题简述 | 真假 | 理由/依据 |
| :---: | :--- | :---: | :--- |
| ① | $\alpha \sim \beta \implies \alpha^2 \sim \beta^2$ | ✅ | 极限的幂运算法则 |
| ② | $\alpha^2 \sim \beta^2 \implies \alpha \sim \beta$ | ❌ | 反例：$\alpha=x, \beta=-x$ (符号问题) |
| ③ | $\alpha \sim \beta \implies \alpha-\beta=o(\alpha)$ | ✅ | 等价无穷小的充要条件变形之一 |
| ④ | $\alpha-\beta=o(\alpha) \implies \alpha \sim \beta$ | ✅ | 等价无穷小的充要条件变形之二 |

### 3.2 选项匹配

- 真命题的序号为：①、③、④
- 对照选项：
    - (A) ①③
    - (B) ①④
    - (C) ①③④
    - (D) ②③④

显然 **(C)** 是正确选项。

---

## ✅ 最终答案：(C)

---

## 📌 关键要点总结

1.  **等价无穷小的代数性质**：等价关系对于乘方、开方（需注意符号）等运算通常保持一致，但反之（如偶次幂推一次幂）因涉及符号不确定性而不成立。
2.  **等价无穷小的充要条件**：以下三个式子是完全等价的，解题时可以灵活互换：
    - $\displaystyle \lim \frac{\alpha}{\beta} = 1$ （定义形式）
    - $\beta(x) = \alpha(x) + o(\alpha(x))$ （代换形式）
    - $\alpha(x) - \beta(x) = o(\alpha(x))$ （差形式）
    *注：命题 ③ 和 ④ 其实就是上述第 3 点充要条件的双向验证。*
3.  **反例构造法**：判断命题为假时，最简单的方法是找反例。对于平方相等的条件，$x$ 和 $-x$ 是最经典的反例组合。

---
**您是否希望我针对“无穷小量的阶的比较”再给出一个具体的计算例题进行巩固？**