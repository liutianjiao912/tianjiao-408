这是一道关于**二次型标准化**（正交变换法）和**瑞利商（Rayleigh Quotient）**性质的经典线性代数题目。解题的核心在于写出二次型的矩阵，求出特征值和特征向量，并进行正交单位化。

下面我将分步为您详细解析。

---

## 🎯 题目回顾

已知二次型：
$$
f(x_{1},x_{2},x_{3})=3x_{1}^{2}+4x_{2}^{2}+3x_{3}^{2}+2x_{1}x_{3}
$$

(I) 求正交矩阵 $Q$，使正交变换 $x=Qy$ 将二次型化为标准形；
(II) 证明 $\min_{x\ne0}\frac{f(x)}{x^{T}x}=2$.

---

## 第一部分：求正交矩阵 $Q$

### 1.1 写出二次型的矩阵 $A$

根据二次型的系数，我们可以写出对应的实对称矩阵 $A$。
- 对角线元素为平方项系数：$a_{11}=3, a_{22}=4, a_{33}=3$。
- 非对角线元素 $a_{ij}$ 为 $x_i x_j$ 系数的一半：
  - $x_1 x_3$ 系数为 2，故 $a_{13}=a_{31}=1$。
  - 无 $x_1 x_2$ 和 $x_2 x_3$ 项，故其余元素为 0。

矩阵 $A$ 为：
$$
A = \begin{pmatrix} 3 & 0 & 1 \\ 0 & 4 & 0 \\ 1 & 0 & 3 \end{pmatrix}
$$

### 1.2 求特征值

计算特征多项式 $|\lambda E - A|$：

$$
\begin{aligned}
|\lambda E - A| &= \begin{vmatrix} \lambda-3 & 0 & -1 \\ 0 & \lambda-4 & 0 \\ -1 & 0 & \lambda-3 \end{vmatrix} \\
&\quad \text{(按第二行展开)} \\
&= (\lambda-4) \cdot (-1)^{2+2} \cdot \begin{vmatrix} \lambda-3 & -1 \\ -1 & \lambda-3 \end{vmatrix} \\
&= (\lambda-4) \left[ (\lambda-3)^2 - (-1)(-1) \right] \\
&= (\lambda-4) [ (\lambda-3)^2 - 1 ] \\
&= (\lambda-4) (\lambda^2 - 6\lambda + 8) \\
&= (\lambda-4)(\lambda-2)(\lambda-4) \\
&= (\lambda-2)(\lambda-4)^2
\end{aligned}
$$

令 $|\lambda E - A| = 0$，解得特征值为：
$$
\lambda_1 = 2, \quad \lambda_2 = \lambda_3 = 4
$$

### 1.3 求特征向量

我们需要分别求出对应于特征值的线性无关特征向量。

**1. 当 $\lambda_1 = 2$ 时：**
解方程组 $(2E - A)x = 0$：
$$
2E - A = \begin{pmatrix} -1 & 0 & -1 \\ 0 & -2 & 0 \\ -1 & 0 & -1 \end{pmatrix} \xrightarrow{r_3-r_1} \begin{pmatrix} -1 & 0 & -1 \\ 0 & -2 & 0 \\ 0 & 0 & 0 \end{pmatrix}
$$
对应的线性方程组为：
$$
\begin{cases} -x_1 - x_3 = 0 \\ -2x_2 = 0 \end{cases} \implies \begin{cases} x_1 = -x_3 \\ x_2 = 0 \end{cases}
$$
令 $x_3 = 1$，得特征向量 $\xi_1 = (-1, 0, 1)^T$。

**2. 当 $\lambda_2 = \lambda_3 = 4$ 时：**
解方程组 $(4E - A)x = 0$：
$$
4E - A = \begin{pmatrix} 1 & 0 & -1 \\ 0 & 0 & 0 \\ -1 & 0 & 1 \end{pmatrix} \xrightarrow{r_3+r_1} \begin{pmatrix} 1 & 0 & -1 \\ 0 & 0 & 0 \\ 0 & 0 & 0 \end{pmatrix}
$$
对应的线性方程组为：
$$
x_1 - x_3 = 0 \implies x_1 = x_3
$$
这里 $x_2$ 没有限制，是自由变量。我们需要找两个线性无关的解向量。
- 令 $x_2=1, x_3=0 \implies x_1=0$，得 $\xi_2 = (0, 1, 0)^T$。
- 令 $x_2=0, x_3=1 \implies x_1=1$，得 $\xi_3 = (1, 0, 1)^T$。

### 1.4 正交化与单位化

对于实对称矩阵，不同特征值对应的特征向量天然正交。
- $\xi_1$ 与 $\xi_2$：$(-1)\cdot 0 + 0\cdot 1 + 1\cdot 0 = 0$ (已正交)
- $\xi_1$ 与 $\xi_3$：$(-1)\cdot 1 + 0\cdot 0 + 1\cdot 1 = 0$ (已正交)

我们需要检查重根 $\lambda=4$ 内部的两个特征向量 $\xi_2, \xi_3$ 是否正交：
- $\xi_2 \cdot \xi_3 = 0\cdot 1 + 1\cdot 0 + 0\cdot 1 = 0$。
幸运的是，它们已经正交，无需进行施密特正交化。

接下来进行**单位化**：

1.  对 $\xi_1 = (-1, 0, 1)^T$：
    $$
    \|\xi_1\| = \sqrt{(-1)^2 + 0^2 + 1^2} = \sqrt{2}
    $$
    $$
    p_1 = \frac{1}{\sqrt{2}} \begin{pmatrix} -1 \\ 0 \\ 1 \end{pmatrix}
    $$

2.  对 $\xi_2 = (0, 1, 0)^T$：
    $$
    \|\xi_2\| = 1
    $$
    $$
    p_2 = \begin{pmatrix} 0 \\ 1 \\ 0 \end{pmatrix}
    $$

3.  对 $\xi_3 = (1, 0, 1)^T$：
    $$
    \|\xi_3\| = \sqrt{1^2 + 0^2 + 1^2} = \sqrt{2}
    $$
    $$
    p_3 = \frac{1}{\sqrt{2}} \begin{pmatrix} 1 \\ 0 \\ 1 \end{pmatrix}
    $$

### 1.5 构造正交矩阵 $Q$

将单位化后的特征向量作为列向量构成矩阵 $Q$：
$$
Q = (p_1, p_2, p_3) = \begin{pmatrix} -\frac{1}{\sqrt{2}} & 0 & \frac{1}{\sqrt{2}} \\ 0 & 1 & 0 \\ \frac{1}{\sqrt{2}} & 0 & \frac{1}{\sqrt{2}} \end{pmatrix}
$$

经正交变换 $x=Qy$，二次型化为标准形：
$$
f = 2y_1^2 + 4y_2^2 + 4y_3^2
$$

---

## 第二部分：证明最值问题

### 2.1 证明思路

我们要证明 $\min_{x\ne0}\frac{f(x)}{x^{T}x}=2$。这个分式 $\frac{x^T A x}{x^T x}$ 被称为**瑞利商**。
利用第一部分的正交变换，可以将问题转化为标准形下的代数不等式证明。

### 2.2 详细证明过程

**证明：**

作正交变换 $x = Qy$，其中 $Q$ 为第一部分求得的正交矩阵。
由于 $Q$ 是正交矩阵，具有保持向量长度不变的性质，即：
$$
x^T x = (Qy)^T (Qy) = y^T Q^T Q y = y^T E y = y^T y = y_1^2 + y_2^2 + y_3^2
$$

同时，二次型 $f(x)$ 转化为标准形：
$$
f(x) = y^T \Lambda y = \lambda_1 y_1^2 + \lambda_2 y_2^2 + \lambda_3 y_3^2 = 2y_1^2 + 4y_2^2 + 4y_3^2
$$

于是，原式可化为：
$$
\frac{f(x)}{x^T x} = \frac{2y_1^2 + 4y_2^2 + 4y_3^2}{y_1^2 + y_2^2 + y_3^2}
$$

由于 $y_1^2, y_2^2, y_3^2 \ge 0$ 且 $x \ne 0 \implies y \ne 0$，分母大于 0。
我们可以对分子进行放缩：
$$
2y_1^2 + 4y_2^2 + 4y_3^2 \ge 2y_1^2 + 2y_2^2 + 2y_3^2 = 2(y_1^2 + y_2^2 + y_3^2)
$$

因此：
$$
\frac{f(x)}{x^T x} = \frac{2y_1^2 + 4y_2^2 + 4y_3^2}{y_1^2 + y_2^2 + y_3^2} \ge \frac{2(y_1^2 + y_2^2 + y_3^2)}{y_1^2 + y_2^2 + y_3^2} = 2
$$

当且仅当 $y_2=0, y_3=0, y_1 \ne 0$ 时（即 $x$ 为 $\lambda=2$ 的特征向量时），等号成立。
所以，该表达式的最小值为 2。

**证毕。**

---

## ✅ 最终答案

**(I)** 正交矩阵 $Q$ 为：
$$
Q = \begin{pmatrix} -\frac{1}{\sqrt{2}} & 0 & \frac{1}{\sqrt{2}} \\ 0 & 1 & 0 \\ \frac{1}{\sqrt{2}} & 0 & \frac{1}{\sqrt{2}} \end{pmatrix}
$$
*(注：特征向量的顺序可以交换，对应的标准形顺序也会改变，但不影响 $Q$ 的正交性)*

**(II)** 证明过程如上所述，利用正交变换性质 $x^T x = y^T y$ 及特征值不等式 $2 \le \frac{2y_1^2+4y_2^2+4y_3^2}{y_1^2+y_2^2+y_3^2}$ 得证。