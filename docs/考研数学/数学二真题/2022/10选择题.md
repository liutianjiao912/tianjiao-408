## 🎯 问题回顾

设向量组：
$$
\alpha_{1}=(\lambda,1,1)^{T}, \quad \alpha_{2}=(1,\lambda,1)^{T}, \quad \alpha_{3}=(1,1,\lambda)^{T}, \quad \alpha_{4}=(1,\lambda,\lambda^{2})^{T}
$$
若向量组 $\alpha_{1},\alpha_{2},\alpha_{3}$ 与向量组 $\alpha_{1},\alpha_{2},\alpha_{4}$ 等价，求 $\lambda$ 的取值范围。

**选项：**
- (A) $\{0,1\}$
- (B) $\{\lambda|\lambda\in R, \lambda\ne-2\}$
- (C) $\{\lambda|\lambda\in R, \lambda\ne-1, \lambda\ne-2\}$
- (D) $\{\lambda|\lambda\in R, \lambda\ne-1\}$

---

## 第一部分：理论基础与解题策略

### 1.1 向量组等价的定义
两个向量组 (I) 和 (II) **等价**，意味着它们生成的向量空间相同。
即：组 (I) 中的每个向量都能被组 (II) 线性表示，反之亦然。

### 1.2 判别方法（秩的条件）
若两个向量组 $S_1 = \{\alpha_1, \alpha_2, \alpha_3\}$ 和 $S_2 = \{\alpha_1, \alpha_2, \alpha_4\}$ 含有公共向量 $\alpha_1, \alpha_2$，它们等价的**充要条件**是：
$$
\text{rank}(\alpha_1, \alpha_2, \alpha_3) = \text{rank}(\alpha_1, \alpha_2, \alpha_4) = \text{rank}(\alpha_1, \alpha_2, \alpha_3, \alpha_4)
$$
简而言之，两个向量组必须具有相同的秩，且合并后的秩也不增加。

**✅ 解题路线：**
1.  分别计算两个向量组构成的矩阵的行列式，找出满秩（秩为3）的条件。
2.  对于行列式为 0 的特殊 $\lambda$ 值，代入具体数值验证秩是否相等。

---

## 第二部分：计算行列式与秩

### 2.1 计算第一个向量组的行列式 $|A|$
记 $A = (\alpha_1, \alpha_2, \alpha_3)$，计算其行列式：
$$
|A| = \begin{vmatrix}
\lambda & 1 & 1 \\
1 & \lambda & 1 \\
1 & 1 & \lambda
\end{vmatrix}
$$
将第 2 行、第 3 行加到第 1 行：
$$
|A| = \begin{vmatrix}
\lambda+2 & \lambda+2 & \lambda+2 \\
1 & \lambda & 1 \\
1 & 1 & \lambda
\end{vmatrix} = (\lambda+2) \begin{vmatrix}
1 & 1 & 1 \\
1 & \lambda & 1 \\
1 & 1 & \lambda
\end{vmatrix}
$$
第 2 行减第 1 行，第 3 行减第 1 行：
$$
|A| = (\lambda+2) \begin{vmatrix}
1 & 1 & 1 \\
0 & \lambda-1 & 0 \\
0 & 0 & \lambda-1
\end{vmatrix} = (\lambda+2)(\lambda-1)^2
$$
**结论：**
- 当 $\lambda \ne 1$ 且 $\lambda \ne -2$ 时，$\text{rank}(A) = 3$。
- 当 $\lambda = 1$ 或 $\lambda = -2$ 时，$\text{rank}(A) < 3$。

### 2.2 计算第二个向量组的行列式 $|B|$
记 $B = (\alpha_1, \alpha_2, \alpha_4)$，计算其行列式：
$$
|B| = \begin{vmatrix}
\lambda & 1 & 1 \\
1 & \lambda & \lambda \\
1 & 1 & \lambda^2
\end{vmatrix}
$$
利用列变换简化计算。观察第 2 列 $\alpha_2=(1, \lambda, 1)^T$ 和第 3 列 $\alpha_4=(1, \lambda, \lambda^2)^T$。
执行列变换 $C_3 - C_2$（第 3 列减去第 2 列）：
$$
|B| = \begin{vmatrix}
\lambda & 1 & 0 \\
1 & \lambda & 0 \\
1 & 1 & \lambda^2-1
\end{vmatrix}
$$
按第 3 列展开：
$$
|B| = (\lambda^2-1) \cdot (-1)^{3+3} \begin{vmatrix}
\lambda & 1 \\
1 & \lambda
\end{vmatrix} = (\lambda^2-1)(\lambda^2-1) = [(\lambda-1)(\lambda+1)]^2
$$
**结论：**
- 当 $\lambda \ne 1$ 且 $\lambda \ne -1$ 时，$\text{rank}(B) = 3$。
- 当 $\lambda = 1$ 或 $\lambda = -1$ 时，$\text{rank}(B) < 3$。

---

## 第三部分：分类讨论确定 $\lambda$ 的范围

根据行列式的根 $\lambda = 1, -1, -2$，我们将实数域划分为不同情况进行讨论。

### 情况 1：$\lambda \ne 1, \lambda \ne -1, \lambda \ne -2$
此时：
- $|A| \ne 0 \implies \text{rank}(S_1) = 3$（生成整个 $\mathbb{R}^3$ 空间）。
- $|B| \ne 0 \implies \text{rank}(S_2) = 3$（生成整个 $\mathbb{R}^3$ 空间）。
- 两者生成的空间均为 $\mathbb{R}^3$，故**等价**。
- **此范围有效**。

### 情况 2：$\lambda = 1$
此时：
- $S_1: (1,1,1)^T, (1,1,1)^T, (1,1,1)^T \implies \text{rank}(S_1) = 1$。
- $S_2: (1,1,1)^T, (1,1,1)^T, (1,1,1)^T \implies \text{rank}(S_2) = 1$。
- 两者显然等价（都只生成向量 $(1,1,1)^T$ 所在的直线）。
- **$\lambda = 1$ 有效**。

### 情况 3：$\lambda = -1$
此时：
- 对于 $S_1$：代入 $|A| = (-1+2)(-1-1)^2 = 4 \ne 0$。
  故 $\text{rank}(S_1) = 3$。
- 对于 $S_2$：代入 $|B| = ((-1)^2-1)^2 = 0$。
  故 $\text{rank}(S_2) < 3$。
- 由于秩不相等（$3 \ne \text{rank}(S_2)$），两者**不等价**。
- **$\lambda = -1$ 无效**。

### 情况 4：$\lambda = -2$
此时：
- 对于 $S_1$：代入 $|A| = 0$。
  由于 $\alpha_1=(-2,1,1)^T, \alpha_2=(1,-2,1)^T$，两者不成比例，故 $\text{rank}(S_1) = 2$。
- 对于 $S_2$：代入 $|B| = ((-2)^2-1)^2 = 9 \ne 0$。
  故 $\text{rank}(S_2) = 3$。
- 由于秩不相等（$2 \ne 3$），两者**不等价**。
- **$\lambda = -2$ 无效**。

---

## 第四部分：综合结论

综合上述讨论，$\lambda$ 必须满足：
1.  不能取 $-1$（会导致 $S_2$ 降秩而 $S_1$ 满秩）。
2.  不能取 $-2$（会导致 $S_1$ 降秩而 $S_2$ 满秩）。
3.  可以取 $1$（两者同时降秩且空间相同）。
4.  可以取其他任意实数（两者均为满秩）。

因此，$\lambda$ 的取值集合为：
$$
\{\lambda \in \mathbb{R} \mid \lambda \ne -1 \text{ 且 } \lambda \ne -2\}
$$

对照选项：
- (A) 漏掉了大量实数解。
- (B) 包含了无效解 $-1$。
- (C) 正确排除 $-1$ 和 $-2$。
- (D) 包含了无效解 $-2$。

---

## ✅ 最终答案：**(C) $\{\lambda|\lambda\in R, \lambda\ne-1, \lambda\ne-2\}$**

---

## 📌 考点复盘

1.  **向量组等价的本质**：不仅要求线性相关性一致，更要求生成的**空间维数（秩）相等**。
2.  **满秩判别法**：对于 $n$ 个 $n$ 维向量，计算行列式是最快判断秩是否为 $n$ 的方法。
3.  **陷阱处理**：$\lambda=1$ 时，虽然两个矩阵的行列式都为 0，但这并不意味着不等价，必须具体检查降秩后的状态（本题中正好都是秩为1，等价）。而 $\lambda=-1$ 和 $\lambda=-2$ 分别只让其中一方降秩，导致秩不相等，从而不等价。