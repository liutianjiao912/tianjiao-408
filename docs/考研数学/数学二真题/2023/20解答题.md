## 🎯 问题回顾

计算二重积分：
$$
I = \iint_{D}\frac{1}{3x^{2}+y^{2}}dx dy
$$

其中积分区域 $D$ 位于第一象限，围成该区域的边界为：
* 曲线：$x^{2}+y^{2}-xy=1$
* 曲线：$x^{2}+y^{2}-xy=2$
* 直线：$y=\sqrt{3}x$
* 直线：$y=0$

---

## 第一部分：坐标变换与区域定限

### 1.1 选择坐标系

观察积分区域的边界特征：
1.  直线 $y=0$ 和 $y=\sqrt{3}x$ 都是过原点的射线，适合用极坐标的角度 $\theta$ 描述。
2.  曲线方程 $x^2+y^2-xy=C$ 关于原点也就是极径 $r$ 是二次齐次的。

因此，采用**极坐标变换**是最优解。
令：
$$
\begin{cases}
x = r \cos\theta \\
y = r \sin\theta
\end{cases}
$$
此时面积元素 $dx dy = r dr d\theta$。

### 1.2 确定积分限

**1. 确定角度 $\theta$ 的范围：**
* 直线 $y=0$ 对应 $\tan\theta = 0$，即 $\theta = 0$。
* 直线 $y=\sqrt{3}x$ 对应 $\tan\theta = \sqrt{3}$，即 $\theta = \frac{\pi}{3}$。
* 由于区域在第一象限，故 $\theta \in [0, \frac{\pi}{3}]$。

**2. 确定极径 $r$ 的范围：**
将 $x, y$ 代入曲线方程 $x^2+y^2-xy=C$：
$$
r^2\cos^2\theta + r^2\sin^2\theta - r^2\sin\theta\cos\theta = C
$$
$$
r^2(1 - \sin\theta\cos\theta) = C
$$
由此解出 $r$：
$$
r = \sqrt{\frac{C}{1 - \sin\theta\cos\theta}}
$$
记 $g(\theta) = 1 - \sin\theta\cos\theta$，则两曲线对应的极径分别为：
* 内边界 ($C=1$)：$r_1(\theta) = \frac{1}{\sqrt{g(\theta)}}$
* 外边界 ($C=2$)：$r_2(\theta) = \frac{\sqrt{2}}{\sqrt{g(\theta)}}$

因此，$r$ 的积分限为：
$$
\frac{1}{\sqrt{1 - \sin\theta\cos\theta}} \le r \le \sqrt{\frac{2}{1 - \sin\theta\cos\theta}}
$$

---

## 第二部分：代入积分计算

### 2.1 被积函数的变换

原被积函数为 $\frac{1}{3x^2+y^2}$，代入极坐标：
$$
\frac{1}{3r^2\cos^2\theta + r^2\sin^2\theta} = \frac{1}{r^2(3\cos^2\theta + \sin^2\theta)}
$$

### 2.2 建立累次积分

结合面积元素 $r dr d\theta$，积分表达式为：
$$
I = \int_{0}^{\frac{\pi}{3}} d\theta \int_{r_1(\theta)}^{r_2(\theta)} \frac{1}{r^2(3\cos^2\theta + \sin^2\theta)} \cdot r \, dr
$$
$$
I = \int_{0}^{\frac{\pi}{3}} \frac{1}{3\cos^2\theta + \sin^2\theta} \left( \int_{r_1(\theta)}^{r_2(\theta)} \frac{1}{r} \, dr \right) d\theta
$$

### 2.3 计算内层积分（关于 $r$）

$$
\int_{r_1(\theta)}^{r_2(\theta)} \frac{1}{r} \, dr = \ln r \Big|_{r_1}^{r_2} = \ln \left( \frac{r_2(\theta)}{r_1(\theta)} \right)
$$
代入 $r_1, r_2$ 的表达式：
$$
\frac{r_2(\theta)}{r_1(\theta)} = \frac{\sqrt{2}/\sqrt{g(\theta)}}{1/\sqrt{g(\theta)}} = \sqrt{2}
$$
于是内层积分的结果是一个**常数**：
$$
\ln(\sqrt{2}) = \frac{1}{2}\ln 2
$$

> 💡 **技巧点拨**：此处 $r$ 的上下限虽然都是 $\theta$ 的复杂函数，但它们的比值是常数。这一特性使得原本复杂的积分大大简化。

此时，原积分简化为：
$$
I = \frac{1}{2}\ln 2 \int_{0}^{\frac{\pi}{3}} \frac{1}{3\cos^2\theta + \sin^2\theta} \, d\theta
$$

---

## 第三部分：计算外层积分（关于 $\theta$）

### 3.1 积分变形

我们需要计算定积分：
$$
J = \int_{0}^{\frac{\pi}{3}} \frac{1}{3\cos^2\theta + \sin^2\theta} \, d\theta
$$
分子分母同时除以 $\cos^2\theta$（注意 $\theta \in [0, \pi/3]$，$\cos\theta \ne 0$）：
$$
J = \int_{0}^{\frac{\pi}{3}} \frac{\sec^2\theta}{3 + \tan^2\theta} \, d\theta
$$

### 3.2 换元求解

令 $u = \tan\theta$，则 $du = \sec^2\theta d\theta$。
变换积分限：
* 当 $\theta = 0$ 时，$u = 0$；
* 当 $\theta = \frac{\pi}{3}$ 时，$u = \sqrt{3}$。

代入得：
$$
J = \int_{0}^{\sqrt{3}} \frac{1}{3 + u^2} \, du
$$
利用标准积分公式 $\int \frac{1}{a^2+x^2}dx = \frac{1}{a}\arctan\frac{x}{a}$，此处 $a=\sqrt{3}$：
$$
J = \left[ \frac{1}{\sqrt{3}} \arctan \left( \frac{u}{\sqrt{3}} \right) \right]_{0}^{\sqrt{3}}
$$
$$
J = \frac{1}{\sqrt{3}} \left( \arctan(1) - \arctan(0) \right)
$$
$$
J = \frac{1}{\sqrt{3}} \cdot \frac{\pi}{4} = \frac{\pi}{4\sqrt{3}}
$$

---

## 第四部分：最终结果

将 $J$ 的值代回 $I$ 的表达式：

$$
I = \left( \frac{1}{2}\ln 2 \right) \cdot J = \frac{1}{2}\ln 2 \cdot \frac{\pi}{4\sqrt{3}}
$$
$$
I = \frac{\pi \ln 2}{8\sqrt{3}}
$$
或者写成有理化形式：
$$
I = \frac{\sqrt{3}\pi \ln 2}{24}
$$

---

## ✅ 最终答案

$$
\iint_{D}\frac{1}{3x^{2}+y^{2}}dx dy = \frac{\pi \ln 2}{8\sqrt{3}}
$$

---

## 📌 关键要点总结

1.  **极坐标的适用性**：当积分区域由射线 ($y=kx$) 和二次型曲线 ($Ax^2+Bxy+Cy^2=D$) 围成时，极坐标变换通常能将复杂的区域边界转化为 $r^2(\theta)$ 的形式。
2.  **变量分离技巧**：本题中，$r$ 的上下限之比为常数，这使得内层积分结果与 $\theta$ 无关，从而可以将常数提出来，极大地降低了计算难度。
3.  **三角有理式积分**：处理 $\int \frac{1}{A\cos^2\theta + B\sin^2\theta} d\theta$ 型积分时，通法是分子分母同除以 $\cos^2\theta$，转化为关于 $\tan\theta$ 的积分。