## 🎯 题目回顾

**题目**：已知向量 $\alpha_{1}=(1, 2, 3)^T, \alpha_{2}=(2, 1, 1)^T, \beta_{1}=(2, 5, 9)^T, \beta_{2}=(1, 0, 1)^T$。若 $\gamma$ 既可由 $\alpha_{1},\alpha_{2}$ 线性表示，也可由 $\beta_{1}, \beta_{2}$ 线性表示，则 $\gamma=$ （ ）。

(A) $k(3, 3, 4)^T, k\in \mathbb{R}$

(B) $k(3, 5, 8)^T, k\in \mathbb{R}$

(C) $k(-1, 2, 2)^T, k\in \mathbb{R}$

(D) $k(1, 5, 8)^T, k\in \mathbb{R}$

---

## 第一部分：题目分析与解题策略

题目要求寻找一个向量 $\gamma$，它位于由 $\{\alpha_1, \alpha_2\}$ 张成的子空间 $V_1$ 与由 $\{\beta_1, \beta_2\}$ 张成的子空间 $V_2$ 的**交集**中。即 $\gamma \in V_1 \cap V_2$。

**解题策略：**
1.  **建立方程**：根据 $\gamma \in V_1$ 且 $\gamma \in V_2$，$\gamma$ 可以分别写成两组基向量的线性组合，建立等式。
2.  **求解齐次方程组**：将向量代入，化简为齐次线性方程组，求出系数之间的关系。
3.  **确定向量**：将求得的系数代回线性组合表达式，得到 $\gamma$ 的通解形式。
4.  **方法验证（几何法）**：在 $\mathbb{R}^3$ 中，两个过原点的平面的交集通常是一条直线。可以通过法向量叉乘验证方向。

---

## 第二部分：代数法求解（推荐）

### 2.1 建立等式关系

设 $\gamma$ 的线性表示系数分别为 $x_1, x_2$ 和 $y_1, y_2$：
$$
\gamma = x_1 \alpha_1 + x_2 \alpha_2 = y_1 \beta_1 + y_2 \beta_2
$$

移项得齐次方程：
$$
x_1 \alpha_1 + x_2 \alpha_2 - y_1 \beta_1 - y_2 \beta_2 = \mathbf{0}
$$

### 2.2 构造矩阵并消元

将向量代入，得到以 $x_1, x_2, y_1, y_2$ 为未知数的方程组。系数矩阵为 $[\alpha_1, \alpha_2, -\beta_1, -\beta_2]$：

$$
\begin{pmatrix}
1 & 2 & -2 & -1 \\
2 & 1 & -5 & 0 \\
3 & 1 & -9 & -1
\end{pmatrix}
$$

对该矩阵进行初等行变换：

1.  $r_2 - 2r_1, r_3 - 3r_1$：
    $$
    \begin{pmatrix}
    1 & 2 & -2 & -1 \\
    0 & -3 & -1 & 2 \\
    0 & -5 & -3 & 2
    \end{pmatrix}
    $$
2.  $3r_3 - 5r_2$（消去 $r_3$ 的第二列元素）：
    $$
    3(-5) - 5(-3) = 0 \\
    3(-3) - 5(-1) = -4 \\
    3(2) - 5(2) = -4
    $$
    矩阵变为：
    $$
    \begin{pmatrix}
    1 & 2 & -2 & -1 \\
    0 & -3 & -1 & 2 \\
    0 & 0 & -4 & -4
    \end{pmatrix}
    $$

### 2.3 求解系数关系

由第三行可得：
$$
-4y_1 - 4y_2 = 0 \implies y_1 = -y_2
$$

令 $y_2 = k$（$k$ 为任意常数），则 $y_1 = -k$。

### 2.4 计算向量 $\gamma$

利用 $\{\beta_1, \beta_2\}$ 的线性组合直接计算 $\gamma$：
$$
\begin{aligned}
\gamma &= y_1 \beta_1 + y_2 \beta_2 \\
&= -k \begin{pmatrix} 2 \\ 5 \\ 9 \end{pmatrix} + k \begin{pmatrix} 1 \\ 0 \\ 1 \end{pmatrix} \\
&= k \begin{pmatrix} 1 - 2 \\ 0 - 5 \\ 1 - 9 \end{pmatrix} \\
&= k \begin{pmatrix} -1 \\ -5 \\ -8 \end{pmatrix} \\
&= -k \begin{pmatrix} 1 \\ 5 \\ 8 \end{pmatrix}
\end{aligned}
$$

由于 $k$ 是任意常数，$-k$ 也是任意常数，故 $\gamma$ 可以表示为 $k(1, 5, 8)^T$。

---

## 第三部分：几何法验证（叉乘法）

对于 $\mathbb{R}^3$ 中的两个平面，其交线的方向向量 $\mathbf{v}$ 垂直于两个平面的法向量 $\mathbf{n}_1, \mathbf{n}_2$。

1.  **计算平面 $V_1$ 的法向量 $\mathbf{n}_1$**：
    $$
    \mathbf{n}_1 = \alpha_1 \times \alpha_2 = \begin{vmatrix} \mathbf{i} & \mathbf{j} & \mathbf{k} \\ 1 & 2 & 3 \\ 2 & 1 & 1 \end{vmatrix} = (-1, 5, -3)^T
    $$

2.  **计算平面 $V_2$ 的法向量 $\mathbf{n}_2$**：
    $$
    \mathbf{n}_2 = \beta_1 \times \beta_2 = \begin{vmatrix} \mathbf{i} & \mathbf{j} & \mathbf{k} \\ 2 & 5 & 9 \\ 1 & 0 & 1 \end{vmatrix} = (5, 7, -5)^T
    $$

3.  **计算交线方向 $\mathbf{v}$**：
    $$
    \mathbf{v} = \mathbf{n}_1 \times \mathbf{n}_2 = \begin{vmatrix} \mathbf{i} & \mathbf{j} & \mathbf{k} \\ -1 & 5 & -3 \\ 5 & 7 & -5 \end{vmatrix} = (-4, -20, -32)^T
    $$
    提取公因数 $-4$，得方向向量 $(1, 5, 8)^T$。

结果与代数法一致。

---

## 第四部分：选项判定

计算结果为 $k(1, 5, 8)^T$。

* (A) $(3, 3, 4)^T$ —— 不符
* (B) $(3, 5, 8)^T$ —— 不符
* (C) $(-1, 2, 2)^T$ —— 不符
* (D) $(1, 5, 8)^T$ —— **符合**

---

## ✅ 最终答案

**(D)**