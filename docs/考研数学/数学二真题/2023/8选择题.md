## 🎯 题目回顾

设 $A, B$ 为 $n$ 阶可逆矩阵，$E$ 为 $n$ 阶单位矩阵，$M^{*}$ 为矩阵 $M$ 的伴随矩阵，则 $\begin{pmatrix}A&E\\ 0&B\end{pmatrix}^{*} =$ （ ）。

(A) $\begin{pmatrix}|A|B^{*}&-B^{*}A^{*}\\ 0&|B|A^{*}\end{pmatrix}$

(B) $\begin{pmatrix}|A|B^{*}&-A^{*}B^{*}\\ 0&|B|A^{*}\end{pmatrix}$

(C) $\begin{pmatrix}|B|A^{*}&-B^{*}A^{*}\\ 0&|A|B^{*}\end{pmatrix}$

(D) $\begin{pmatrix}|B|A^{*}&-A^{*}B^{*}\\ 0&|A|B^{*}\end{pmatrix}$

---

## 第一部分：解题核心公式

对于可逆矩阵 $M$，其伴随矩阵 $M^*$ 与逆矩阵 $M^{-1}$ 满足以下关系：
$$
M^{-1} = \frac{1}{|M|} M^* \implies M^* = |M| M^{-1}
$$

因此，我们解题的策略是：
1.  计算分块矩阵 $M = \begin{pmatrix} A & E \\ 0 & B \end{pmatrix}$ 的**行列式** $|M|$。
2.  计算分块矩阵 $M$ 的**逆矩阵** $M^{-1}$。
3.  利用公式 $M^* = |M| M^{-1}$ 得出结果。

---

## 第二部分：计算步骤

### 2.1 计算行列式 $|M|$

对于分块上三角矩阵，其行列式等于主对角线上各子块行列式的乘积：
$$
|M| = \left| \begin{pmatrix} A & E \\ 0 & B \end{pmatrix} \right| = |A| \cdot |B|
$$

### 2.2 计算逆矩阵 $M^{-1}$

设 $M^{-1} = \begin{pmatrix} X & Y \\ Z & W \end{pmatrix}$，根据逆矩阵定义 $M M^{-1} = \begin{pmatrix} E & 0 \\ 0 & E \end{pmatrix}$，建立方程组：
$$
\begin{pmatrix} A & E \\ 0 & B \end{pmatrix} \begin{pmatrix} X & Y \\ Z & W \end{pmatrix} = \begin{pmatrix} AX+EZ & AY+EW \\ BX+BW & BZ+BW \end{pmatrix} = \begin{pmatrix} E & 0 \\ 0 & E \end{pmatrix}
$$
(注：此处左侧 $BX+BW$ 应为 $B Z$，右下应为 $B W$。分块乘法展开为：第二行第一列 $0 \cdot X + B \cdot Z = BZ$，第二行第二列 $0 \cdot Y + B \cdot W = BW$)

修正后的方程组：
1.  **左下角**：$BZ = 0$。因 $B$ 可逆，故 **$Z = 0$**。
2.  **右下角**：$BW = E$。故 **$W = B^{-1}$**。
3.  **左上角**：$AX + EZ = E \implies AX = E$（因 $Z=0$）。故 **$X = A^{-1}$**。
4.  **右上角**：$AY + EW = 0 \implies AY = -W = -B^{-1}$。
    故 $Y = -A^{-1}B^{-1}$。

综上所述：
$$
M^{-1} = \begin{pmatrix} A^{-1} & -A^{-1}B^{-1} \\ 0 & B^{-1} \end{pmatrix}
$$

### 2.3 计算伴随矩阵 $M^*$

将 $|M|$ 和 $M^{-1}$ 代入公式 $M^* = |M| M^{-1}$：

$$
\begin{aligned}
M^* &= |A||B| \begin{pmatrix} A^{-1} & -A^{-1}B^{-1} \\ 0 & B^{-1} \end{pmatrix} \\
&= \begin{pmatrix} |A||B|A^{-1} & -|A||B|A^{-1}B^{-1} \\ 0 & |A||B|B^{-1} \end{pmatrix}
\end{aligned}
$$

利用公式 $|K|K^{-1} = K^*$（其中 $K$ 为矩阵）化简各子块：

1.  **左上角**：
    $$|B| \cdot (|A|A^{-1}) = |B| A^*$$
2.  **右下角**：
    $$|A| \cdot (|B|B^{-1}) = |A| B^*$$
3.  **右上角**：
    这里利用恒等式 $(XY)^* = Y^* X^*$ 的逆运用思路，或者直接结合：
    $$
    -|A||B|A^{-1}B^{-1} = -(|A|A^{-1}) (|B|B^{-1}) = -A^* B^*
    $$
    *(注意：矩阵乘法一般不交换，顺序保持为 $A^{-1}$ 在前 $B^{-1}$ 在后，分别配对 $|A|$ 和 $|B|$)*

所以：
$$
M^* = \begin{pmatrix} |B|A^* & -A^*B^* \\ 0 & |A|B^* \end{pmatrix}
$$

---

## 第三部分：选项比对

* (A) 左上角是 $|A|B^*$，错误。
* (B) 左上角是 $|A|B^*$，错误。
* (C) 左上角 $|B|A^*$ 正确，但右上角是 $-B^*A^*$。根据矩阵乘法顺序，我们的结果是 $-A^*B^*$。一般情况下 $A^*B^* \ne B^*A^*$，故排除。
* (D) 左上角 $|B|A^*$，右上角 $-A^*B^*$，右下角 $|A|B^*$。**完全匹配**。

---

## ✅ 最终答案

**(D)**