# 思维导图生成指南

## 📊 思维导图功能说明

项目使用 **markmap** 工具将 Markdown 格式的思维导图转换为可交互的 HTML 网页。

---

## 🚀 生成思维导图的命令

### 方式一：使用 pnpm 命令（推荐）

```bash
# 生成所有思维导图
pnpm build:mark-map
```

### 方式二：使用脚本

```bash
# 直接运行脚本
./scripts/mark-map

# 或使用 npx
npx node --loader ts-node/esm --no-warnings scripts/core/mark-map.ts
```

### 方式三：自动生成（开发时）

```bash
# 启动开发服务器时会自动生成思维导图
pnpm dev
# 等价于: pnpm build:mark-map && npx vuepress dev docs
```

---

## 📁 文件结构

### 源文件位置

```
docs/mark-map/
├── ds-map.md          # 数据结构思维导图
├── os-map.md          # 操作系统思维导图
├── ccp-map.md         # 计算机组成原理思维导图
├── cn-map.md          # 计算机网络思维导图
└── *.xmind            # XMind 原始文件（可选）
```

### 生成文件位置

```
docs/.vuepress/public/mind-map/
├── ds-map.html        # 数据结构思维导图 HTML
├── os-map.html        # 操作系统思维导图 HTML
├── ccp-map.html       # 计算机组成原理思维导图 HTML
└── cn-map.html        # 计算机网络思维导图 HTML
```

---

## 📝 思维导图 Markdown 格式

思维导图使用特殊的 Markdown 代码块格式：

````markdown
```mindmap
root(数据结构)
    (基础入门)
    (线性表)
    (栈和队列)
    (队列)
    (串)
    (树与二叉树)
    (图论)
    (查找)
    (排序)
```
````

### 格式说明

- 使用 `mindmap` 代码块类型
- `root()` 定义根节点
- `()` 定义子节点
- 使用缩进表示层级关系（4 个空格或 1 个 Tab）

### 示例

````markdown
```mindmap
root(计算机408)
    (数据结构)
        (线性表)
            (顺序表)
            (链表)
        (栈和队列)
    (操作系统)
        (进程管理)
        (内存管理)
    (计算机组成原理)
        (CPU)
        (存储器)
    (计算机网络)
        (物理层)
        (数据链路层)
```
````

---

## 🔧 工作原理

生成过程分为三步：

1. **清空目标目录**

   ```bash
   rm -rf docs/.vuepress/public/mind-map/*
   ```

2. **转换 Markdown 为 HTML**

   ```bash
   npx markmap --no-open docs/mark-map/ds-map.md
   npx markmap --no-open docs/mark-map/os-map.md
   # ... 其他文件
   ```

3. **移动 HTML 文件到目标目录**
   ```bash
   mv -f docs/mark-map/*.html docs/.vuepress/public/mind-map/
   ```

---

## 🌐 访问思维导图

### 在开发服务器中访问

1. 启动开发服务器：

   ```bash
   pnpm dev
   ```

2. 访问思维导图页面：
   - 数据结构: http://localhost:4200/mark-map/ds-map.html
   - 操作系统: http://localhost:4200/mark-map/os-map.html
   - 计算机组成原理: http://localhost:4200/mark-map/ccp-map.html
   - 计算机网络: http://localhost:4200/mark-map/cn-map.html

### 直接打开 HTML 文件

```bash
# 在浏览器中打开
open docs/.vuepress/public/mind-map/ds-map.html
```

---

## ✏️ 如何创建新的思维导图

### 步骤 1: 创建 Markdown 文件

在 `docs/mark-map/` 目录下创建新的 `.md` 文件，例如 `new-map.md`：

````markdown
---
title: 新思维导图
permalink: /mark-map/new-map.html
levels: 3
---

# 新思维导图

```mindmap
root(根节点)
    (子节点1)
        (子节点1.1)
        (子节点1.2)
    (子节点2)
        (子节点2.1)
```

<a href="../../mind-map/new-map.html" target="_blank">在线预览</a>
````

### 步骤 2: 生成 HTML

```bash
pnpm build:mark-map
```

### 步骤 3: 添加到侧边栏（可选）

编辑 `docs/mark-map/mark-map.sidebar.ts`，添加新思维导图的链接。

---

## 🛠️ 使用的工具

- **markmap-cli**: Markdown 转 HTML 思维导图的命令行工具
  - 版本: 0.18.11
  - 文档: https://www.npmjs.com/package/markmap-cli

---

## ⚙️ 配置说明

### 思维导图级别

在 Markdown 文件头部可以设置显示的层级：

```yaml
---
levels: 3 # 默认显示3层，可以调整
---
```

### 思维导图样式

生成的 HTML 文件支持：

- 缩放（鼠标滚轮）
- 拖拽
- 节点展开/折叠
- 搜索功能

---

## 🔍 常见问题

### Q: 思维导图没有生成？

A: 检查以下几点：

1. 确保 Markdown 文件在 `docs/mark-map/` 目录下
2. 确保文件扩展名是 `.md`
3. 确保 `mindmap` 代码块格式正确
4. 运行 `pnpm build:mark-map` 查看错误信息

### Q: 如何更新思维导图？

A:

1. 修改 `docs/mark-map/*.md` 文件
2. 运行 `pnpm build:mark-map` 重新生成
3. 刷新浏览器页面

### Q: 思维导图太大怎么办？

A: 可以调整 `levels` 参数，减少默认显示的层级。

---

## 📚 相关文件

- 生成脚本: `scripts/core/mark-map.ts`
- 脚本入口: `scripts/mark-map`
- 源文件目录: `docs/mark-map/`
- 输出目录: `docs/.vuepress/public/mind-map/`

---

## 💡 提示

- 思维导图会在 `pnpm dev` 时自动生成，无需手动运行
- 修改思维导图后，需要重新运行生成命令
- 生成的 HTML 文件可以独立打开，不依赖开发服务器
